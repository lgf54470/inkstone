# 看板模块改进执行计划（kanban improvement plan）

> 依据：`review.md`（合并版审查总报告，31 项台账）。分支：`kanban-improvement-qoder-qwen38f`（自 `dev` 53824e3c）。
> 约定：每个条目 = 一个原子提交；顺序执行；每项先写能失败的复现测试，修复后跑回归（typecheck + 相关单测 + 受影响门禁）再提交，提交后更新本文件状态。
> 状态图例：`[ ]` 待办 · `[~]` 进行中 · `[x]` 已提交（附 commit short hash）

## 基线

- [x] BASE-0 创建 worktree + `node_modules` 软链 + 复制 review.md
- [x] BASE-1 基线门禁全绿确认（typecheck / 全量 test:unit 1644 通过；首跑 1 个失败套件为 worktree 缺 blog-frontend/node_modules，软链后通过）

## 第一批 · 止血（P0 + 安全最小集，按风险从小到大）

- [x] K-01 日历「新建」把 dateStr 写进 status → 改传日期字段（review #4；`kanban-calendar-view.tsx:12,122,140`、`kanban-root-hooks.ts:240-258`）— 含 handleAddItem 去 string 联合、新增 handleAddItemInGroup 按 groupBy 归组（board 顺带修正 groupBy≠status 时误写 status）
- [x] K-02 详情弹窗早 return 后调 hooks → 拆外层判空 + Body（review #5；`kanban-item-detail.tsx:250-253`）
- [x] K-03 PDF 预览被 CSP（`object-src 'none'`）挡死 → 移除 `<object>`，新标签打开 + 图片/文本内联（review #12）
- [x] K-04 筛选开启时拖拽落点错位 → 落点契约改 pivot 锚定（review #6；`kanban-board-dnd.ts:35-53`）
- [x] K-05 子任务「复制名称」clipboard 无 catch → `?.` 守卫 + then/catch + toast（review #22c；`kanban-subtask-menu.tsx:81`）
- [x] K-06 「转换为项目」实现为提升子任务→顶级卡；「打开子任务」死路径移除（review #22a/b）
- [x] K-07 text/plain 拖拽兜底无校验 → 校验 data-item-id（review #18）
- [x] K-08 分组只按 id 匹配 → id/label 双匹配（review #20a；`filter-sort.ts:136`）
- [x] K-09 ID `Date.now()` 碰撞 → 不可碰撞生成（review #23）
- [x] K-10 头部进度条口径 → 传筛选后 items（review 共识#15）
- [x] K-11 undo 快捷键作用域 → 绑定实例容器、焦点归属判定（review #7；`kanban-history.ts:47-69`）
- [x] K-12 KanbanRoot ErrorBoundary（review #8；`registry.ts:143-151`）
- [x] K-13 全屏单根化四件套（review #1；moveInto + is-fullscreen 类 + 高度契约 + 内联占位 + 透传；含 Ctrl+Z 单实例化）
- [x] K-14 outline 写回升级 JSON + parse 正则吞标题修复（review #2）
- [x] K-15 冲突写回保留 dirty + 「未保存 · 重试/放弃」（review #3）
- [x] K-16 GET /api/kanban/file 鉴权 + DELETE metadata 缺失即拒（review #9；worker `kanban.ts:74,115`）
- [x] K-17 `safeKanbanUrl()` 协议白名单 + normalize 清洗（review #11）
- [x] K-18 附件删除接线 `deleteKanbanFile` + `kanbanName` 透传（review #10 删除侧）
- [x] K-19 上传 MIME 白名单/配额/节流对齐 organizer（review #10 增量）
- [x] K-20 甘特进度隐藏交互 → 可键盘进度控件（review #24b）
- [x] K-21 排序与拖拽静默冲突 → 明确反馈（review #24a）
- [x] K-22 详情标题每键提交 → 草稿 + Enter/blur（review #17）
- [x] K-23 hover-only 控件空白行 → 绝对定位叠加/常显（review 共识16）
- [x] K-24 子任务「复制名称」以外的 UI 微调（view-tabs timeline/gantt 图标区分）（review #29 局部）

## 第二批 · 持久化与性能

- [x] K1-01 `commitData` 函数式更新去 `state.data` 依赖 + 回调稳定化（review #13）
- [x] K1-02 dragover 同 cardId+position bail（review #13）
- [x] K1-03 视图状态按 viewId 持久化 + 7 死字段接线或删除（review #19）
- [x] K1-04 新建分组/批量泛化到 `activeView.groupBy`（review #20b）
- [x] K1-05 图表 dataset memo + 主题跟随 + i18n + ARIA（review #14）
- [x] K1-06 搜索 debounce/`useDeferredValue` + 表格全选一次 set（review #16）
- [x] K1-07 `--kanban-tag-*`/chart 色板接令牌 + contrast 门禁（review #28；含 text-white×10、!important×2）
  - 进度：7a ✅ 标签色板入 tokens.css + AA 重标定 + 声明/对比度门禁；7b ✅ chart 调色板改读令牌（删第二套 hex，主题/强调色/白底切换均重绘）；7c ✅ 9 处 text-white 改 `--accent-contrast`、时间轴/甘特「今天」徽标改强调填充配对比色（+ 填充×文字 AA 门禁与 text-white 扫描）。声称的 `!important×2` 不复现（见日志）
- [x] K1-08 死代码清理（entry.dark/locale、useMemo[props]、files.remove）（review #27）
  - 进度：三处全删；`entry.dark` 无人读且主题已由 CSS 令牌/图表观察器走，直接删；`locale` 名义上没人读、实际是宿主重挂的唯一语言刷新通道，删字段同时把订阅移进 `KanbanRoot`（新增 registry-locale.test.ts 2 例先红后绿守住）；`api.kanban` 命名空间两个别名零调用，连带改直接 re-export；tests/kanban-dead-code.test.ts 3 例扫描守住不回潮
- [x] K1-09 R2 存量孤儿回收（Cron 扫 kanban/ 前缀 + 删笔记联动）（review #10 存量）
  - 进度：新增 `src/worker/attachments/kanban-reclaim.ts`（以 R2 前缀列举为事实来源，按 `customMetadata.userId` 归组，引用集取自该账号 notes/blog_posts/community_templates 正文，24h 宽限期，单次 200 个上限），接进 `scheduled` Cron。选型：不并轨 attachments 表（见日志）；「删笔记联动」由回收器每次从现存笔记派生引用来实现，不在笔记删除事务里同步删 R2

## 第三批 · 完整度与规范收敛

- [x] K2-01 表格 columns schema 驱动列（review #20c）
  - 进度：a ✅ 表头/表体改由 `data.columns` 驱动（新建 `kanban-property-cell.tsx` 作列序/列宽/单元分派单一来源，附件走 item 级 `handleUpdateFiles`，删三个恒空表头与其文案键）；b ✅ text/number 草稿式行内输入（Enter/blur 单次提交、Escape 丢弃、`aria-label` 取本列名）、date 接 `KanbanDatePicker`、multi-select 接 `KanbanTagPicker` 并按列写回（新同步泛化出 `handleUpdateMultiSelect`，一次编辑=一次历史）；c1 ✅ 表头点击排序接已持久化的 `activeView.sorts`（不新增文件：排序逻辑在 `filter-sort.ts` 纯函数、表头控件在 `kanban-property-cell.tsx` 原生 button、下发在 `kanban-table-view.tsx`）；c2 ✅ 列显隐接已持久化的 `activeView.hiddenColumns`（同样不新增文件：切换纯函数在 `filter-sort.ts`、面板一节在 `kanban-view-options.tsx`、入口复用 header 已有的选项按钮）
- [x] K2-02 分享/导出/放映 kanban 快照通道（review #21）
  - 进度：`enhancePreview` 新增 `kanban?: 'live' | 'snapshot'`，与 mindmap/excalidraw 同口径三答（未声明=露栅栏）；新建 `lib/markdown/kanban/static.ts` 作纯 DOM 同步快照（按 activeView.groupBy 分组 + 本地化组名 + 标题 + 空板/坏体三态，刻意不套视图筛选）；实时预览走 `live`，编辑器 live preview、放映 deck、幻灯片缓存、链接悬停卡、分享页、导出文档走 `snapshot`；快照/源码通道摘掉只有活实例能应答的全屏与重试按钮；`tests/kanban-render-channel.test.ts` 源码扫描钉住每个调用点必须声明通道
  - 待办（K2-02 实测登记，均不在本项范围）：① `features/preview/card-content.ts` 的增强触发条件仍漏 `[data-excalidraw]`，只含白板绘图的笔记在悬停卡里同样停在加载态（本次只补了 `[data-kanban]`）；② `createKanbanCanvas(entry.editable)` 的 `is-readonly` 类无任何 CSS，看板 UI 恒为可交互，`editable` 只闸门写回——所以「只读实时挂载」并不是快照通道之外的可用答案，要么把该类接上真正的只读态，要么删掉这条死路径；③ `.kanban-error pre`（连同 `mindmap.css`/`excalidraw.css` 同类处）引用了仓库里从未定义的 `--code-font-size`/`--code-line-height`，实际落到继承值，令牌收敛时应改为既有 `--text-*`
- [ ] K2-03 浮层/控件迁移 overlay + a11y 专项（review #26/#29）
  - [x] a 浮层 ESC/点击外部/z-index 收敛（96ca97dc + 6b30445b）
    - 进度：实测看板全树 0 处 `useEscape`、5 份各自复制的 window-mousedown 关闭逻辑（报告记 1 份）；9 个面板（column/sort/filter/view-options/icon-picker/subtask-menu/date-picker/tag-picker/详情状态下拉）统一接 `components/overlay` 的 `useEscape` + 共享 `useClickOutside`，本地副本 `usePopoverDismiss`×2、`useMenuClickOutside`、`useDropdownDismiss` 全删；5 处 `z-50` 字面量改 `z-[var(--z-popover)]`（值同为 50，绘制顺序不变）；`kanban-popover-dismiss.test.ts` 18 例先红（9 例 ESC）后绿，含「下拉 ESC 只收起下拉、详情弹窗留着」一条（overlay 的 ESC 栈 LIFO）
    - 待办（本项实测登记，不在 a 范围）：焦点归还触发器未做——共享 `useEscape` 只负责关闭，`useDialogFocus` 是模态用的焦点陷阱，这 9 个面板按仓库约定（非模态浮层）不带；键盘打开面板后 ESC 后焦点落回何处，随 b 的 `aria-expanded`/`aria-controls` 一并处理
  - [x] b 触发器/面板 ARIA 关系与可访问名称（含 K2-01b 推迟的日期触发器逐列名称）（b8b9e063）
    - 进度：实测看板全树 `aria-haspopup`/`aria-expanded`/`aria-controls` 各 0 处、面板根 0 个 `id`，即 11 组触发器/面板对（列菜单、排序、筛选、视图选项、详情图标、子任务图标、子任务更多操作、详情状态下拉、日历、详情标签、卡片标签）在读屏里全部只是「一个按钮」；按 `components/overlay/submenu.tsx` 已有的写法逐对接上三属性（`aria-controls` 只在打开时挂），面板改收**必填** `panelId: string`——由持有 `open` 的一方 `useId()` 生成后下发，TS 强制每个新挂载点（含测试）都声明这条关系，可选 props 会被漏接；子任务「更多操作」触发按钮与菜单根补 `aria-label`（复用全站同一个 `common.more_actions`，不新增键），状态下拉根补所改属性名的 `aria-label`，其列表标记抽为同目录 `StatusOptionList`（`DetailStatusDropdown` 函数体加完 54 行违 50 上限，且 listbox 本就该与它的行组件 `StatusOptionItem` 同处表现层模块，不是为拆分而拆分）；K2-01b 推迟的日期触发器逐列名称落为 `KanbanDatePicker` 必填 `propertyName` + 触发器内 `span.sr-only`（值与属性名都读得到、画面上不重复出现，四处调用点已全部传值故不留可选分支）。新增 `ui/kanban-popover-aria.test.ts` 25 例（先红 25/25）：每组断言 `aria-haspopup` 等于面板 role、`aria-expanded` 由 `false` 翻 `true`、`aria-controls` 解析到文档里真实存在且带可访问名称的那个面板；另一例断言 ESC 关闭后焦点仍在触发按钮上——即 a 登记待办的焦点归还，实测只有标签面板因面板内 autoFocus 输入真正丢焦，改由 `TagCreatePopover.dismiss` 按「焦点是否仍在面板内」条件归还触发按钮（点击外部时指针已把焦点带走，不抢回）。`dismiss` 同时接住选项点击与 Enter 提交两条自关闭路径。全量 test:unit 243 文件 1897 测试 ✅，typecheck ✅，13 项静态门禁 exit=0 ✅
  - [x] c 表格补 `table/row/cell` 语义（d4d4a9fa）
    - 进度：实测表格视图整棵树 0 处表格语义（只有 7 个视图共用的那层 `role='region'`），读屏把一个条目行读成一串无归属的控件（review #29）。原生 `<table>` 会毁掉「每组一张圆角卡」的版式，按既定路线在既有 flex div 上补 ARIA role、画面零改动。`kanban-property-cell.tsx` 新增 `kanbanTableColumnCount(columns, hidden?)`（选择列与标题列画在 `kanbanPropertyColumns` 之外，故 +2），跨列行据此写 `aria-colspan`，隐藏列时随之重算；`KanbanPropertyCell` 根改 `role='cell'`、`KanbanTableHeaderCell` 根改 `role='columnheader'`。`kanban-table-view.tsx`：外框 `role='table'`（仍在命名它的 `region` 内），表头行 `role='row'` + 全选格 `role='columnheader'`，分组列表改 `role='presentation'`，「新建分组」按钮包进行 + 跨全列格，使该动作是表的一行而非漂浮内容。`kanban-table-group.tsx`：组卡 `role='rowgroup'`，组标题与组脚各改为 `row` > 跨全列 `cell`（flex 类上移到该格、边框与内边距留在行上，盒模型逐项等价）。`kanban-table-row.tsx`：条目外层 `role='presentation'`、横向行 `role='row'`、选择格 `cell`、标题格 `rowheader`（行因此有名字），展开的子任务面板同样改为跨全列的一行。新增 `ui/kanban-table-semantics.test.ts` 9 例（先红 8/9，第 9 例是 containment 不变式在无 role 时的空跑护栏）：表在 region 内、表头 5 格名称与顺序、条目行直读为 `cell/rowheader/cell…`、每行归属 `table|rowgroup` 且每格的父级是 `row`、每组自持其行（a 与 b 不混）、跨列格数与隐藏列后重算、新建分组动作是表的一行、子任务面板是跨全列的一行。`size:check` 拦到 `table structure` describe 体 56 行，把两条行/格用例分出 `rows and cells` describe（套件边界，非新增文件）。变异自检四次各杀具名测试：`kanbanTableColumnCount` 少算选择列 → 3 例红；组内计数漏传 `hiddenColumns` → 「recounts the span」红（该变异初次存活，据此把跨列断言扩到整棵视图并补新建分组格的跨度断言）；列表 `presentation` 去掉 → 3 例红；由 /tmp 备份还原。全量 test:unit 244 文件 1906 测试 ✅，typecheck ✅，13 项静态门禁 exit=0 ✅
    - 待办（本项实测登记，不在 c 范围）：①「版式未变」只按盒模型逐项核对（边框/内边距留在原元素，新增包裹层均为无样式 block），jsdom 不布局且 `e2e-visual.mjs` 无看板场景，故未实测——随 K2-05 把看板表格纳入浏览器门禁时，用 axe（`aria-required-parent`/`aria-required-children`、colspan 上限）与一次几何量测同时守住；届时也顺带证明「`presentation` 包裹层把 row/rowgroup 归到 table」依赖的 generic 节点展平约定在真实可访问性树里成立。②子任务展开 chevron 仍无可访问名称（新测试的 `subtaskToggle` 注释已点名），归 e——table/list 两处已由 K2-03e2 补名，看板卡与画廊卡的那条 `SubtaskSummaryBar`（`div role='button'`，名字只有「1/2」）随 e4 一并处理（e4 已改原生 button + `sr-only` 动作名，a07d6e0c）
  - [x] d view-tabs tablist 方向键与 `aria-controls`（0a595f83）
    - 进度：实测 `role='tablist'`/`role='tab'`/`aria-selected` 本就齐备，缺的是三样——每个 tab 各自是 Tab 落点、四个方向键无处理、`aria-controls` 全树 0 处且没有任何元素是 `role='tabpanel'`。`kanban-view-tabs.tsx`：导出 `kanbanViewTabId(panelId, viewId)` 作 tab 自身 id 的单一来源（面板只拿到 panelId，反推不出 viewId），选中项 `tabIndex=0` 其余 `-1`，`rovingTabIndex` 按 ArrowRight/ArrowLeft/Home/End 给目标下标（两端环绕）并由 `handleKeyDown` 同时移动焦点与选中项（APG 的自动激活）；`kanban-root.tsx`：`useId()` 生成 `viewPanelId` 同时下发头部与视图，视图容器改挂 `role='tabpanel'` + `id` + `aria-labelledby`（名字取自选中 tab，不再往视图那层 `region` 名上叠第二份可访问名称），`kanban-header.tsx` 的 `KanbanHeader` 改收**必填** `viewPanelId` 透传，漏接会被 TS 拒。面板改挂在原本那个 `<main>` 元素上（改为 `div role='tabpanel'`，类名不动）而非新增包裹 div：属性不影响盒模型，零版式风险，且顺带不再在 app shell 的 `<main>`（`app-shell.tsx:153`）里嵌套第二个 main 地标。新增 8 例（先红 8/9）：roving tabindex、四向键与两端环绕、无关键不动、每个 tab 的 `aria-controls` 指向面板、整棵看板里选中的 tab 与 `role='tabpanel'` 双向对上、方向键换视图后面板名跟着换到新 tab。`size:check` 通过（`KanbanMain` 函数体加注释后 47 行）。变异自检五次各杀具名用例：删 `tabIndex` → 1 例红；删 `onKeyDown` → 4 例红；删 tab 的 `aria-controls` → 3 例红；删面板 `aria-labelledby` → 2 例红；ArrowRight 改不环绕 → 环绕那例红；均由 /tmp 备份还原。typecheck ✅，13 项静态门禁 exit=0 ✅，全量 test:unit 244 文件 1914 测试 ✅
    - 待办（本项实测登记，不在 d 范围）：①面板刻意不加 `tabIndex={0}`：实测 8 个视图文件每个都渲染至少一个原生 `<button>`（最少的也有 1 个），Tab 从 tablist 直接落到面板首个控件，符合 APG；若日后出现无可聚焦控件的视图需重评。②面板现在套着两层有名字的结构——`tabpanel`（名取选中 tab）内含视图自带的 `role='region'`（`preview.kanban_view_*`「Board view」），读屏会连着听两遍近似名称；是否把 7 个视图的 `region` 收掉只留 tabpanel，归 e 一并判定
  - [ ] e 剩余 a11y 清点（图标按钮名称、嵌套 `role='button'`、拖拽 `aria-live`、subtask-menu → `Menu`）
    - [x] e1 图标按钮/裸输入补可访问名称 + 名称清点探针（5e4160ce）
      - 进度：新增 `ui/kanban-control-names.test.ts` 探针，挂真实看板后遍历交互元素，按 computed accessible name 计算顺序（`aria-labelledby` → `aria-label`/`title`/`alt`/`placeholder` → 关联 label → 内层 `img[alt]` → `textContent`）取名为空即失败。实测修三处：`DetailPropertyField` 的说明文字是既无 `for` 也不嵌套的裸 `<label>`（读屏拿不到名字），数字与文本分支改由外层 `<label>` 包裹（零新增 id、点击可聚焦、`display:flex` 下盒模型等价），日期分支降为 `<span>`（`KanbanDatePicker` 已按 `propertyName` 自名，留空 `<label>` 反而无主）；隐藏 `type=file` 输入与子任务标题输入各补 `aria-label`（新增 `preview.kanban_subtask_title` 双语键）
      - 局限（e2 才暴露）：探针的 `selectView` 只 `click()` 不校验切换结果，8 个视图用例实际量的都是同一个默认挂载视图
    - [x] e2 补回页签点击切换，并命名被它暴露的 table/list/分组控件（ea3459f4）
      - 进度：实测 0a595f83 把 tab 的 `onClick` 整条丢了——鼠标点页签完全不切视图，而探针因不校验切换仍全绿，是「用例全绿、覆盖为空」的合谋。`kanban-view-tabs.tsx` 恢复 `onClick`；`kanban-view-tabs.test.ts` 加两例（单击回调 + 整棵看板切到被点视图且面板 `aria-labelledby` 跟随）；探针 `selectView` 增断言「点击后该页签 `aria-selected` 为 true」。探针一旦真切视图立刻量出 table 7 个、list 1 个无名控件：行选择框、卡片详情（`MessageSquare`）、子任务勾选、子任务删除、标题展开器、分组折叠箭头，逐一补 `aria-label` 与 `aria-expanded`；新增 `preview.kanban_collapse_group`/`kanban_expand_group`（分组行折叠与列头折叠语义不同，不借 `*_column` 键）
      - 变异自检：删 `onClick` → 9 例红（除默认挂载的 board 外 7 个视图用例 + 2 条指针例）；四处名称分别删 → 各 1 具名例红；均由 /tmp 备份还原
    - [x] e3 名称清点扩到子任务展开后的面板（7b5f7aa5）
      - 进度：子任务行只在展开后才进文档，收起态的名称断言覆盖不到它。探针加 `expandSubtasks()`（按展开器自身的 `aria-label` 找到并逐个点开，断言至少有一个可展开，否则用例空跑）+ table/list 各一例；无名控件描述符带出 lucide 图标名，便于失败时一眼定位来源。变异自检：删子任务勾选名称 / 删除名称 → 各 1 具名例红；删标题展开器名称 → 2 例红（选择器先找不到展开器）。本提交为纯测试加固，未改产品代码
    - [x] e4 嵌套 `role='button'` 全部改真实控件（a07d6e0c）
      - 进度：新增 `ui/kanban-semantic-controls.test.ts` 13 例（首版 9 例即 7 红，热区与键盘移动四例在实现后补入并全绿）：board/table/list/gallery 四视图在子任务展开后 `role='button'` 计数为 0、board/list/gallery 各有一条**真实** `<button>` 打开详情、卡片主体作为指针热区仍能打开、折叠列与子任务汇总条是原生 button 且 `aria-expanded` 会翻、Alt+→ 从卡内真实控件冒泡到容器并提交跨列移动。`kanban-column-header.tsx` 的 `CollapsedColumn`（此前只有 `onClick`，键盘根本进不去）与 `kanban-card-subtasks.tsx` 的 `SubtaskSummaryBar` 直接改原生 `<button>`：`<button>` 内容模型只收 phrasing，内部 `<div>` 改 `span` 并补 `block`/`w-full` 保持版式；汇总条的动作名放 `span.sr-only`，可见的「1/2」计数与进度条不变，另补 `aria-expanded`。三处卡片容器（`kanban-card.tsx`、`kanban-gallery-view.tsx`、`kanban-list-view.tsx`）去 `role='button'`/`tabIndex`/Enter，只留 `onClick` 作热区，键盘与读屏走真实控件：看板卡沿用 `CardHeader` 的 'Card details'，画廊与列表把标题 `<span>` 换成 `<button>`（`ListRowLeading` 新增必填 `onOpen`；标题按钮与同排控件一样 `stopPropagation`，不让热区把同一个 handler 再跑一遍）。`handleCardKeyDown` 随之收窄为 Alt+←/→，「冒泡到容器仍生效」由那条键盘用例实证——该快捷键此前 0 测试覆盖，本轮补上。双语键 0 新增（复用 `kanban_expand_subtasks`/`kanban_collapse_subtasks`，名称清点的展开器选择器改按可访问名称匹配，并断言「至少有一个可展开控件」防空跑）。变异自检 9 次各杀具名用例：容器补回 `role='button'` → 1 红；删容器 `onClick` → 热区例红；删 `onKeyDown` → 键盘移动例红；列表/画廊标题退回 `<span>` → 各 1 红；折叠列退回 `div` → 1 红；汇总条退回 `div`／删 `sr-only` 名／删 `aria-expanded` → 各 3 红（后两条同时被「找不到可展开控件」护栏抓到），均由 /tmp 备份还原。typecheck ✅，13 项静态门禁 exit=0 ✅，全量 test:unit 246 文件 1940 测试 ✅
      - 局限：容器仍带 `onClick`（整卡可点是既有交互，本轮不改），合规依据是「无 role 的中性 div + 卡内真实控件」，`nested-interactive` 不再成立；真实浏览器里的焦点顺序、热区点击与 `sr-only` 播报未实测，归 K2-05 的看板浏览器门禁一并量
    - [x] e5 拖拽与键盘跨列移动补 `aria-live` 播报（cedbfee1）
      - 进度：实测基线与登记一致——看板全树只有 `kanban-write-status.tsx:15` 一处 `role='status'`（写入状态，与移动无关），两条移动路径各自直达 `props.onMoveItem`。改为单一出口：`useKanbanGroups` 更名 `useKanbanBoardMoves`，同时持有分组结果、播报文案 state 与 `handleMoveItem`，`useKanbanBoardDndState` 与 `handleMoveColumn` 都收这个包装（此前 dnd 收裸 prop，键盘路径收 hook 内另一份闭包，两条路径无法一起守住）。播报文案由纯函数 `kanbanMoveAnnouncement(groups, itemId, targetGroupKey)` 生成：来源组按「卡片当前落在哪个组」查，同组即返回 null（组内重排序不是换了位置），查不到来源组也返回 null（不播报一个可能没离开的列）。区域常驻：`[data-kanban-board]` 末尾一个 `span role='status' aria-live='polite' class='sr-only'`（`sr-only` 是绝对定位，flex 行内不占位，版式零改动）。走参数化键 `preview.kanban_moved_to_group`（`{title}`/`{group}`，列名经 `formatKanbanGroupLabel` 本地化）而非 `${a}: ${b}` 拼接，兑现 K2-04 那条登记。新增 `kanban-move-announcement.test.ts` 5 例（先红 5/5）：区域先于任何移动就在文档里（移动时才插入的 live region 常常不播）、键盘 Alt+→ 播「标题 + 目标列」、指针拖放落他列同样播、组内重排不播、首列再按 ← 无处可移不播；探针选择器限定在 `[data-kanban-board]` 内，避免头部那个写入状态 `role='status'` 顶替。拖放用例派发真实 `drop` 事件（带 `application/json` 载荷），量的是视图整棵树而非 hook 单测的手捏事件
      - 待办（本项实测登记，不在 e5 范围）：①整列拖放重排（`onReorderColumns`）仍无播报——它是「列的顺序」而非「卡片的位置」，需要另一条消息键与另一种判定，不在本轮夹带。②同一句播报连续重复时（把同一张卡在两列间来回移第二次，字符串完全相同）部分读屏不再复述，需在浏览器里实测后才能决定是否加清空/扰动。③`item.title` 为空时走 `preview.kanban_untitled` 兜底，该分支无测试覆盖（现探针两个条目都有标题）。④真实读屏是否播报、`aria-live` 与 `role='status'` 隐式语义在浏览器里的实际行为，归 K2-05 的看板浏览器门禁
      - 验证：typecheck ✅，13 项静态门禁 exit=0 ✅（`size:check` 先拦下 `KanbanBoardView` 52 行，把 hook 调用改回单行本行风格后 49 行通过；未拆组件），变异自检 6 次各杀具名用例（删区域 → 5 例红；不写 state → 2 播报名例红；去掉同组判定 → 组内重排那例红；dnd 改收裸 prop → 拖放那例红；区域移出 board region → 5 例红；键盘路径绕过包装 → 键盘那例红），均由 /tmp 备份还原；全量 test:unit（串行）247 文件 1945 测试 ✅
    - [x] e6 `kanban-subtask-menu.tsx` → `components/overlay` `Menu`（08506e8e）
      - 进度：基线与登记一致——`KanbanSubtaskMenu` 自建 `role='menu'` + `menuitem`，只挂 `useEscape`/`useClickOutside`，游标/焦点移入/归还三者全缺。前置条件先补：`Menu` 新增可选 `panelId`，写在 portal 那个 `role='menu'` 根节点上，b8b9e063 建立的 `aria-controls` 契约（`kanban-popover-aria.test.ts` 解析到真实面板）因此不因迁移而破，其余约 20 个调用点不必凭空造 id。`KanbanSubtaskMenu` 名字与 props 一字未动（唯一产品调用点 `kanban-subtask-list.tsx:81` 无需改），内部改渲染 `Menu`（`align='end'`、`width=176` 即旧 `w-44`、`label=t('common.more_actions')`）；四行改 `MenuItem` 数据（图标由组件改 ReactNode），convert 行仍按 `onConvertToItem` 有无给出，删除行 `tone='danger'` + `separatorBefore`（`role='separator'` 由 `Menu` 自己画，旧代码那条裸 `div` 一并消失），复制名称抽成 `copySubtaskName`，行内不再各自 `onClose()`——`Menu.handleClick` 先 `onSelect` 再 `onClose`。层级由 `--z-popover`(50) 升为 `--z-pop`(260)，portal 到 body 且落在卡片详情模态(250)之上
      - 待办（本项实测登记）：①登记说「三处测试要跟着改」，实测只有本组件的测试改了：`kanban-popover-dismiss.test.ts` 与 `kanban-popover-aria.test.ts` 一行未动即通过（前者传的 props 形状未变，后者用 `document.getElementById` 找面板，天然穿 portal），影响面高估。②版式确有变动（行高 `py-1`≈25px → 共享 `h-10 md:h-7.5`、圆角 `r-md` → `r-lg`、与触发器间距 4px → 5px），这是收敛到共享组件的既定代价，真实浏览器下的浮层几何与 axe 归 K2-05。③`Menu` 在 `open=false` 时仍挂载并跑完自身 hooks 才返回 null，与旧组件的早退不同（关闭态无副作用），后续若给这行做渲染优化要重新量。④详情模态里 Escape 只关子菜单、模态留在原地的嵌套，迁移前后用的是同一个 `useEscape` 栈，但浏览器未实测 → K2-05
      - 验证：`kanban-subtask-menu.test.ts` 重写为 10 例（先红 5/5：焦点移入第一行、方向键移游标、Enter 执行高亮行并关闭、`role='separator'`、把子任务交给 converter；旧面板本来就有的 id/名字/三例剪贴板保持绿），探针挂真实调用点 `KanbanSubtaskList` 并点它的触发器，面板经 `aria-controls` 解析。typecheck ✅，13 项静态门禁 exit=0 ✅（`comments:check` 先拦 5 条新注释，`node scripts/sync-comments-allowlist.mjs` 重算后 573 文件 / 3746 条通过），变异自检 7 次各杀具名用例（去 `panelId` → 7 例红；换面板名 → 名字那例；去 `separatorBefore` → 行集合那例；convert 改成恒给 → 同一例；duplicate 去掉 `onSelect` → Enter 那例；复制 `subtask.id` 而非 title → 剪贴板那例；在共享 `Menu` 里把 `useCursorFocus(open, …)` 改成 `false` → 焦点移入 + 方向键两例，证明本项继承的确实是共享键盘路径而非组件自己又写了一遍），均由 /tmp 备份还原；全量 test:unit（串行，最终树）247 文件 1952 测试 ✅
    - [x] e7 `tabpanel` 与视图自带 `region` 的重名判定（8610dea1）
      - 事实：0a595f83 之后视图容器是 `role='tabpanel'`（名取选中页签），其内 7 个视图各自还渲染 `role='region'` + `aria-label=t('preview.kanban_view_*')`（「Board view」），读屏进入一次视图会连着听两遍近似名称
      - 判定：收掉视图那层 `region`，名字只留在面板上。三条实测依据支撑这个方向而非另一个——①两处字符串同一个来源（`formatKanbanViewName` 的 `VIEW_NAME_MAP` 把 `preview.kanban_view_*` 映射到页签名），所以不是「两个不同名字选一」，是同一句念两遍；②`role='region'` 在 `src/client` 全树只此 7 处，仓库其余多视图表面（`renderer/containers.ts:279` 的 markdown 页签块）给面板命名到此为止，不再嵌一层 landmark，保留才是异类；③7 个视图只经 `KanbanRoot` 的 tabpanel 渲染，不存在脱离面板的单视图入口，「landmark 可达」在这里没有额外受众
      - 保留：表格视图的名字没有一起删，从被删的 `region` 下移到 `role='table'` 网格本身，K2-03c 建立的「命名数据网格」保证不因本轮而回退
      - 待办（本项实测登记）：①真实读屏在面板内的实际播报序列（是否确实两遍、收掉后是否仍报视图名）jsdom 量不到 → K2-05 的看板浏览器门禁。②landmark 导航模型的取舍按「与仓库其余表面一致」定，若将来确实要让看板视图出现在 landmark 列表里，正解是给外层容器（面板那一个）加名，而不是把 7 个内层 `region` 请回来
      - 验证：`kanban-view-tabs.test.ts` 新增 `KanbanRoot view naming` 块（`it.each` 7 个视图类型，先红：board/table/list/gallery/calendar/timeline/gantt 全部因内层 `region` 存在而挂），断言面板仍是 `role='tabpanel'`、`aria-labelledby` 指回选中页签、`firstElementChild` 非空（视图确实挂载）且面板内 `[role='region']` 计数为 0；`kanban-table-semantics.test.ts` 首例如实改为「网格自带名字、容器无 region」。选择器刻意同时断言 `panel.getAttribute('role')`，因为 `querySelectorAll` 看不见 panel 自身——补上这条之前，「把 panel 的 role 也一起改掉」这一版变异会存活。typecheck ✅，13 项静态门禁 exit=0 ✅（`comments:check` 先拦 4 条新注释，重算白名单后 573 文件 / 3750 条通过），变异自检 6 次各杀具名用例（board 重新包一层 region → 该视图那例红；gantt 同样 → 它自己那例，证明逐个视图都在读；`role='table'` 丢掉 `aria-label` → table-semantics 那例；表格视图在网格外面再套一层裸 `region` → 2 例；面板 `role='tabpanel'` 改成 `region` → 8 例；面板的 `aria-labelledby` 指向不存在的页签 → 10 例），均由 /tmp 备份还原；全量 test:unit（串行，最终树，start 11:28:57）247 文件 1959 测试 ✅
- [ ] K2-04 i18n 平台键/周起始/句子拼接/借键清理（review #30）
  - [x] a 快捷键提示按平台与语言出词（6fd0ab01）
    - 进度：基线两处——`kanban-header.tsx` 的两个 `title` 用 `${t('common.undo')} (Ctrl+Z)` 这种「译文标签 + 英文字面括号」拼装，`kanban-context-menu.tsx` 的历史两行把 `combo` 写成字面量 `'Ctrl+Z'`/`'Ctrl+Y'`。后者是错的承诺：`kanban-history.ts:64-75` 在所有平台绑定 mod+Z 与 mod+Shift+Z，`Ctrl+Y` 只在非 macOS 额外可用，macOS 读者照着按 ⌘Y 什么都不会发生。改法是回到仓库既有的一套机制而不是再造：`combo` 写规范 token（`'mod+z'`/`'mod+shift+z'`），共享 `Menu` 的行本来就把它交给 `Kbd`→`prettyCombo` 出平台图元（`components/overlay/menu.tsx:40`）；头部 `title` 走新参数化键 `preview.kanban_undo_shortcut`/`kanban_redo_shortcut`（`{shortcut}` 占位，zh 用全角括号），值取 `prettyCombo(...).join('+')`，与 `workspace/no-note-selected.tsx:15`、`list-empty.tsx:241` 同一写法
    - 待办（本项实测登记）：①`IS_MAC` 在 `lib/hotkeys.ts:20` 于模块首次读取时冻结，jsdom 里改 `navigator.platform` 不生效——本轮靠 `vi.resetModules()` + 先桩平台再动态取整张模块图来量 macOS 分支，若以后要给「同一文件内两种平台」补例，这个夹具是唯一路径，值得抽公共 helper（第三次出现时）。②`prettyCombo` 自身无测试（`src/client/lib/hotkeys.ts` 没有对应 test 文件），本轮的 macOS 例是它第一个外部消费者；补它自己的表由 K2-04 之外的门面负责。③真实 macOS 上 ⌘ 字形是否按预期绘制归 K2-05 的浏览器门禁
    - 验证：新增 4 例（先红 3/1：头部非 Mac 承诺真 chord、macOS 出 `⌘+Z`/`⌘+⇧+Z`、zh-CN 期望值从 `ZH_CN_MESSAGES` 读同一模板再比（测试文件写中文会被 `i18n:check` 拒），菜单两行断言规范 token）。typecheck ✅，13 项静态门禁 exit=0 ✅（`size:check` 先拦 describe 体超 50 行，把夹具移到模块作用域后通过；`comments:check` 先拦 4 条，重算白名单后 575 文件 / 3756 条通过），变异自检 6 次各杀具名用例（undo 改回 JSX 拼接 → zh 那例；redo 改回 `(Ctrl+Y)` → 3 例全红；`mod+z` 写成 `ctrl+z` → 只杀 macOS 那例，证明 `mod` 与 `ctrl` 两个 token 真的在两条不同分支上；删掉 title → 3 例；菜单 undo/redo 各改回字面量 → 同一例），均由 /tmp 备份还原；全量 test:unit（串行，最终树，start 12:02:33）247 文件 1963 测试 ✅
  - [x] b 日期选择器周起始改按 locale 数据（4db57a0b）
    - 事实：`kanban-date-picker.tsx:255` `const weekStart = locale === 'zh-CN' ? 1 : 0`，写死在语言码上；`weekStart` 是 `Date#getDay()` 的 0 基（周日=0），`DatePickerWeekRow` 与 `buildMonthCalendarDays` 都吃这一个数。Node 24 的 `new Intl.Locale(l).getWeekInfo()` 可用且给出 `firstDay`（1 基：zh-CN=1、en-US=7、de-DE=1、ar-EG=6），换算式 `firstDay % 7`；`locale.info.weekInfo` 这条旧路径在本运行时不存在（实测打印 `no info.weekInfo`），所以只能走 `getWeekInfo()` 并显式处理「运行时没有这个方法」的取值（不是静默回落到 0）
    - 进度：导出 `kanbanWeekStartFor(locale)`（`kanban-date-picker.tsx:36`）走 `getWeekInfo?.()?.firstDay`，拿到数就 `% 7` 换成 JS 口径；拿不到（旧运行时没有这个方法、或方法不给数）落回「本控件在这个 API 之前的答案」——zh-CN 周一、其余周日，这条回落写在该函数的文档注释里并由桩例守住，不是一行无人读的 `?? 0`。`DatePickerPopover` 只留一个 `weekStart` 给星期行与日期网格共用，两处不会各按各的口径排
    - 待办（本项实测登记）：①回落分支在真实运行时永远走不到（workerd 与 Chrome 都有 `getWeekInfo`），只有桩例读得到它——留桩例，别当死代码删。②en-US 那两例对「不换算/写死周日」这类变异天然无区分力（Sunday 恰好既是 en 的答案也是被写死的 0），区分力全在 zh 那例与 de/ar 两行断言上。③真浏览器里 narrow 星期图元（`一`/`S`）的实际绘制归 K2-05
    - 验证：新增 `kanban-date-picker.test.ts` 4 例（先红：整文件因 `kanbanWeekStartFor` 尚不存在而 4 例全挂）。期望值不写中文字面量——`i18n:check` 只允许中文出现在 `src/shared/locales/zh-CN/`，所以首个星期标签由 `narrowWeekday(locale, jsDay)` 现算（与 `DatePickerWeekRow` 同一个参考日 2024-01-07 = 周日），月首溢出格则按 2026-09-01 是周二写成 `['31','1','2']`/`['30','31','1']`。typecheck ✅，13 项静态门禁 exit=0 ✅（只有 `comments:check` 先拦 6 条新注释，重算白名单后 576 文件 / 3762 条通过），变异自检 6 次各杀具名用例（`firstDay % 7` 去掉换算 → 导出那例；整段 locale 数据读法改回语言码三元 → 同一例，证明 de-DE/ar-EG 两行确实读过 API；回落写死周日 / 写死周一 → 桩例各一次；网格与星期行分别改回吃常量 0 → zh 渲染那例，说明两处都真在消费同一个数），均由 /tmp 备份还原；全量 test:unit（串行，最终树，start 12:29:35）248 文件 1967 测试 ✅
  - [ ] g 日历视图的周起始与星期表头仍写死（K2-04b 实测登记）
    - 事实：`calendar-helpers.ts:33` 的 `getMonthWeeks` 以 `firstDate.getDay()` 直接当偏移（等于永远周日起），`kanban-calendar-view.tsx:60-72` 的 `CalendarWeekHeader` 按 sun→sat 固定顺序渲染 7 个 `preview.kanban_*` 键——两处自洽所以今天看不出问题，但和 b 修掉的控件是同一个决定的两半。看板之外还有四处同一写法：`features/sidebar/sidebar-calendar.tsx:85`、`features/list/note-list.tsx:352`、`features/settings/appearance-settings.tsx:280`、`components/use-date-range-popover.ts:107`（后者的 `weekStart` 类型被注成 `0 | 1`，接 ar-EG 的周六起点得先改这个联合）
    - 为什么不并进 b（按实测划的线）：`getWeekEventSegments` 的列号是拿 `week.findIndex` 现算的，`calendar-helpers.test.ts:14-23` 又只断言「7 天一列 / 当月 30 天」，所以这两处会跟着新的 `weekStart` 走、无需改；真正要动的是 `getMonthWeeks` 的签名（多收一个 `weekStart`）、`CalendarWeekHeader` 那 7 个键（全仓库只有此一个消费者，改成 `Intl` narrow 就得删掉它们）以及看板外那四处的 `0 | 1` 联合，跨模块且要 i18n 键删除，另开一轮
  - [x] c 句子拼接改参数化消息键（1c50b6c8，含 K2-03e1/e2 三条登记）
    - 事实：五处在 JSX 里把消息当积木拼：`kanban-batch-bar.tsx:57-58`（`{t('…batch_selected')} {count} {t('…batch_items')}`，一条句子的三个碎片，中文语序里数量该在动词前后各不同）、`kanban-column-header.tsx:117`、`kanban-table-group.tsx:53`、`kanban-table-row.tsx:54`（三处 `${action}: ${name}`，e2 新增两处 + 既有的一处）、`kanban-context-menu.tsx:81`（`${t('…batch_delete')} (${count})`）。另有 `kanban-chart-view.tsx:182-183` 的 `${summary}: ${counts}` 属 K1-05 图表摘要，本轮一并量但不在 c 的范围里动它
    - 进度：本轮按三种写法（`\$\{t\(`、`} {`、`t(...) + `）全仓扫过一遍，实测**六**处而非台账记的五处——漏的那处是 `kanban-table-row.tsx:94` 的 `{t('…subtasks')} ({count})`。六处各改成一条整句键（`kanban_batch_selected_count`、`kanban_expand_column_named`、`kanban_collapse_group_named`/`kanban_expand_group_named`、`kanban_delete_named`、`kanban_subtasks_count`、`kanban_batch_delete_count`），取值只进 `{count}`/`{name}` 占位；zh 侧把半角括号与冒号一并换成全角，数量在中文里回到动词后面。被替换掉的 5 个碎片键（`kanban_batch_selected`、`kanban_batch_items`、`kanban_expand_column`、`kanban_collapse_group`、`kanban_expand_group`）实测已无消费者，直接删；仍有人在用的裸键（`kanban_batch_delete`、`kanban_delete_subitem`、`kanban_subtasks`）保留不动
    - 待办（本项实测登记）：①`kanban-progress-bar.tsx:86` 的 `` `${seg.label}: ${seg.count} (${…}%)` `` 不在 c 里改——它的可变部分是**数据**（`opt.label` 是用户自己写的分组名）与百分数，消息里没有英文词可参数化；但 `:54` 兜底的 `label: 'Other'` 是硬编码英文，归 d/e。②`kanban-chart-view.tsx:182-183` 同理由 K1-05 负责。③两处本轮新量到、刻意留在范围外的拼接：`view.ts:118` 的 `${t('…render_failed')}: ${detail.slice(0, 500)}`（冒号后是截断的诊断串而非译文，参数化只换一个分隔符，且该路径由 `static.test.ts:121` 的 `toContain` 守着）与 `kanban-root-hooks.ts:402` 的 `${item.title} (${t('common.copy')})`（副本名会**写进栅栏成为数据**，按 locale 出词等于让同一张卡在中西文界面下产生不同的持久化标题，属产品决定）。④真实浏览器里读屏听到的整句（含全角括号停顿）归 K2-05
    - 验证：新增 `kanban-message-composition.test.ts` 13 例（`it.each` 双语 × 六处 + 单数一例），期望值一律从 `EN_US_MESSAGES`/`ZH_CN_MESSAGES` 现读模板再替换占位，因此资源里改了措辞测试跟着改、组件里退回拼接即红；表组内嵌的子任务计数标题首版选择器命中了分组表头单元格（`'Backlog1'`），改按「含新增子任务 form 的那个 cell」定位。typecheck ✅，13 项静态门禁 exit=0 ✅（只有 `comments:check` 先拦 2 条新注释，重算白名单后 577 文件 / 3764 条通过），变异自检 9 次各杀具名用例（六处各丢掉占位实参 → 批量条 3 例、收起列 3 例、子任务删除 2 例、子任务计数 2 例、菜单 2 例、分组两态互换 → 2 例；子任务标题/删除按钮、收起列三处改回拼接 → 分别杀 zh 一例 / 两例 / 三例。其中「改回拼接」那三例给出本轮的区分力事实：`kanban_subtasks_count` 的 en 期望值恰好等于拼接结果，所以**只有 zh 那例守得住语序**，`kanban_delete_named` 与 `kanban_expand_column_named` 则两种语言都红），均由 /tmp 备份还原；`kanban-semantic-controls.test.ts:187` 跟着换到新键（旧断言在旧键被删后如实报 `preview.kanban_expand_column: To Do`）；全量 test:unit（串行，最终树，start 13:12:50）249 文件 1980 测试 ✅
  - [x] d 标签删除按钮不再借 mindmap 键（0e19f5f8）
    - 事实：`kanban-tag-picker.tsx:55` 的 `aria-label={t('preview.mindmap_shortcut_remove')}`，读屏在看板标签上听到的是「思维导图」语境的删除文案，需换成看板自己的删除标签键
    - 进度：`TagChip` 先把「读者看见的那个名字」算成一份 `const name = formatKanbanOptionLabel(opt?.label ?? tag, 'tags')`，可见 `<span>` 与按钮 `aria-label` 同读它（不是各算一遍，免得可见文字与读屏名从此分叉），新键 `preview.kanban_remove_tag_named`（`Remove tag {name}` / 移除标签「{name}」）把名字放进整句而非 `action: name` 拼接，与 c 同一口径。删键后实测：看板全树里非 `kanban*` 的 `preview.*` 键只剩 `could_not_copy` 与 `open_in_new_tab` 两个通用键，`mindmap_shortcut_remove` 在本模块零命中，仍由 `mindmap-fullscreen.tsx:24` 导图自己使用；`preview.kanban_remove_tag`（裸键）未删，卡片头还在用（见 d 待办）
    - 待办：`kanban-card-header.tsx:53` 的卡片标签删除按钮走裸键 `preview.kanban_remove_tag`（有译文、无名字）——同属「删除不说删哪一条」，但它既不是借键也不是硬编码英文，不在 d 的判定口径内，登记进 h
  - [x] e 子任务与进度条的英文写死改为按语言出词（0e19f5f8）
    - 事实：`kanban-subtask-list.tsx:162` 完成切换按钮 `aria-label` 写 `'Mark incomplete'/'Mark complete'`，en/zh 下读屏都只有英文，违反「源码 UI 文案必须走英文 message id」。同一类还有 `kanban-progress-bar.tsx:54` 兜底分段写死的 `label: 'Other'`（K2-04c 实测登记，它被 `:86` 的 title 拼进用户可见提示）。K2-03e1 另登记的新增子任务输入框 `placeholder: 'Task name'` 实测**不存在**：全 `src/client` grep 无此字面量，`git log -S"Task name"` 在看板目录下也从未有过——该框一直用 `t('preview.kanban_add_subtask')`（`:258`），登记时把文案与键名记混了，别再去找
    - 进度：完成切换按状态取 `kanban_mark_complete_named`/`kanban_mark_incomplete_named` 并把子任务标题进 `{name}`（顺带从「只有动词」变成「说清改哪一条」）。进度条的兜底分段不再存英文字面量，改存 `labelKey: 'preview.kanban_status_other'`，`Segment` 因此分成 `label?`（真实分组＝看板作者写的名字，是数据）与 `labelKey?`（兜底桶＝消息），`title` 经 `segmentLabel(seg)` 出词。**为什么在渲染时翻译**：`segments` 算在 `useMemo([items, statusColumn])` 里，若在计算时 `t()` 就把语言冻在上一次求值的时刻——与 f 同一类病，本处按「数据进 memo、文案留渲染」避开，不去动 f 的根因。百分数与计数按 c 的判定属数据，不动
    - 待办：①「在计算时翻译」这条不变量无探针可守（jsdom 单次挂载只有一种语言，量不到切换时序），归 f 的根治一并处理。②`kanban-progress-bar.tsx:93` 的分段 `key={idx}`（分段集合会随数据增删而位移）是列表 key 规范问题，不属 i18n，登记不夹带
    - 验证（d+e）：新增 `kanban-label-language.test.ts` 6 例（`it.each` 双语 × 三处），期望值经共享夹具 `kanban-bilingual-labels.test-helpers.ts` 从资源现读模板再填占位（测试文件写中文会被 `i18n:check` 拒），`message-composition.test.ts` 同时改为消费该夹具（-63/+16 行，13 例不变）；新探针首跑先红 6/6（报 `… is not a message in the en-US resource` 与 `nothing names the incomplete subtask`）。`kanban-table-view.test.ts:419` 的标签删除定位器随新键更新（原本按借来的键找按钮，那条测试替组件记住了错误的文案）。typecheck ✅，13 项静态门禁 exit=0 ✅（`comments:check` 先拦 5 条新注释 + 1 条因抽夹具而搬了家的旧注释，重算白名单后 580 文件 / 3768 条通过），变异自检 7 次各杀具名用例（删键改回借键 → 2 例；改回英文字面量 → 2 例；两态键互换 → 2 例；不传 `{name}` → 2 例；名字改取原始 id → 2 例，证明命名的确实是选项标签而非存储值；兜底段改回 `label: 'Other'` → 只杀 zh 一例——en 的 'Other' 恰好等于写死值，本轮区分力仍在中文侧；`title` 绕开 `segmentLabel` → 2 例），均由 /tmp 备份还原；全量 test:unit（串行，最终树，start 13:47:19）250 文件 1986 测试 ✅
  - [ ] f 18 个 memo 组件的 locale 重绘根治（需决策，K2-01c1 登记）
    - 事实：看板 18 个 `memo(...)` 组件直接渲染 `t()` 标签，稳定性目前只靠「回调 props 每次渲染新建」意外维持；一旦有人把写入函数 `useCallback` 化，memo 就 bail，切语言后标签不再重绘（`registry-locale.test.ts` 会红）。根治方向二选一（订阅下沉到渲染标签那层 / 这些组件不再 memo），属设计决策，动手前需与用户确认
  - [ ] h 图标 / emoji / 色名仍以内部 id 作可访问名称（d+e 实测登记）
    - 事实：`kanban-icon-picker.tsx:88-89` 把 lucide 图标名同时用作 `title` 与 `aria-label`（读屏逐字母念 `ArrowRight`），而该名字正是持久化值 `lucide:${name}` 的一部分——改显示文案可行，改存储值会动栅栏；`:66` 的 emoji 选项 `aria-label={emoji}` 会让读屏把表情逐符念出来（或未念）。`kanban-tag-picker.tsx:76-77` 配色圆点的 `title`/`aria-label` 交的是色名 id（`slate`、`coral`…）。加 `kanban-card-header.tsx:53` 的裸键 `preview.kanban_remove_tag`（有译文但不说删哪个标签）。这一批不是「漏了语言」而是「名字本身是内部标识」，要先定产品口径（图标/颜色是否给人名、emoji 是否需要名字），且牵动持久化值的稳定性，另开一轮
- [ ] K2-05 surfaces/e2e-visual/axe 纳入全屏看板场景（review #31）

## 进度日志

| 日期 | 条目 | commit | 回归结果 |
| --- | --- | --- | --- |
| 2026-09-18 | BASE-0/BASE-1 + plan/review 登记 | 9b282dce | typecheck ✅，kanban 61 测试 ✅ |
| 2026-09-18 | K-01 日历/分组新建落字段 | 980c9926（软链误入库由 a7c5444d 移除） | 新增 kanban-add-operations.test.ts 5/5 ✅（先红后绿），全量 test:unit 1644 ✅，typecheck ✅ |
| 2026-09-18 | K-02 详情弹窗 hooks 早退 | e3f09756 | 新增 kanban-item-detail.test.ts 2/2 ✅（修复前捕获 "Internal React error: Expected static flag was missing"，修复后归零），kanban 68 测试 ✅，typecheck/size/escape ✅ |
| 2026-09-18 | K-03 PDF 预览去 `<object>` | b365a151 | 新增 kanban-file-preview-modal.test.ts 2/2 ✅（修复前 object 存在为红），typecheck/tokens/hardcoded ✅ |
| 2026-09-18 | K-04 拖拽落点 pivot 锚定 | 572bf9b8 | dnd.test.ts 重写为 pivot 契约 + 筛选/after/自拖 3 条回归 ✅（先红后绿），kanban 73 测试 ✅，全量 test:unit 1652 ✅，typecheck ✅ |
| 2026-09-18 | K-05 复制名称 clipboard 兜底 | 1e934632 | 新增 kanban-subtask-menu.test.ts 3/3 ✅（修复前无 clipboard 即同步抛错为红），kanban 76 测试 ✅，全量 test:unit 1655 ✅，typecheck/size ✅ |
| 2026-09-18 | K-06 子任务死操作 | a45ddda2 | 新增 kanban-convert-subtask.test.ts 7/7 ✅（先红后绿），kanban 83 测试 ✅，全量 test:unit 1662 ✅，typecheck/size/i18n ✅ |
| 2026-09-18 | K-07 拖拽落点归属校验 | 889d584c | 新增 kanban-board-dnd.test.ts 5/5 ✅（3 例外部拖入先红后绿），kanban 88 测试 ✅，全量 test:unit 1667 ✅，typecheck/size/escape/comments/i18n ✅ |
| 2026-09-18 | K-08 分组 id/label 双匹配 | 67feb0e6 | filter-sort.test.ts 新增 1 例 ✅（先红后绿：label 存值归 in_progress 组、乱值仍进 No Status），kanban 89 测试 ✅，全量 test:unit 212 文件 ✅，typecheck/size/comments ✅ |
| 2026-09-18 | K-09 ID 防碰撞生成 | 70dffabc | 新增 id.test.ts 2/2 ✅（先红：模块不存在即失败；2000 连击无重复），9 处 Date.now 生成点全部接线，kanban 91 测试 ✅，全量 test:unit 213 文件 ✅，typecheck/size/comments ✅ |
| 2026-09-18 | K-10 进度条改可见口径 | 451d8bfc | 新增 kanban-header.test.ts 2/2 ✅（先红：筛选态仍按全量算 50%），header 增 visibleItems 并由 root 传 viewData.items，kanban 93 测试 ✅，全量 test:unit 214 文件 ✅，typecheck/size/escape/comments ✅ |
| 2026-09-18 | K-11 undo 快捷键绑实例容器 | 73b05d10 | 新增 kanban-history.test.ts 3/3 ✅（先红 2：body 触发与他板隔离；修复后全绿），keydown 从 window 改绑 containerRef，root 容器 tabIndex=-1 承接空白点击焦点，kanban 96 测试 ✅，preview 套件 149 ✅，全量 test:unit 215 文件 ✅，typecheck/size/comments ✅ |
| 2026-09-18 | K-12 实例 ErrorBoundary | 02d3cc3b | 新增 registry-error-boundary.test.ts 2/2 ✅（先红 1：注入抛错后无错误态；修复后显示源码+提示、健康板不受影响），registry 渲染统一包 KanbanRootBoundary，kanban 98 测试 ✅，全量 test:unit 216 文件 ✅，typecheck/size/escape/hardcoded/comments ✅ |
| 2026-09-18 | K-13 全屏单根化四件套 | 9329eced | 新增 2 例单根测试（先红：overlay 内无活 canvas/内联无 h2；修复后 moveInto/moveBack、is-fullscreen 类、占位不塌、退出钮走 onCloseFullscreen、撤销栈跨搬移）✅，全屏二次根删除，搬移后重渲染走微任务防与 commit 抢跑，kanban 98+preview 153 ✅，全量 test:unit 216 文件 1679 测试 ✅，typecheck/size/comments/i18n/全部门禁 ✅ |
| 2026-09-18 | K-14 outline 写回升级 JSON + 方括号标题转义 | 7dacb6ff | 新增 write.test.ts 2 例 + outline.test.ts 转义往返 1 例（先红后绿）：flushKanbanEntry 首次写回把 outline 栅栏升级为 JSON，子任务/附件/views/标题不再静默丢弃；parse 两处正则加 (?<!\\) 反向断言并在 cleanText 还原 \[ \]，serialize 对标题转义方括号。mindmap 式「转换为 JSON/大纲」菜单动作与 undo toast 未纳入本提交（另项跟进）。kanban+preview 30 文件 156 ✅，全量 test:unit 217 文件 1682 测试 ✅，typecheck/size/comments/全部门禁 ✅ |
| 2026-09-18 | K-15 冲突写回未保存态 + 重试/放弃 | e7ab152b | 新增 kanban-unsaved.test.ts 3 例 + write.test.ts 3 例（先红后绿）：flush 失败置 entry.unsaved、改动留在内存；header 持久「未保存 · 重试/放弃」徽章（role=status，重试走 fence-edit 按当前正文重定位，放弃经 commitData 回到最后落盘体且不再触发写）；mountBlock 在 unsaved 时不再改 ref/重解析，preview 重渲染不冲掉未写入编辑。已知边界：unsaved 期间栅栏既移位置又被改 body 时按 index 匹配失败会重建实例（深层 rehost 问题，另项）。kanban+preview 31 文件 162 ✅，全量 test:unit 218 文件 1688 测试 ✅，typecheck/size/comments/全部门禁 ✅ |
| 2026-09-18 | K-16 文件接口鉴权 + 属主校验 | 276abe3d | kanban-routes.test.ts 扩至 7 例（先红后绿）：GET /file 加 requireAuth（未登录 401），读取前比对 customMetadata.userId 与当前用户（他人 403，无属主 metadata 的旧对象按不可读处理而非公开）；DELETE 由「有 ownerId 且不匹配才拒」收紧为「无 ownerId 即拒」，与 GET 同规则。全量 test:unit 218 文件 1692 测试 ✅，typecheck/11 项静态门禁 ✅ |
| 2026-09-18 | K-17 栅栏 URL 协议白名单 | cc6aa8cf | 新增 url.ts safeKanbanUrl()（放行 /api 相对路径、http(s)、blob:、data:image/*；拒 javascript:/file:/data:text、// 协议相对、控制符混淆，共 4 例 url.test.ts 先红后绿）+ body.test.ts 3 例：normalizeKanbanData 经 assertFenceUrlsAreSafe 清洗 item.cover 与 files[].url，非法协议整体走错误态提示不静默丢字段。UI 侧未逐处过滤：normalize 是唯一数据入口（上传回包为自家 /api 相对 URL），与 review #11 所列 3 个 UI 文件收敛为此单点（默认渲染数据均已过闸）。顺带把默认列/视图提为 defaultKanbanColumns()/defaultKanbanViews() 以满足 50 行函数上限。kanban+preview 32 文件 169 ✅，全量 test:unit 219 文件 1699 测试 ✅，typecheck/11 项静态门禁 ✅ |
| 2026-09-18 | K-18 附件删除接线 + 上传命名空间透传 | c27f3cbb | 新增 kanban-files-cell.test.ts 5 例 + url.test.ts kanbanFileLocation 3 例（先红后绿）：Trash2 删除由「只摘 JSON 引用」改为先摘引用、再按文件自己存的 url/r2Key 解析 (kanbanName, filename) 调 deleteKanbanFile 真删 R2 对象（deleteKanbanFile 自此有真实调用方）；外部/blob/data 文件只摘引用不瞎调 API；服务端拒绝时回滚引用并 toast（新增 preview.kanban_file_delete_failed 双语键）。上传命名空间经 KanbanFilesScope context 由 registry（entry.noteId）→ KanbanRoot 透传，不再全落 kanban/default/；删除按存量 URL 寻址，天然满足「旧前缀双读」，不迁历史 key。review #10 的孤儿清理 Cron、并轨 attachments 选型与配额（K-19）不在本项。kanban+preview 33 文件 177 ✅，全量 test:unit 220 文件 1707 测试 ✅，typecheck/11 项静态门禁 ✅ |
| 2026-09-18 | K-19 上传 MIME 白名单/配额/节流 | 7ae296fe | kanban.ts /upload 对齐 organizer 样板（先红后绿 6 例）：逐小时节流 consumeAttemptBudget（key kanban-upload:${userId}，LIMITS.attachmentUploadsPerHour/1h，ThrottleError→429）；form 改经 readFormDataWithinLimit 强制 multipart 与体积上限；缺 FILES 绑定由静默假 200 改为显式 503；配额=attachments 表 SUM(size) + R2 `kanban/` 前缀分页 scan（customMetadata.userId 过滤）超 attachmentQuotaBytesR2 → 413 且不放 put；mime 由直取 file.type 改为 safeAttachmentMime 白名单+图片签名嗅探（伪装 png 落 octet-stream）。tests/kanban-routes.test.ts 依 files-routes 惯例迁入 node 工程（jsdom 无法解析 multipart 请求体）。孤儿清理 Cron 仍属 review #10 后续，不在本项。全量 test:unit 220 文件 1713 测试 ✅，typecheck/11 项静态门禁 ✅ |
| 2026-09-18 | K-20 甘特进度改可键盘控件 | baff9376 | 新增 kanban-gantt-view.test.ts 3 例（先红后绿）：进度编辑从条形上「仅双击、(p+25)%125 回环归零、无键盘无提示」的隐藏交互，改为侧栏 Progress 列的项目库 Slider（原生 input[type=range]，0..100 step 5，aria-label 走 preview.kanban_progress，值文本自带 %）；条形移除 onDoubleClick，单击开详情不变，滑块点击 stopPropagation 不冒泡开详情。全量 test:unit 221 文件 1716 测试 ✅，typecheck/11 项静态门禁 ✅ |
| 2026-09-18 | K-21 排序与拖拽冲突明确反馈 | e318400c | 新增 kanban-manual-move.ts + kanban-manual-move.test.ts 2 例（先红后绿）：makeMoveItemClearingSorts 在 sorts 生效时拖拽落卡即清空排序并 toast（preview.kanban_sort_cleared_for_drag 双语键），手动顺序立即可见，不再「数据变了显示没变」；无排序时静默不提示。useKanbanRootState 经 useMoveItemClearingSorts 接线替换 items.handleMoveItem（root-hooks 因 500 行/50 行上限拆出接线 hook）。全量 test:unit 222 文件 1718 测试 ✅，typecheck/11 项静态门禁 ✅ |
| 2026-09-18 | K-22 详情标题草稿提交 | 42fcdf0e | kanban-item-detail.test.ts 新增 3 例（先红后绿）：DetailHeader 标题从每键 onUpdate（每次击键产生一条历史/写回）改为本地草稿——Enter 或失焦（focusout）提交一次、Escape 还原为上次提交值、切换详情目标（itemId 变化）时渲染中重置草稿；input 带 data-owns-escape 沿用既有豁免约定，编辑中按 Escape 不误关 Modal。review #17 其余项（MetricCards 栅格、description 限长、history 合并窗口）不在本项范围。全量 test:unit 222 文件 1721 测试 ✅，typecheck/11 项静态门禁 ✅ |
| 2026-09-18 | K-23 hover-only 空白行与焦点可见 | ad1ac1b9 | 新增 kanban-card.test.ts 4 例（先红后绿）+ tests/kanban-hover-focus.test.ts 源码扫描 1 例：卡片无标签时整行 hover-only 头部（选择框/加标签/详情钮）不再占位——KanbanCard 按 cardSize 内边距把该行绝对定位叠加到卡片顶部（review 截图「卡片顶部空白行」根因），GalleryTagsHeader 同型同修（根节点补 relative，无标签行叠加到封面左上）；有标签时保持常规流。10 处 opacity-0 悬停控件（卡头×4、gallery、附件删除、日历新建、子任务×2、标题铅笔）全部补 focus-visible:opacity-100，Tab 聚焦不再落在隐形按钮上；扫描测试守住该目录后续新增。全量 test:unit 224 文件 1726 测试 ✅，typecheck/11 项静态门禁 ✅ |
| 2026-09-18 | K-24 view-tabs timeline/gantt 图标区分 | 4c9bf705 | 新增 kanban-view-tabs.test.ts 1 例（先红后绿）：timeline 与 gantt 两个视图页签原本共用 SlidersHorizontal，用户无法从图标区分两种时间类视图；改为 Timeline 与 ChartGantt 两个语义图标（lucide 1.28 既有导出），测试断言两页签 svg 不同且 gantt 图标带 gantt 语义类。review #29 的 tablist 方向键/aria-controls 与其余图标可访问名称不在本项（K-24 只覆盖图标区分局部）。全量 test:unit 225 文件 1727 测试 ✅，typecheck/11 项静态门禁 ✅ |
| 2026-09-18 | K1-01 commitData 稳定化与回调去 data 依赖 | fbf97a8b | kanban-history.test.ts 新增 3 例（先红后绿）：commitData 用 dataRef/onUpdateRef 改 useCallback([])——跨 commit 身份不变、函数式 updater 始终对最新数据求值、解析结果照常转发 onUpdateData。kanban-root-hooks.ts 把移动/改题/改卡/删卡/列四操作/批量改状态/批量删除/复制共 11 个回调改函数式 commit 去掉 data 依赖（useKanbanSelection/useKanbanColumnOperations/useKanbanItemMutations 签名随之收窄）；Add 系与 handleConvertSubtaskToItem 保留 data 依赖并注明原因（新条目/降级父项身份要同步交给 setDetailItem）。kanban-board-dnd.ts 六个 handler 改 useCallback+drag 状态/回调 ref，drop 恒路由到最新 onMoveItem（新增 2 例断言身份稳定与最新路由）；useMoveItemClearingSorts 改经 sortsRef 调用期读取排序，memo 只跟 moveItem（新增 1 例断言跨渲染身份稳定）。review #13 的 dragover 同值 bail 属 K1-02。全量 test:unit 225 文件 1733 测试 ✅，typecheck/11 项静态门禁 ✅ |
| 2026-09-18 | K1-02 dragover 同目标帧 bail | 58ea826b | kanban-board-dnd.test.ts 新增 1 例（先红后绿）：handleCardDragOver 对同 cardId+position 的连续帧改 setCardDropTarget(prev=>同值?prev:新)，cardDropTarget 对象身份保持（memo 卡片在拖拽悬停同一半区时收到不变 prop），cardId 或 position 任一变化仍即时更新；fakeDragEvent 补 clientY 并以 rect 桩驱动上下半区判定。注：React 对同值 dispatch 仍可能渲染持有 hook 的组件一次，契约以对象身份为准而非渲染计数。全量 test:unit 225 文件 1734 测试 ✅，typecheck/10 项静态门禁 ✅（tokens 未涉改动，由 pre-commit 复跑） |
| 2026-09-18 | K1-03 视图状态按 viewId 持久化 + 死字段接线/删除 | 08c19fc2 | 新增 kanban-view-state.test.ts 4 例 + tests/kanban-view-live-fields.test.ts 1 例（先红后绿）：filters/sorts/searchQuery/cardSize 从组件内存 useState 改为读写 activeView（updateActiveView 经 commitData 落盘），切视图/关全屏/刷新不再丢配置，各视图互不串味；activeViewId 选择即写回 data.activeViewId；未配置时 filters/sorts 复用模块常量保持数组身份稳定。7 死字段处置：dateField/startField/endField/progressField 接线（timeline-helpers 收 TimelineDateFields 按配置字段取数、配置缺失回退 startDate/dueDate 旧链；calendar-helpers.getWeekEventSegments 收 dateField 且保留多日跨度语义；甘特进度读写走 progressField，日历日新建也写配置字段），coverField/chartMetric/hiddenProperties 无人读且无近期接线计划——按「不留死字段」从 KanbanView 删除（历史栅栏多余键 parse 本就忽略）。live-fields 扫描守卫：KanbanView 每个字段必须在 types.ts 之外有 `.field` 读取方，新配置旋钮不接线即被拒。全量 test:unit 227 文件 1744 测试 ✅，typecheck/11 项静态门禁 ✅ |
| 2026-09-18 | K1-04 新建分组/批量泛化到 groupBy | 09c04770 | kanban-add-operations.test.ts 新增 3 例 + 新建 kanban-batch-group.test.ts 5 例（先红后绿）：handleAddColumn 从写死 status 列改为按 `activeView.groupBy || 'status'` 选列（新选项 id 前缀随列、无 options 的 text/date 列不提交），「新增分组」终于跟着当前分组属性走；useKanbanSelection 收 groupColumn，handleBatchStatusChange 更名 handleBatchGroupChange 写 `properties[groupColumn.id]`（multi-select 列写 `[groupId]` 保数据形状，缺省仍走 status），useKanbanRootState 派生并暴露 groupColumn；KanbanBatchBar props 改 groupColumn/onBatchGroupChange，占位文案换新键 preview.kanban_batch_change_group（双语，旧 kanban_batch_change_status 删除）。全量 test:unit 228 文件 1752 测试 ✅，typecheck/11 项静态门禁 ✅ |
| 2026-09-18 | K1-05 图表 dataset memo + 主题跟随 + i18n + ARIA | afe0a9c6 | 新建 kanban-chart-view.test.ts 6 例 + chart-helpers.test.ts 补 2 例（先红后绿，chart.js/auto 以 vi.mock 捕获每次 Chart 构造的 config）：dataset 改 useMemo([items,groupBy,groupProp])——无关字段提交不再销毁重建 Chart.js 实例（items 变仍重建）；useThemeRevision 以 MutationObserver 监听 data-theme，切主题按新配色重绘（ADR-0002「读一次冻结」违例修复）；'All Tasks'/'No Value'/'Tasks' 三处硬编码英文进 locales（新 4 键双语）；canvas 补 role='img' + 「分组名+总数+逐项计数」可读摘要，groupBy select 补 aria-label；KanbanChartView 包 memo，root.tsx 的 onUpdateView 由内联箭头改接 filterSort.updateActiveView（身份稳定，memo 才生效），props 表新增 handleUpdateView。ChartToolbar 拆出 ChartGroupByControl、aria 摘要与完成数提为纯函数（size 50 行）。全量 test:unit 229 文件 1760 测试 ✅，typecheck/11 项静态门禁 ✅ |
| 2026-09-18 | K1-06 搜索 debounce + 表格全选一次 set | 556df1b5 | 新增 kanban-search-and-select.test.ts 7 例（先红后绿）：搜索框改本地草稿 + 200ms 静默期后一次 onSearchChange（连打只提交末次，外部提交的 query 仍同步进草稿，卸载清计时器），不再每击键 commitData 触发全量过滤并堆历史；表格全选从逐条 onToggleSelect（N 次 setState）改 KanbanTableView 新 prop onToggleAll 一次性交全部 id，useKanbanSelection 新增 handleToggleAll，走导出的纯函数 computeSelectionAfterToggleAll（全覆盖则删、否则并集，作用域外选中保留），root.tsx/KanbanMain 接线 handleToggleAll。KanbanSearchBox 连同 useDebouncedSearch 拆出 kanban-search-box.tsx（header 原 524 行且函数超 50 行，违 size 门禁）。review #16 的小写化索引缓存以 debounce 达成同一目标，未再引入索引层。全量 test:unit 230 文件 1767 测试 ✅，typecheck/11 项静态门禁 ✅ |
| 2026-09-18 | K1-07a 标签色板迁入令牌层 + AA 重标定 + 幽灵令牌门禁 | a72e55b3 | 新增 tests/kanban-tag-contrast.test.ts 3 例（先红后绿）：标签色板必须在 tokens.css 声明且 kanban.css 不含字面色、每色在两主题的每个实心表面上「色 vs 自己的 14% 底色」≥4.5:1、`--kanban-tag-*` 引用必须真实声明（模板占位符按 12 色展开后判定）。落地：24 个 `--kanban-tag-*` 从 kanban.css 迁到 tokens.css 两套主题块，改 oklch 表达并按 AA 反解亮度（浅色 L≈45–50、深色 L≈71–76，色相饱和度沿用原值），底色统一 `color-mix(in oklab, …-fg 14%, transparent)` 由前景派生（同 `--accent-soft` 配方，前景与底色不会再各走各的）；colors.ts 删掉从未声明的 `--kanban-tag-<name>-border` 引用（一直是 transparent 空转，标签本身无边框宽度，第三例门禁现成拦住同类幽灵令牌）。对比度实测：真 Chrome 解析 var+color-mix 后逐表面逐色量，浅色最差 4.61（slate on --bg-sunken）、深色最差 4.84（red/coral on --bg-overlay），12×2 全过 AA。`check-token-drift` 无受影响（88 个共享令牌不变，标签色板仍是 app 私有）。色值数学从 scripts/check-contrast.mjs 抽到 scripts/lib/contrast.mjs 供两处共用（浏览器门禁用它判合成，本门禁用它判声明）。review #28 的 chart 第二套调色板与 text-white×10 见 K1-07b/K1-07c；声称的 `!important×2` 在本模块 HEAD 与基线 53824e3c 均 0 命中，不复现。全量 test:unit 231 文件 1770 测试 ✅，typecheck/11 项静态门禁 ✅ |
| 2026-09-18 | K1-07b chart 调色板改读令牌，删第二套 hex | efcfd0b9 | 新建 chart-palette.ts（CHART_TOKENS 八角色→令牌名、KanbanChartPalette、buildKanbanChartPalette 纯映射、readKanbanChartPalette 用隐藏探针元素读 `getComputedStyle` 解析后的色值）+ chart-palette.test.ts 3 例、tests/kanban-chart-tokens.test.ts 2 例（先红后绿）：chart-helpers.ts 删掉 11 个 `COLOR_*` 与 8 色 `DEFAULT_PALETTE`/`resolveOptionColor` 十六进制表，改由 `resolveChartColorName` 只产出 12 个标签色名（KanbanChartDataset.colors 类型随之收窄到 KanbanColorName），`buildChartJsConfig(type, dataset, palette)` 不再收 isDark；折线填充/描边走 `--accent-soft`/`--accent`，坐标与 tooltip 走 text/border/overlay 令牌，图表与标签从此共用一份色板。视图侧 useThemeRevision 监听扩展到 data-accent、data-background（折线跟强调色、白底变体改 overlay），palette 按主题代号 memo，仅数据变化不重复解析（新增 4 例断言切片色来自所读调色板、切主题/切强调色各重读一次重绘、数据变化只读一次）。实测：真 Chrome 里三种探针（template/hidden span/离屏 span）解析两主题 + indigo 强调色结果一致且 body 无残留，canvas fillStyle 接收 oklch 与 color-mix 计算后的 `oklab(… / 0.14)` 序列化串并能画出像素；`--accent`/`--accent-soft` 位于 tokens.css 第二个 `:root` 块，暴露出 themeVars 只取首个同选择器块的缺陷，已改为按文档序合并所有同名块。enhance/chart.ts 的 `CHARTJS_TEXT_COLORS` 与 slides/ui/chart-block.tsx 自带 option 配色属其他模块，按「不夹带无关改动」另计后续。全量 test:unit 233 文件 1784 测试 ✅，typecheck/11 项静态门禁 ✅ |
| 2026-09-18 | K1-07c 板上文字改走填充对比令牌 + text-white 扫描门禁 | bb23512f | 新增 tests/kanban-contrast-tokens.test.ts 3 例（先红后绿，红时列出全部 9 处违规）：模块源码不得再出现 `text-white`/`text-black`/`text-[#hex]`；`--accent-contrast` 对 7 个强调色 × 两主题 ≥4.5:1（实测浅 6.56–9.70、深 7.22–10.42）；`--accent-contrast` 对 12 个标签前景色 ≥4.5:1（浅 6.21–6.77、深 7.86–8.13，同一处白色在暗色标签上实测仅 2.39–2.47）。落地：日历/时间轴/甘特「今天」徽标、日期选择器选中日、表格头像、列表完成勾选、子任务勾选、附件预览按钮、配色选择器勾号共 9 处 `text-white` → `text-[var(--accent-contrast)]`（app 既有「填充上文字」约定，kanban-header/date-range-popover 等已在用）；时间轴与甘特的「今天」由 `bg-[var(--danger)]`（白字实测浅 3.69、深 3.14，两主题都不达标且该目录再无其他 danger 用法）改为与日历一致的强调填充；配色选择器勾号顺带删掉为救可读性加的 drop-shadow。theme-tokens.mjs 增 `accentNames`/`accentVars`，按两属性选择器压过其后 `:root` 默认值的级联取该强调色。全量 test:unit 234 文件 1787 测试 ✅，typecheck/11 项静态门禁 ✅ |
| 2026-09-19 | K1-08 死代码清理（挂载契约 / context menu memo / api 命名空间） | d3225530 | 按 review #27 删三处。① `KanbanMountOptions`/`KanbanBlockEntry` 去 `dark`/`locale`，registry 两处赋值与 `AppLocale` import 随之删，`use-kanban-blocks.ts` 不再传也不再 `useLocale()`，`use-preview.ts` 只去掉 kanban 的 `dark`（mindmap/excalidraw/slides 三处仍读，未动）。关键实测：`locale` 字段虽无人读，但它是宿主重挂的唯一语言通道——临时探针用例证明宿主再调一次 `mountKanbans` 确实把 "New item" 刷成中文，故不能只删字段：改由 `KanbanRoot` 自己 `useLocale()` 订阅（新增 registry-locale.test.ts 2 例，删掉订阅第 1 例即红、恢复即绿），任何宿主（含全屏叠加层）切语言都自刷新。`dark` 确证无人读（配色走 CSS 令牌，chart 已有 data-theme/data-accent/data-background 观察器），删除不改行为。② `kanban-context-menu.tsx` 的 `useMemo(…, [props])` 依赖每次渲染都是新对象、永不命中，去 memo 与 `useMemo` import（items 本就每渲染重建）。③ `api.kanban` 的 `upload`/`remove` 两个别名全仓零调用（调用方按名字 import 端点函数），删命名空间并把 import+export 合成一条直接 re-export；review 文中「files.remove」即指此处——`api.files.remove`（files.ts:82）在 attachment-drive-modal/hooks.ts:83 有真实调用，保留未动。新增 tests/kanban-dead-code.test.ts 3 例源码扫描（挂载契约不得再出现 dark/locale/AppLocale、context menu 不得以 [props] 为依赖、api 不带回 kanban 命名空间且具名导出仍在、src 内无 `api.kanban.` 引用），先红 3/3 后绿；i18n 门禁禁 src 内中文字面量，断言改按当前 locale 取 `t('preview.kanban_new_item')` 为期望串。5 处测试挂载调用同步去 dark/locale。kanban+preview+tests/kanban 50 文件 269 测试 ✅（act 警告已消），全量 test:unit 236 文件 1792 测试 ✅，typecheck/11 项静态门禁 ✅ |
| 2026-09-19 | K1-09 R2 存量孤儿回收（Cron `kanban/` 前缀） | ab6e345a | 新增 `src/worker/attachments/kanban-reclaim.ts`：`reclaimOrphanKanbanFiles(env, {now, limit})` 以 **R2 前缀列举为事实来源**（不给看板对象造 D1 行），按 `customMetadata.userId` 归组；候选须同时满足 kind=`kanban-attachment`、有 userId、`now - object.uploaded ≥ 24h`（新上传可能还停在「响应已返回、笔记尚未保存」之间），归因不到的对象一律保留（猜属主即数据丢失事故）。引用集从该账号 `notes`/`blog_posts`/`community_templates` 三张正文表分页 `content LIKE '%kanban%'` 取回，用一条 `kanban/(?:file/)?<name>/<segment>` 正则同时收「末段全名」与「id 前缀」两类 token（URL 与 r2Key 两种存储形式、cover 与 files 两种落点共用一把尺；回收站笔记因不带过滤条件仍算引用）。删除复用 `deleteAttachmentObjects(env,'r2',keys)`，失败不入清理队列——候选每轮从 R2 重新派生，天然幂等重试；单次上限 200、列举上限 5000，余量记 `deferred` 留给下一轮。`src/worker/index.ts` scheduled 与 `runAttachmentCleanup` 并列接 `runKanbanFileReclaim`。新增 tests/kanban-orphan-reclaim.test.ts 12 例（node 工程，真 TABLE/INDEX schema + R2 list/delete 桩，先红后绿；含「越界不碰 images/ 命名空间」「分页遍历不止第一页」「他人笔记提及不阻止回收」「模板/博文仍嵌引用则保留」「无 FILES 绑定返回全零」）。变异自检：把两个额外引用源删掉或把宽限期判定短路，对应 2 例转红。**踩坑记录**：R2 对象时间戳是 `uploaded: Date`，不是列举里常见的 `uploadDate`；初版实现配初桩都写 `uploadDate` 时测试全绿而真 R2 上恒 undefined（回收器会永久「一个都不删」），直到把 `as ListedObject` 交叉类型换成绑定 `R2Object` 才由 tsc TS2551 暴露——桩必须按绑定类型写，不要用宽松 cast 绕过。选型（review #10「先决定通道」）：**不并轨 attachments**——看板对象无库/标签/星标语义，并轨要补 attachments 行且历史笔记正文已写死 `/api/kanban/file/...`（公共契约，改动要 expand-contract 双读双写），而本项只要「存量回收」，读 R2 自身列举即零迁移零新表；配额与鉴权已由 K-16/K-19 对齐。「删笔记联动」由本回收器承担（引用集每次从现存笔记派生，purge 后下一轮即回收），不在笔记删除事务里同步删 R2：那会把外部存储往返与失败语义绑进笔记删除，且失败无重试。稳态成本=前缀分页列举（15 分钟一次）+ 仅在确有超期候选时按账号跑三条 LIKE 扫描。全量 test:unit 237 文件 1804 测试 ✅，typecheck ✅，11 项静态门禁 ✅，`npm run build` ✅ |
| 2026-09-19 | review #20a 表格「新增分组」误加卡片（独立小提交，先于 K2-01） | a6fdda84 | 表格底部按钮原本 `onClick={onAddGroup ?? onAddItem}`，而根上从未传过 `onAddGroup`，于是点「新增分组」实际新建了一张卡片。改为直接调 `onAddColumn`（`handleAddColumn` 给当前 `groupBy` 列追加选项，K1-04 已泛化），`onAddColumn` 转必填、`onAddGroup` 死 prop 删除。新增 kanban-table-view.test.ts 1 例（先红后绿：断言 `onAddColumn` 被调且 `onAddItem` 未调），kanban-search-and-select.test.ts 补必填桩。全量 test:unit 237 文件 1805 测试 ✅，typecheck ✅ |
| 2026-09-19 | K2-01a 表格列改 columns schema 驱动 | b009c138 | 表头与表体原先各写死 6 个单元格（Task name/Owner/Status/Due date/Timeline/Files），与文档 `columns` 无任何关系：默认 schema 的 priority/startDate/endDate/progress 与任意自定义列一律不显示，而「Due date」「Timeline」读的 `dueDate`/`date`/`timeline` 字段在本模块任何生成路径里都不写入，恒为 `-`（review #20c）。新建 `ui/kanban-property-cell.tsx` 作表头/表体共用的单一来源：`kanbanTitleColumn()`/`kanbanPropertyColumns()` 按 schema 排序并补上附件列（附件挂在 item 上而非 `properties`，故 schema 未声明也保留该列，已注释并登记白名单），`kanbanColumnWidth()` 按 `KanbanPropertyType` 给宽；`CellContent` 按类型分派——select 保留可编辑并写回选项 id（id/label 双匹配，K-08 口径）、multi-select 画标签 chip、checkbox 用原生 input 带 `aria-label`、files 走 `KanbanFilesCell`、其余（text/number/date/person）先按纯文本渲染，行内编辑留 K2-01b。`kanban-table-row.tsx` 删掉 `StatusCell`/`OwnerAvatar`/`ItemMetaCells` 与标题单元格里的标签 chip（`assignee\|\|owner`、`dueDate\|\|date`、`timeline` 三处兜底读一并删——默认 schema 的 assignee 是 text 列，头像分支不可达）；`kanban-table-view.tsx` 的 `TableHeaderRow` 改收 `columns` 并与表体共用同一列序与宽度，表头/表体不可能再漂移。附件写入改走新的 item 级 `handleUpdateFiles`（`{...item, files}`），不再经 `onUpdateProperty(id,'files')` 写进 `properties.files`（详情/画廊/列表都按 item 级读附件）。死掉的三个表头文案键 `preview.kanban_owner`/`kanban_due_date`/`kanban_timeline` 双语删除（`kanban_task_name` 仍被时间轴视图读，保留）。新增 kanban-table-view.test.ts 8 例（先红后绿，schema  fixture 覆盖 9 种类型 + 附件列 + 空值 `-` + label 存值本地化 + 标签移出标题格 + select 写回 id + 附件走 `onUpdateFiles`）。kanban-table-view/row 的 `onUpdateProperty` 转必填。表头点击排序与列显隐（K2-01c）、`table/row/cell` 语义（K2-03）不在本项。全量 test:unit 238 文件 1812 测试 ✅，typecheck ✅，13 项静态门禁 ✅ |
| 2026-09-19 | K2-01b 表格各类型单元格行内编辑 | 61390f1e | 单元格从只读改为可编辑（review #20c 剩余部分）。`ui/kanban-property-cell.tsx`：删 `PlainValue`/`MultiSelectValue` 与空值「-」占位（`EMPTY_VALUE_MARK` 一并删），text/number 走新的 `EditableValue`——本地草稿 + Enter/blur 单次提交 + Escape 还原（沿用 K-22 详情标题口径），`aria-label` 取本列名，带 `data-owns-escape='true'` 使全屏 ESC 不与输入框争抢（同 card/detail/header 既有约定），number 空串写 `''` 而非 `NaN`；date 接 `KanbanDatePicker`（点日历即写回、带清除钮）；multi-select 接 `KanbanTagPicker`，按 `column.id` 写回而非只认 `tags`。`ui/kanban-root-hooks.ts`：抽出 `useKanbanValueWrites`（50 行上限），`handleUpdateTags` 泛化为 `handleUpdateMultiSelect(id, columnId, values, newOption?)`（新选项仍经 `appendOptionToColumn` 一次提交），`handleUpdateTags` 保留为 `('tags', …)` 薄封装——卡片头/详情仍在用它，不是兼容层。`ui/kanban-tag-picker.tsx`：`onChangeTags` 一次回带 `newOption`、删 `onAddOption`，使「加一个不存在的标签」= 一条历史（原先两次 commit）；`computeTagAddition` 从 `kanban-card-header.tsx` 迁入并导出，卡片头改 import（同一段逻辑第二次出现，按 DRY 提前抽取）。`ui/kanban-item-detail.tsx`/`kanban-card-header.tsx` 走新契约。view→group→row→cell 透传 `onUpdateMultiSelect`。新增 kanban-table-view.test.ts 9 例（文本单次提交/Enter 提交/Escape 丢弃、列名 aria-label、数字与清空、日历写回与清除、标签增删、从格里新建选项）+ kanban-item-mutations.test.ts 4 例（非 tags 列写 `properties.labels`、无新选项时 `columns` 引用不变、`handleUpdateTags` 仍写 tags、`files` 存于条目而非 `properties`）。刻意推迟：日期触发器的逐列可访问名称（加 `aria-label` 会把可见日期值从读屏里盖掉）与 `table/row/cell` 语义归 K2-03。全量 test:unit 239 文件 1823 测试 ✅，typecheck ✅，13 项静态门禁 ✅ |
| 2026-09-19 | K2-01c1 表头点击按列排序 | 998ddba5 | 表格视图的列从只读表头变为可点排序，结论写回已持久化的 `activeView.sorts`，与排序弹层共用同一份视图状态（review #20c 剩余部分之排序）。按「不新增文件」方案落地：`filter-sort.ts` 新增纯函数 `toggleKanbanColumnSort(sorts, propertyId)`——点表头是「按这一列排」，故整条替换弹层写入的多列规则（asc→desc→清除），回到多列仍走弹层与撤销历史；不从 `kanban/index.ts` 导出（只被本模块 UI 读）。`ui/kanban-property-cell.tsx` 的 `KanbanTableHeaderCell` 接 `sort`/`onSort`，用原生 `button`（`text-left` + `focus-visible:ring`，`text-inherit` 继承 `<th>` 字重而非写死）带方向图标 `ArrowUp`/`ArrowDown`，`aria-label` 走三条整句 i18n 键（`kanban_sort_by_column`/`kanban_sorted_ascending`/`kanban_sorted_descending`，双语）；附件列不参与——排序读 `properties[columnId]` 而附件存在 item 上，故保持纯文本标签（已注释）。`ui/kanban-table-view.tsx` 由 `view?.sorts` 解析本列排序态并下发 `onSortColumn`，同时把分组列表连同「新增分组」按钮拆为同文件内 `TableGroupList`，以守 50 行函数上限（`size:check` 量的是函数体跨度，不是文件行数）。`ui/kanban-view-state.ts` 新增稳定 `toggleSortColumn`，经函数式 `commitData` 读已提交视图而非渲染时快照，使同批次内两次点击仍按 asc→desc 递进。`ui/kanban-root.tsx` 补 `handleToggleSortColumn` 接线，并把 Board/List 两处重复的条目写入闭包收敛为同文件 `kanbanItemWriter`（写入函数 identity 保持不稳定，见下）。新增 filter-sort.test.ts 4 例（含 asc/desc/清除往返）+ kanban-table-view.test.ts 3 例（先红：点击回报列 id、`aria-label` 随存储排序、desc 与附件列无按钮）+ kanban-view-state.test.ts 1 例（三次点击写回 `views[0].sorts` 且 setter 引用不变），并对 `toggleKanbanColumnSort` 做变异自检（改坏后 2 个具名测试转红）。**过程回归（已修）**：曾把条目写入收敛进 `useKanbanValueWrites` 的稳定回调，`registry-locale.test.ts` 立刻红——看板 18 个 `memo` 组件的 props 全变稳定后直接 bail，语言切换时标签不再重绘；改回同文件工厂保持 identity 不稳定后恢复绿。根因（memo 边界吞掉 locale 重渲染）不在本项，登记进 K2-04。全量 test:unit 239 文件 1831 测试 ✅，typecheck ✅，13 项静态门禁 ✅ |
| 2026-09-19 | K2-01c2 表格列显隐写回视图 | 8598e73b | 表格的列从此可藏可还，结论与排序同属视图状态、写入已持久化的 `activeView.hiddenColumns`（review #20c 剩余部分之列显隐；K2-01 收尾）。继续走「不新增文件」：`filter-sort.ts` 新增纯函数 `toggleKanbanHiddenColumn(hidden, propertyId)`；显隐 UI 是 `kanban-view-options.tsx` 里新加的 `ColumnsSection`（`fieldset`/`legend` 包原生 `input[type=checkbox]`，勾选态即「可见」），入口沿用 header 已有的那个选项按钮——表格拿到 `Columns3` 图标与列面板，看板拿到原有分组/卡片尺寸一节，两边都看不到对方的控件（面板各节按调用方是否接线该节 writer 决定渲染，故看板路径字节不变）。`ui/kanban-property-cell.tsx` 的 `kanbanPropertyColumns(columns, hidden?)` 承担过滤：隐藏名单只作用于属性列，标题列根本不在此列表口径内，任何文档都藏不掉它（已注释）。view→group→row 三层透传 `hiddenColumns`；刻意不预先过滤 `columns` 再下发，因为 `GroupFooter` 的进度条要按 status 列取数——那是数据读取而非显示列，收全量列才不会因藏一列把分组进度一起清空。`ui/kanban-view-state.ts` 把两个 toggle 收敛为同文件 `useKanbanViewToggles`（`useKanbanViewState` 函数体加完后 54 行，违 50 行上限），沿用读已提交视图的函数式 `commitData`，同批次两次点击仍按加→减递进。序列化零改动：`normalizeKanbanData` 原样透传 `raw.views`，新字段随栅栏自然往返，并由 body.test.ts 往返例守住（`tests/kanban-view-live-fields.test.ts` 的 live-fields 扫描同时要求该字段有真实读取方）。新增 filter-sort.test.ts 2 例 + kanban-table-view.test.ts 3 例（隐藏列从表头与每行消失、附件列可藏、标题列在任何隐藏名单下仍在）+ kanban-view-state.test.ts 2 例（只作用当前视图、未设列表引用稳定）+ kanban-header.test.ts 2 例（先红：表格按钮 labelled `preview.kanban_columns` 开合面板、勾选取当前 hiddenColumns 且回调带列 id）+ body.test.ts 1 例，共 +10。变异自检：改坏 `toggleKanbanHiddenColumn` 的还原分支与 `kanbanPropertyColumns` 的过滤，4 个具名测试转红。过程踩坑：`formatKanbanPropertyName()` 收字符串时会原样返回（除非该 id 在 `PROPERTY_NAME_MAP` 里）， fixture 里传裸 id 断言列名会得到恒真结论，已改按 fixture 的 `KanbanProperty` 对象解析显示名（未知 id 直接抛）。全量 test:unit 239 文件 1841 测试 ✅，typecheck ✅，13 项静态门禁 exit=0 ✅ |
| 2026-09-19 | K2-02 分享/导出/放映 kanban 快照通道 | 6db68b36 | 看板块在增强器什么都不做的表面上从此有三态可走，不再永挂「正在加载看板…」（review #21）。选型：沿用 mindmap/excalidraw 已成型的三答契约（`live` 由调用方挂载 / `snapshot` 就地画静态 / 未声明露栅栏），而不是造一个「只读实时挂载」——实测 `entry.editable` 只闸门写回（`write.ts:8`），`createKanbanCanvas` 的 `is-readonly` 类无任何 CSS，UI 控件恒可点，只读挂载会让读者做出悄悄丢失的编辑。新增 `lib/markdown/kanban/static.ts`（纯 DOM、同步、无 React 根、不加载厂商代码，按 `activeView.groupBy` 分组、组名走 `formatKanbanGroupLabel`、含标题、空板与坏体各一态；刻意不套视图筛选——静态快照没有能告诉读者「这是子集」的控件，已注释登记白名单）；`view.ts` 新增 `removeKanbanLiveControls`（摘 `[data-kanban-fullscreen]` 与 `[data-kanban-retry]`——两者只由 `preview-interactions.ts` 接线应答，导出物上的重试按钮是「看着能按、按了没反应」的死控件）与模块内私有的 `showKanbanSource`/`showKanbanSourceAll`；`enhancePreview` 收 `kanban?: 'live' \| 'snapshot'`，`use-preview.ts` 声明 `live`，编辑器 live preview、放映 `deck-print`、`use-slide-html`、悬停卡 `card-content`、分享页、`export-note` 六处声明 `snapshot`。顺带修 `card-content` 的增强触发条件漏 `[data-kanban]`（只含看板的笔记整块跳过增强，卡里必留加载占位）。样式：`kanban.css` 新增 `.kanban-source`/`.kanban-snapshot*` 令牌规则，`export-note` 打印 CSS 隐藏块头控制条。文案新增 `preview.kanban_snapshot_empty` 双语。测试 +13：static.test.ts 8 例（分组与本地化组名、收尾态与死控件、标题、outline 体同通道、空板、坏体、栅栏兜底、重复增强幂等）、enhance.test.ts 3 例守三答（未声明出源码、`live` 不碰块、`snapshot` 出卡片列表）、`tests/kanban-render-channel.test.ts` 2 例源码扫描钉住「每个 `enhancePreview` 调用点必须写明看板通道」并锁死期望映射（沿用 `fullscreen-policy.test.ts` 的「扫不到东西也算失败」写法）。变异自检：把 snapshot 分支与未声明分支各改坏一次，对应具名测试各转红后还原。刻意登记为后续（不在本项）：悬停卡仍漏 `[data-excalidraw]`、`is-readonly` 死路径、`--code-font-size`/`--code-line-height` 为幽灵令牌（本次新样式改用 `--text-12`，未新增引用）。全量 test:unit 241 文件 1854 测试 ✅，typecheck ✅，13 项静态门禁 exit=0 ✅ |
| 2026-09-19 | K2-03a 浮层 ESC/点击外部/z-index 收敛 | 96ca97dc + 6b30445b | 看板浮层从「只有指针能关」变为键盘等价（review #26/#29 第一批）。实测：看板全树 `useEscape` 0 处、自绘 window-mousedown 关闭逻辑 5 份（`usePopoverDismiss`×2/`useMenuClickOutside`/`useDropdownDismiss`/icon-picker 同名副本），报告记「1 份重复」偏低。9 个面板（列菜单/排序/筛选/视图选项/图标选择/子任务菜单/日历/标签/详情状态下拉）统一走 `components/overlay` 的 `useEscape` + `useClickOutside`，本地副本与 `useDropdownDismiss` 全删（唯一消费方已迁走，按「不留死代码」直接删而非留壳）；日历与标签面板只在打开时挂载，故传 `active=true`（已注释），其余按 `open` 传；详情状态下拉靠 overlay 的 ESC 栈 LIFO 实现「只收起下拉、详情弹窗不动」。新增 `ui/kanban-popover-dismiss.test.ts` 18 例：先红 9 例全为 ESC（9 例点击外部本就是绿的，此处作为回归护栏），面板名单按 `describe.each` 登记，新面板漏接即被点名；测试夹具把 `unmount()` 交给 harness 的 `mounted` 注册表——用例中途断言失败会跳过自己的 unmount，遗留在 `document.body` 的面板会被下一个用例的 `querySelector` 先命中（表现为 8 个假红，已按 `-t` 单跑定位）。第二提交把 5 处 `z-50` 原子字面量换成 `z-[var(--z-popover)]`（令牌值即 50，绘制顺序字节不变，与同目录已用 `--z-menu`/`--z-float` 的浮层同源）。刻意登记为后续：焦点归还触发器（`useDialogFocus` 是模态陷阱，非模态浮层按仓库约定不带）随 b 的 `aria-expanded`/`aria-controls` 一并处理。变异自检两次各杀一个具名测试：删日历 `useEscape` → 「puts the calendar away with Escape」红；删详情下拉 `useClickOutside` → 「keeps closing on a click outside」红，均由 /tmp 备份还原。全量 test:unit 242 文件 1872 测试 ✅，typecheck ✅，13 项静态门禁 exit=0 ✅ |
| 2026-09-19 | K2-03b 触发器/面板 ARIA 关系与可访问名称 | b8b9e063 | 看板的 11 组浮层从「读屏里只是一个按钮」变为带展开态与指向关系的控件（review #26/#29 第二批）。实测基线：看板全树 `aria-haspopup`/`aria-expanded`/`aria-controls` 各 0 处、面板根 0 个 `id`。写法沿用 `components/overlay/submenu.tsx`：`aria-controls` 仅打开时挂，面板根 `id`；关系由**必填** `panelId: string` 承担（拥有 `open` 的一方 `useId()` 后下发），故新增挂载点漏接会被 TS 拒、也会被测试夹具点名，而可选 prop 会静默漏接。可访问名称两处补齐：子任务「更多操作」触发按钮与菜单根（复用 `common.more_actions`，全站同一菜单名，不新增键）、详情状态下拉根（取所改属性名）。K2-01b 推迟的日期触发器逐列名称落为 `KanbanDatePicker` 必填 `propertyName` + 触发器内 `span.sr-only`——可见值与属性名都读得到、画面不重复，四处调用点（表格日期格、详情属性格、起止日期两格）全传值故不留可选分支。a 登记待办的焦点归还由新测试的「ESC 后焦点仍在触发按钮」一例守住：实测仅标签面板因面板内 autoFocus 输入真正丢焦，`TagCreatePopover.dismiss` 改为按「焦点是否仍在面板内」条件把焦点交回触发按钮（点击外部时指针已带走焦点，不抢回指针落点），选项点击与 Enter 提交两条自关闭路径同走 `dismiss`。`size:check` 拦到 `DetailStatusDropdown` 函数体 54 行，把 listbox 标记抽为 `kanban-item-detail-fields.tsx` 的 `StatusOptionList`（与它的行组件 `StatusOptionItem` 同模块，非新增文件亦非为拆分而拆分）。新增 `ui/kanban-popover-aria.test.ts` 25 例（先红 25/25：11 组 × 关系与焦点 + 日期格列名 3 例），dismiss/subtask-menu 两处测试夹具随必填 `panelId` 补入参。变异自检三次各杀具名测试：删日期触发器 `aria-expanded` → 该组两例红；把 `dismiss` 的焦点归还改成 `void hadFocus` → 标签面板两例红；删表格日期格的 `propertyName` → 列名两例红，均由 /tmp 备份还原。全量 test:unit 243 文件 1897 测试 ✅，typecheck ✅，13 项静态门禁 exit=0 ✅ |
| 2026-09-19 | K2-03c 表格补 table/row/cell 语义 | d4d4a9fa | 表格视图从「一串无归属的 div」变为读屏可按行列导航的表（review #29 第三批）。实测基线：表格全树 0 处 `table/row/cell` 语义，只有 7 个视图共用的 `role='region'`。按用户选定的「div 上补 ARIA role（不改视觉）」路线落地，不用原生 `<table>`（会毁掉每组一张圆角卡）：`kanban-property-cell.tsx` 新增 `kanbanTableColumnCount()`（选择列 + 标题列画在 `kanbanPropertyColumns` 之外故 +2）并给单元/表头根补 `cell`/`columnheader`；`kanban-table-view.tsx` 外框 `table`、表头行 `row` + 全选格 `columnheader`、分组列表 `presentation`、「新建分组」包进跨全列的一行；`kanban-table-group.tsx` 组卡 `rowgroup`、组标题与组脚改 `row` > 跨全列 `cell`（flex 类上移到格、边框与内边距留在行上）；`kanban-table-row.tsx` 条目外层 `presentation`、行 `row`、选择格 `cell`、标题格 `rowheader`、子任务面板改跨全列的一行。新增 `kanban-table-semantics.test.ts` 9 例（先红 8/9）：表在 region 内、表头 5 格名称与顺序、条目行格序 `cell/rowheader/cell…`、每行归 `table|rowgroup` 且每格父级是 `row`、两组不混、跨列格在隐藏列后重算、新建分组与子任务面板均为跨全列的一行。`size:check` 拦到 `table structure` describe 体 56 行 → 行/格两例分出 `rows and cells` describe（套件边界，不新增文件）。变异自检：计数少算选择列 → 3 例红；组内计数漏传 `hiddenColumns` → 「recounts the span」红（此变异初次存活，据此把跨度断言扩到整棵视图并补新建分组格）；去掉列表 `presentation` → 3 例红；均由 /tmp 备份还原。视觉等价按盒模型逐项核对（边框/内边距留原元素、新增包裹层为无样式 block），jsdom 不布局故未实测，已连同 axe 表格规则登记为 K2-05 待办；子任务展开 chevron 无可访问名称登记给 e。typecheck ✅，13 项静态门禁 exit=0 ✅，全量 test:unit 244 文件 1906 测试 ✅（其间一次运行中 `tests/starter-deck-render.test.ts` 因并发负载 5s 超时，单跑 2.0s 绿、复跑全量绿，与本批无因果，按「不夹带无关修复」仅登记）|
| 2026-09-19 | K2-03d view-tabs 方向键与面板配对 | 0a595f83 | 视图切换从「只能逐个 Tab 过去、方向键无反应」变为一个 roving 落点加四向键导航，并与视图区建立 tab/tabpanel 双向关系（review #29 第四批）。实测基线：`role='tablist'`、`role='tab'`、`aria-selected` 三样本就有，缺 `aria-controls`（全树 0 处）、缺 `tabIndex` 轮换、缺 ArrowLeft/Right/Home/End 处理，且整个看板没有任何 `role='tabpanel'`。`kanban-view-tabs.tsx` 导出 `kanbanViewTabId(panelId, viewId)` 作 tab 自身 id 的单一来源（面板侧只拿得到 panelId，反推不出 viewId，两处各拼一次必然漂移），`rovingTabIndex` 纯函数给目标下标并两端环绕，`handleKeyDown` 据此同时移动焦点与选中项（APG 自动激活），选中项 `tabIndex=0`、其余 `-1`。`kanban-root.tsx` 用 `useId()` 生成 `viewPanelId`，同时下发头部与视图两条分支；面板角色挂在原本那个 `<main>` 元素上（改 `div role='tabpanel'` + `aria-labelledby` 指回选中 tab，类名与盒模型一字不动），不新增包裹 div，顺带消掉在 app shell `<main>`（app-shell.tsx:153）之内嵌套的第二个 main 地标——嵌套 main 正是 axe `landmark-main-is-top-level` 判的那类问题。`KanbanHeader` 改收**必填** `viewPanelId`（沿用 b 的口径：可选 prop 会被静默漏接，必填才让新挂载点被 TS 与夹具点名），三处头部测试夹具随之补参。`kanban-view-tabs.test.ts` 新增 8 例（先红 8/9，图标那例本就是护栏）：roving tabindex、四向键各自移动焦点与选中项、两端环绕、无关键不动、每个 tab 的 `aria-controls` 指向面板、整棵看板里选中 tab 与 `role='tabpanel'` 双向对上、方向键换视图后面板名跟随。刻意登记：面板不加 `tabIndex={0}` 的理由（8 个视图文件均渲染至少一个原生按钮，实测最少 1 个）与「tabpanel 内含视图自带 region、名称近似重复」一并交 e 判定。视觉与真实可访问性树未实测（jsdom 不布局、`e2e-visual.mjs` 无看板场景），随 K2-05 补。变异自检五次各杀具名用例：删 `tabIndex` → 1 例红；删 `onKeyDown` → 4 例红；删 tab 的 `aria-controls` → 3 例红；删面板 `aria-labelledby` → 2 例红；ArrowRight 改不环绕 → 环绕那例红，均由 /tmp 备份还原。typecheck ✅，13 项静态门禁 exit=0 ✅，全量 test:unit 244 文件 1914 测试 ✅ |
| 2026-09-19 | K2-03e1 详情弹窗控件补名 + 名称清点探针 | 5e4160ce | 探针挂在真实看板上量「有没有控件读屏叫不出名字」，详情裸 label 改外层 label 包裹、隐藏 file 输入与子任务标题输入补名，新增 kanban_subtask_title 双语键；typecheck ✅，13 项静态门禁 exit=0 ✅，全量 test:unit 245 文件 1923 测试 ✅，变异自检 3 次各杀具名例 |
| 2026-09-19 | K2-03e2 补回页签点击切换并命名其暴露的控件 | ea3459f4 | 修 0a595f83 丢掉的 tab onClick（鼠标此前完全无法切视图，且探针不校验切换所以全绿）；探针 selectView 增「aria-selected 真的翻过来」断言，随即量出 table 7 个、list 1 个无名控件并逐一补 aria-label/aria-expanded，新增 collapse/expand_group 两键；typecheck ✅，13 项门禁 exit=0 ✅，全量 test:unit 245 文件 1925 测试 ✅，变异删 onClick → 9 例红、四处名称各删 → 1 具名例红 |
| 2026-09-19 | K2-03e3 名称清点扩到子任务展开面板 | 7b5f7aa5 | 纯测试加固：expandSubtasks() 按展开器名称点开每行子任务（先断言有可展开者，防空跑），table/list 各一例覆盖展开态控件，无名描述符带图标名便于定位；typecheck ✅，13 项门禁 exit=0 ✅，全量 test:unit 245 文件 1927 测试 ✅，变异删三处名称 → 1/1/2 例红 |
| 2026-09-19 | K2-03e4 看板不再用 div 假装按钮 | a07d6e0c | 5 处 `div role='button'` 清零：折叠列与卡片子任务汇总条改原生 `<button>`（内部 div→span 保版式，汇总条动作名走 `span.sr-only` 保留可见「1/2」，并补 `aria-expanded`；折叠列此前压根没有键盘路径），看板/列表/画廊三处卡片容器去 `role`/`tabIndex`/Enter 只留 `onClick` 作指针热区，列表与画廊的标题从 `<span>` 升级为真实「打开详情」按钮（看板卡沿用 CardHeader 那个）。`handleCardKeyDown` 收窄为 Alt+←/→，其「从卡内控件冒泡到容器」此前 0 覆盖，本轮由新探针补上。新增 `kanban-semantic-controls.test.ts` 13 例（首版 9 例 7 红）：四视图零 `role=button`、三种卡片视图各有真实打开控件、卡体热区仍可点、两处原生 button 语义与 `aria-expanded` 翻转、键盘跨列移动确实写回。名称清点的展开器选择器改按可访问名称匹配并断言「至少有一个可展开控件」。9 次变异各杀具名用例，均由 /tmp 备份还原。typecheck ✅，13 项静态门禁 exit=0 ✅，全量 test:unit 246 文件 1940 测试 ✅。未实测：真实浏览器焦点顺序与热区点击，归 K2-05 |
| 2026-09-19 | K2-03e5 卡片跨列移动补 aria-live 播报 | cedbfee1 | 两条移动路径（卡片 Alt+←/→ 与指针拖放）此前各自直达 `props.onMoveItem`，读屏用户移完不知道卡片落在哪一列。`useKanbanGroups` 改 `useKanbanBoardMoves`，把分组、播报文案 state 与 `handleMoveItem` 收在一处，dnd 与键盘分支都改收这个包装；播报文案由纯函数 `kanbanMoveAnnouncement` 生成，同组重排与查不到来源组返回 null。`[data-kanban-board]` 末尾常驻 `span role='status' aria-live='polite' sr-only`。消息走新参数化双语键 `preview.kanban_moved_to_group`（{title}/{group}，列名经 `formatKanbanGroupLabel` 本地化），不再新增 `${a}: ${b}` 拼接。新增 `kanban-move-announcement.test.ts` 5 例（先红 5/5，选择器限定在 board region 内以免被头部的写入状态顶替），拖放用例派发真实 `drop` 事件量整棵视图。登记待办：整列重排（`onReorderColumns`）仍无播报、同一句连续重复可能被读屏跳过、`kanban_untitled` 兜底分支无覆盖、真实读屏行为归 K2-05。size:check 先拦 `KanbanBoardView` 52 行，hook 调用回单行后 49 行通过（未拆组件）。6 次变异各杀具名用例，均由 /tmp 备份还原。typecheck ✅，13 项静态门禁 exit=0 ✅，全量 test:unit（串行）247 文件 1945 测试 ✅ |
| 2026-09-19 | K2-03e6 子任务菜单迁移到共享 Menu | 08506e8e | `KanbanSubtaskMenu` 此前自建 role='menu' 只挂 useEscape/useClickOutside，游标、焦点移入、关闭归还焦点三者全缺。前置给 `components/overlay` `Menu` 加可选 `panelId`（写到 portal 根节点），b8b9e063 的 aria-controls 契约不因迁移而破；kanban 侧保持同名同 props，内部改渲染 `Menu`（align=end、width=176 即旧 w-44、面板名沿用 common.more_actions），四行改 MenuItem 数据、删除行 tone=danger + separatorBefore、复制名称抽成 copySubtaskName、关闭交给 Menu.handleClick。`kanban-subtask-menu.test.ts` 重写为 10 例并挂真实调用点 KanbanSubtaskList（先红 5/5：焦点进首行、方向键游标、Enter 执行并关闭、role=separator、convert 透传子任务），另 5 例（id/名字/三条 clipboard）迁移前后皆绿。登记影响面高估：dismiss 与 aria 两处测试一行未改即通过（后者用 getElementById 找面板，天然穿 portal）；版式行高/圆角/间距随共享组件改变，浏览器几何与 axe 归 K2-05。typecheck ✅，13 项静态门禁 exit=0 ✅（comments:check 先拦 5 条新注释，重算白名单后通过），7 次变异各杀具名用例（含把共享 Menu 的 useCursorFocus 关掉 → 焦点与方向键两例红，证明确实继承共享键盘路径），均由 /tmp 备份还原；全量 test:unit（串行，最终树）247 文件 1952 测试 ✅ |
| 2026-09-19 | K2-03e7 视图不再在 tabpanel 内重复命名 | 8610dea1 | 判定收掉 7 个视图自带的 `role='region'`：两处名字同源于 `formatKanbanViewName` 的映射（同一句念两遍），`role='region'` 在 `src/client` 全树只此 7 处而仓库其余多视图表面（markdown 页签块）命名到面板为止，视图又只在 `KanbanRoot` 的 tabpanel 下渲染。表格视图的名字下移到 `role='table'` 网格本身，K2-03c 的命名网格不回退。`kanban-view-tabs.test.ts` 新增 `it.each` 7 视图的命名块（含 `panel.getAttribute('role')`，因 `querySelectorAll` 看不见 panel 自身）+ `kanban-table-semantics.test.ts` 首例改写，先红 8/8。真实读屏播报序列与 landmark 取舍归 K2-05。typecheck ✅，13 项静态门禁 exit=0 ✅（comments:check 先拦 4 条新注释，重算后通过），6 次变异各杀具名用例（board/gantt 各 1、网格丢名 1、表格外套 region 2、面板改 role 8、面板指错页签 10），均由 /tmp 备份还原；全量 test:unit（串行，最终树）247 文件 1959 测试 ✅ |
| 2026-09-19 | K2-04a 快捷键提示按平台与语言出词 | 6fd0ab01 | 基线：头部两个 `title` 用 `${t('common.undo')} (Ctrl+Y/Z)` 拼、菜单两行 `combo` 写字面量，其中 `Ctrl+Y` 在 macOS 上不通（`kanban-history.ts` 绑定 mod+Shift+Z）。改走仓库既有机制：`combo` 写规范 token 交给共享 `Kbd`→`prettyCombo`，头部 `title` 走新参数化键 `preview.kanban_undo_shortcut`/`kanban_redo_shortcut` + `prettyCombo(...).join('+')`（zh 全角括号）。新增 4 例（先红 3/1；zh 例期望值从 `ZH_CN_MESSAGES` 读，避免测试里写中文被 `i18n:check` 拒；macOS 例靠 `navigator.platform` 桩 + `vi.resetModules()` 重取模块图，因 `IS_MAC` 在模块首次读取时冻结）。K2-04 拆成 a…f 六个子项并按实测登记基线（周起始 `kanban-date-picker.tsx:255`、五处句子拼接、`kanban-tag-picker.tsx:55` 借 mindmap 键、子任务两处硬编码英文、18 个 memo 的重绘决策）。typecheck ✅，13 项静态门禁 exit=0 ✅（size 先拦 describe 体 >50 行，夹具移到模块作用域后通过；comments 重算白名单 575 文件 / 3756 条），6 次变异各杀具名用例（`mod+z`→`ctrl+z` 只杀 macOS 那例，证明两个 token 走不同分支），均由 /tmp 备份还原；全量 test:unit（串行，最终树）247 文件 1963 测试 ✅ |
| 2026-09-19 | K2-04b 日期选择器周起始走 locale 数据 | 4db57a0b | 基线：`kanban-date-picker.tsx:255` 用 `locale === 'zh-CN' ? 1 : 0` 猜周首日，恰好对今天两种语言为真、对任何第三种为假。新增导出的 `kanbanWeekStartFor` 改读 `Intl.Locale#getWeekInfo().firstDay`（`% 7` 换成 `getDay()` 口径），无该 API 时按文档化回落；星期行与日期网格继续共用同一个数。新增 `kanban-date-picker.test.ts` 4 例（先红：整文件因函数不存在而全挂），期望值不含中文字面量（`i18n:check` 只允许中文在 zh-CN 资源里），zh 例的 narrow 图元由 `narrowWeekday()` 现算。typecheck ✅，13 项静态门禁 exit=0 ✅（`comments:check` 先拦 6 条，重算白名单后 576 文件 / 3762 条），变异自检 6 次各杀具名用例（去掉 `% 7` → 导出那例；整段 locale 数据读法退回语言码三元 → 同一例；回落写死周日 / 写死周一 → 桩例；网格或星期行改吃常量 0 → zh 渲染那例），均由 /tmp 备份还原；全量 test:unit（串行，最终树，start 12:29:35）248 文件 1967 测试 ✅。另按实测登记 g：日历视图的 `getMonthWeeks`/`CalendarWeekHeader` 与看板外四处同一写法本轮不动 |
| 2026-09-19 | K2-04c 看板标签改参数化整句消息 | 1c50b6c8 | 基线按三种写法（`${t(`、`} {`、`t(...) +`）全仓实测为**六**处而非台账的五处：批量条数量、收起列按钮、分组折叠按钮、子任务删除按钮、子任务计数标题（漏记的那处 `kanban-table-row.tsx:94`）、菜单批量删除。六处各改一条整句键，取值只进 `{count}`/`{name}` 占位，zh 侧半角括号与冒号换全角；5 个碎片键实测无消费者后删除，仍在用的裸键不动。新增 `kanban-message-composition.test.ts` 13 例，双语期望值一律现读 `EN_US_MESSAGES`/`ZH_CN_MESSAGES` 模板再替换占位（资源改措辞测试跟着改，组件退回拼接即红）。刻意不改并登记：`kanban-progress-bar.tsx:86`（可变部分是用户自填的分组名与百分数，其 `:54` 的 `'Other'` 转 d/e）、`kanban-chart-view.tsx:182-183`（K1-05）、`view.ts:118`（冒号后是截断诊断串）、`kanban-root-hooks.ts:402`（副本名写进栅栏成为持久化数据，按 locale 出词＝同一张卡两种界面两个标题，属产品决定）；同时纠正 e 的登记——`'Task name'` placeholder 实测不存在（grep + `git log -S` 双向确认，该框一直用 `t('preview.kanban_add_subtask')`）。`kanban-semantic-controls.test.ts:187` 跟到新键。typecheck ✅，13 项静态门禁 exit=0 ✅（`comments:check` 先拦 2 条，重算白名单后 577 文件 / 3764 条），变异自检 9 次各杀具名用例（丢占位实参：3/3/2/2/2 例；两态互换 2 例；改回拼接：zh 1 例、2 例、3 例——`kanban_subtasks_count` 的 en 期望值恰等于拼接结果，语序只有 zh 那例守得住），均由 /tmp 备份还原；全量 test:unit（串行，最终树，start 13:12:50）249 文件 1980 测试 ✅ |
| 2026-09-19 | K2-04d+e 借键与英文写死改按语言出词 | 0e19f5f8 | 基线三处：标签 chip 删除按钮借 `preview.mindmap_shortcut_remove`（读屏听到思维导图语境）、子任务完成切换写死 `'Mark complete'/'Mark incomplete'`、进度条兜底分段写死 `label: 'Other'`（被 title 拼进用户可见提示）。d 让 `TagChip` 先算一份可见同名值再交给新键 `kanban_remove_tag_named` 的 `{name}`；e 把两态换成 `kanban_mark_{complete,incomplete}_named`，进度条 `Segment` 拆成 `label?`（分组名＝数据）与 `labelKey?`（兜底桶＝消息），并在**渲染时**经 `segmentLabel` 出词——`segments` 在 `useMemo([items, statusColumn])` 里算，计算时 `t()` 会把语言冻在上次求值时刻（同 f 那类病）。删键后实测：看板全树非 `kanban*` 的 `preview.*` 键只剩 `could_not_copy`/`open_in_new_tab`。新增 `kanban-label-language.test.ts` 6 例 + 双语探针共享夹具 `kanban-bilingual-labels.test-helpers.ts`（`message-composition.test.ts` 同步改消费，13 例不变），先红 6/6；`kanban-table-view.test.ts:419` 的定位器原本按借来的键找按钮，随新键更新。变异自检 7 次（借键/英文字面量/两态互换/不传 name/名字取原始 id/绕开 segmentLabel 各杀 2 例；兜底段改回 `'Other'` 只杀 zh 1 例——en 的 'Other' 恰等于写死值，区分力又在中文侧）。登记 h（图标名/emoji/色名以内部 id 作可访问名称，牵动持久化值）与进度条 `key={idx}`。全量 test:unit（串行，最终树，start 13:47:19）250 文件 1986 测试 ✅，typecheck ✅，13 项静态门禁 ✅（白名单重算 580 文件 / 3768 条） |
