# 音乐库模块改进执行计划（music improvement plan）

> 依据：`review.md`（四路并行审查合并台账，SEC/UI/PERF/FEAT 编号）。分支：`music-improvement-qoder-qwen38f`（自 `dev` 53824e3c）。
> 约定：每个条目 = 一个原子提交；顺序执行；每项先写能失败的复现测试，修复后跑回归（typecheck + 相关单测 + 受影响门禁 + pre-commit 全量静态门禁）再提交，提交后更新本文件状态与进度日志。
> 状态图例：`[ ]` 待办 · `[~]` 进行中 · `[x]` 已提交（附 commit short hash）
> review 条目编号见 `.qoder/improvement/music/review.md`。

## 基线

- [x] BASE-0 创建 worktree + `node_modules`/`blog-frontend/node_modules` 软链 + 复制 review.md + 登记本计划
- [x] BASE-1 基线门禁全绿确认（typecheck / 全量 test:unit）

## 第一批 · 安全（SEC-1~14，路线图①）

- [x] M-01 SEC-1a `stream.ts` WebDAV 分支 Content-Type 只用 `row.mime` 并过格式允许列表，非白名单降级 `application/octet-stream` + attachment
- [x] M-02 SEC-1b `app.ts` nonce 盖章限定已知 HTML 文档路由（`/`、`/s/*`、`/authorize`），`/api/*` 的 `text/html` 响应拒绝盖章并强制降级；`lookup.ts` 响应类型限 png/jpeg/webp
- [x] M-03 SEC-2 `patchTrackSchema.coverUrl` 改走 `sanitizeCoverUrl`，显式拒绝 `MUSIC_OBJECT_PREFIX` 前缀
- [x] M-04 SEC-3 删除 R2 对象按行重算 key（`source==='r2'` 且派生匹配）；`importMusicSchema.path` 禁 `music/` 开头、绝对路径与控制字符（保留 Unicode 文件名）
- [x] M-05 SEC-4 `public-settings` 加 `role==='owner'` 判定，关闭时清 owner key
- [x] M-06 SEC-5 `GET /api/music/webdav` 移出 `ensureMusicDir` 写副作用（改显式 POST/写入时确保）
- [x] M-07 SEC-6 cover-lookup 出站 `isAllowedOutboundUrl` + `*.apple.com` 白名单 + `redirect:'manual'`
- [x] M-08 SEC-7 抽共享出站/写预算工具，`music-webdav:*`、`music-play:*`、`music-write:*`、`music-lookup:*` 设具名限额
- [x] M-09 SEC-9 导入 mime 过允许列表；`Content-Length` 仅 R2 分支设置
- [x] M-10 SEC-10 `music-flac.ts`/`music-mp4.ts` 越界返回 null；`scanTrackMetadata`/工具栏调用点补 catch + toast
- [x] M-11 SEC-11 `webdav-xml.ts` 实体解码 RangeError 防护 + parse 兜底
- [x] M-12 SEC-12+FEAT-4 `MusicTrack` 不再下发 `objectKey`（服务端下发 format/extension）；M3U 导出改文件名/签名 URL；封面 `http://` 走 sanitize

## 第二批 · 死控件与静默丢失（UI/FEAT P0-P1，路线图②）

- [x] M-13 UI-1 正在播放面板收藏/置顶按钮接线 `toggleFavorite`/`togglePin`
- [x] M-14 UI-2 歌单描述透传落库（新建+重命名+编辑态回填），store 与 API 同步
- [x] M-15 UI-3 沉浸式播放器与 Hub modal 补可见关闭入口与 `ariaLabel`
- [x] M-16 UI-4 加载失败态：`music.retry` 重试按钮 + 技术字符串不外泄
- [x] M-17 UI-5/UI-6 幽灵令牌 `--accent-subtle`→`--accent-soft`（全仓 21 文件）+ `--sp-12` 修复
- [x] M-18 UI-7 触屏不可达的 hover-only 控件三处改 hub-tags 姿势（默认可见 + md: 降级 + pointer-events）
- [x] M-19 UI-8+PERF-18 队列 `indexOf` 索引错位修复 + byId useMemo + key 稳定化
- [x] M-20 UI-18+FEAT-2 单曲/歌单删除统一 `confirm(tone:'danger')`；加入队列 toast 反馈
- [x] M-21 UI-12 浮动播放器把手 `span role=button` 改 `IconButton` + 正确 `aria-label`（键盘移动模式另项）

## 第三批 · 性能结构三件套（PERF-1~4，路线图③）

- [x] M-22 PERF-1 曲目菜单全局单例化（store `menuTarget`），行内删 `useVisibleTracks` 与重复订阅
- [x] M-23 PERF-2 播放心跳出全局 store（独立 progress 订阅源），歌词高亮按 index 变化更新
- [x] M-24 PERF-3 `/library` 去 lyric 字段 + mutation 单条 merge + loadLibrary in-flight 去重/新鲜度
- [x] M-25 PERF-4 KV 分支 Range 请求窗口对齐（或文档化限制 + 禁尾部 range）

## 第四批 · IO 模型与批量任务（PERF-5~13/22~24，路线图④）

- [x] M-26 PERF-8+PERF-5 `mapWithConcurrency` 工具；WebDAV 批量导入本地 append、尾一次 reload、并发 4–6
- [x] M-27 PERF-12 搜索 debounce + `useDeferredValue`；罗马化分片
- [x] M-28a PERF-14 PATCH track 去重复行读 + tag 归属校验并入 batch
- [x] M-28b PERF-15 歌单 addItem 单批写入 / reorder 收敛往返；客户端批量 items 接口
- [x] M-28c PERF-9 批量元数据/封面匹配并入 transfers 任务模型（进度、防叠加）
- [x] M-29 PERF-10 可视器 rAF 随暂停/隐藏停帧；AudioContext suspend/resume
- [x] M-30 PERF-11 playback-sync 量化条件修复 + 服务端队列保存去串行往返
- [x] M-31 PERF-13 WebDAV 目录超限具名错误 + truncated 显式标记
- [x] M-32a PERF-6 元数据解析精确长度一次取（网络扫描单取单解码 + 上传按格式取窗）；Worker 化评估（结论见进度日志）
- [x] M-32b PERF-7 下载流式 Blob（去三重缓冲）+ 进度 200ms 节流
- [x] M-33a PERF-21 MediaSession setPositionState + seekto 接线
- [x] M-33b PERF-22 readDurationMs probe 8s 超时
- [x] M-33c PERF-23 拖拽/进度指针事件直写 DOM、commit 才 set、XHR 进度节流
- [x] M-33d PERF-24 偏好落盘 250ms trailing debounce

## 第五批 · 能力兑现（后端就绪只差 UI，路线图⑤）

- [x] M-34a UI-15 playlist scope 按 items 顺序短路排序 + 隐藏歌单下失效的排序控件
- [x] M-34b UI-15 接 `reorderPlaylist` store 动作 + 菜单「上移/下移」（键盘等价）
- [x] M-34c UI-15 歌单内拖拽排序（指针交互，接同一 store 动作）
- [x] M-35 UI-9 表头点击排序 + `aria-sort` + 升降序 + 艺人列（含 grid ARIA 结构整改）
- [x] M-36 FEAT-1 播放失败自动跳下一首 + 连续失败熔断
- [x] M-37 FEAT-6 元数据扫描「强制覆盖」开关
- [x] M-38 FEAT-8 播放快捷键（空格/←→/上下曲）+ 命令面板条目
- [x] M-39 UI-10/UI-11 空搜索结果与 WebDAV 失败态文案/出口接线
- [x] M-40 FEAT-3+UI-24 上传：文件夹支持 + AbortController 真取消 + 类型预检 + 聚合反馈
- [x] M-41a UI-16 Segmented 替换手写 radiogroup（视图切换 + 来源筛选，补 roving 焦点与组标签）
- [x] M-41b UI-17 timer 语义：睡眠倒计时改 role=timer/aria-live=off（仅启停播报）；溢出滚动容器补 tabIndex
- [ ] M-41c UI-21/UI-22：MusicPopover 焦点管理（打开入焦/Tab 漫游/关闭归还/anchor aria-expanded）+ 搜索历史 combobox 语义

## 第六批 · 门禁补位（路线图⑥）

- [ ] M-42 token 反向校验门禁（使用未定义 var 即失败）
- [ ] M-43 bundle budget 增 music chunk 条目；music barrel 静态引入链懒化评估
- [ ] M-44 music 纳入 e2e-visual / check-contrast / surfaces 覆盖（含 375px 与 reduced-motion）

## 暂缓（需产品决策，见 review 路线图⑦）

- 离线播放（FEAT-10）、专辑/艺人分组视图、最近播放服务端化（FEAT-9）、EQ/ReplayGain/gapless/重复检测/视频/歌词搜索/歌单分享

## 进度日志

| 日期 | 条目 | commit | 回归结果 |
| --- | --- | --- | --- |
| 2026-09-18 | BASE-0/BASE-1 worktree+计划登记 | be5e08cd | typecheck ✅，全量 test:unit exit 0 ✅ |
| 2026-09-18 | M-01 流端点 Content-Type 白名单（SEC-1a） | 1700b4e3 | 新增 2 例先红后绿（text/html 回显证实），music 3 套件 27 ✅，pre-commit 全部门禁+增量测试 ✅ |
| 2026-09-18 | M-02b cover-lookup 响应类型白名单（SEC-1c） | 85f7eb4f | 新增 1 例先红后绿（上游 text/html 回显证实→500 拒绝），music-routes 16 ✅，typecheck ✅ |
| 2026-09-18 | M-02 nonce 盖章限定非 /api（SEC-1b） | 3053d51b | 新增 tests/security-headers.test.ts 3 ✅（/api 不盖章、/s 与 / 盖章一致）；中间件抽至 src/worker/middleware/security-headers.ts；全量 test:unit 207 文件 1645 ✅，typecheck/comments ✅ |
| 2026-09-18 | M-03 PATCH coverUrl 走 sanitizeCoverUrl 白名单（SEC-2） | b5e7f15f | 新增 2 例先红后绿（内部 music/cover key 被接受并代理证实→null+404；https 保留且省略字段不清空），music 2 套件 27 ✅，typecheck/comments ✅ |
| 2026-09-18 | M-04 删除按行派生 key + 导入路径命名空间隔离（SEC-3） | c372c1f1 | 新增 2 例先红后绿（伪造 victim key 被删证实→不删；music/ 前缀导入 201 证实→400），music 3 套件 32 ✅，typecheck/comments ✅ |
| 2026-09-18 | M-05 public-settings 限 owner 且关闭清 key（SEC-4） | 5bd5f6fc | 新增 1 例先红后绿（member PUT 曾 200 证实→403；关闭后 owner meta 为空），music 3 套件 33 ✅，typecheck/comments ✅ |
| 2026-09-18 | M-06 webdav 浏览去掉 GET 期 MKCOL（SEC-5） | 7681d1e2 | 新增 1 例先红后绿（缺目录时 GET 曾 404/发 MKCOL→现 200 空列表且零 MKCOL），music 3 套件 34 ✅，typecheck/comments ✅ |
| 2026-09-18 | M-07 cover-lookup 出站主机白名单+手动重定向（SEC-6） | f8bcd60d | 新增 2 例先红后绿（非 Apple 主机 artwork 曾被直取、302 曾被当 200 回显→均 500 且零内部请求），music-routes 21 ✅，typecheck/comments ✅ |
| 2026-09-18 | M-08 音乐四类端点具名小时预算（SEC-7） | a0f0dcac | 新增 2 例先红后绿（lookup 60 次后曾无限 200→现 429 带 retryAfter 且 play 家族不受影响；write/play key 曾不记账→现各自 fails=1），music 3 套件 38 ✅，typecheck/size（constants.ts 501 行入 grandfather 基线，常量表豁免）/comments ✅ |
| 2026-09-19 | M-09 导入 mime 过允许列表 + 代理不再伪造 Content-Length（SEC-9） | 692d6a61 | 新增 2 例先红后绿（audio/mpegurl 曾被原样入库→现按格式规范为 audio/mpeg；上游省略时曾用 size_bytes 造出 Content-Length:16 而正文仅 8 字节→现不再伪造），music 3 套件 40 ✅，typecheck/size/comments ✅ |
| 2026-09-19 | M-10 FLAC/MP4 解析越界返 null + 扫描调用点 catch（SEC-10） | e3cc0b7e | 新增 5 例先红后绿（picture 块 4 字节/vendor 2 字节/mvhd 截断曾抛 RangeError→现返回 null/0；scan 抛错曾中断整轮→现跳该曲计 unreadable 并 toast），music 客户端 5 套件 38 ✅，typecheck/size/comments ✅ |
| 2026-09-19 | M-11 multistatus 数字实体越界防护 + 逐块兜底（SEC-11） | 547a8302 | 新增 1 例先红后绿（&#99999999999; 曾致 parseMultistatus 抛 RangeError→现保留原文且正常实体照常解码），webdav 2 套件 20 ✅，typecheck/size/comments ✅；逐块 try 兜底为边界防御（守卫后当前无可触达抛点，已在注释说明） |
| 2026-09-19 | M-12 曲目契约不下发内部对象键 + M3U 可播放 + 封面 https 升级（SEC-12+FEAT-4） | 88dddcf6 | 4 处契约断言先红后绿（objectKey 曾随库下发→现 undefined 并以 format/webdavPath 代替，webdav 删除走 webdavPath；http 封面曾原样回显→现读时升级 https；M3U 曾写内部键不可播放→现写文件名），新增 music-export.test.ts 锁定 M3U 逐行输出；全量 test:unit 209 文件 1667 例 ✅，typecheck/size/comments ✅ |
| 2026-09-19 | M-13 正在播放面板收藏/置顶按钮接线 store toggle（UI-1） | d475c695 | 新增 2 例先红后绿（收藏/置顶按钮曾无 onClick 点击零调用→现调用 toggleFavorite/togglePin 且带当前曲 id），jsdom createRoot 真实挂载断言语义标签定位，typecheck ✅ |
| 2026-09-19 | M-14 歌单描述随新建/重命名落库并在编辑态回填（UI-2） | f0b4f136 | 新增 4 例（其中 2 例先红后绿：描述曾被 store 丢弃→现 trim 后随 createPlaylist/patchPlaylist 下发；无描述的重命名曾会清空→现 patch 不含 description 键），模态框描述框对已有歌单同样可见并预填，lib/api 桶补出 MusicPlaylistPatch，music 17 套件 115 ✅，typecheck/size/comments ✅ |
| 2026-09-19 | M-15 沉浸式播放器补可见关闭按钮与对话框可访问名（UI-3） | b9fc0bcb | 新增 2 例先红后绿（dialog 曾挂通用 overlay.dialog 且无可点关闭→现 aria-label=music.immersive 且 exit_immersive 按钮点击调用 onClose 一次；修复前断言实测收到 'overlay.dialog'），hub modal 同法补 ariaLabel=music.hub_title（其关闭按钮原本已在），typecheck ✅ |
| 2026-09-19 | M-16 加载失败态补重试按钮且技术字符串不外泄（UI-4） | 88bdd5c1 | 新增 2 例（1 例先红后绿：修复前 dialog 实测渲染出 'fetch failed' 且无重试按钮→现仅显示 music.load_failed + Retry 接 loadLibrary，另 1 例守住成功态不出现失败面板）；原始 error 改由 library-load catch console.warn 记录；播放路径 toast 沿用模块统一 toastMusicError 形态未动（已记录）；music 18 套件 119 ✅，typecheck ✅ |
| 2026-09-19 | M-17 幽灵令牌 --accent-subtle 全量替换为 --accent-soft 并补 --sp-12（UI-5/UI-6） | 04acd7c8 | 修复前实测：tokens.css 定义 0 处、var 引用 27 处横跨 22 文件（选中态底色实际透明）；现全量替换并 grep 余 0，含 music/share/blog/components 与 blog-frontend 1 文件；tokens.css 补 --sp-12:48px（行高预留 containIntrinsicSize 生效，非共享令牌故漂移基线仍 88）；主仓 557 例 + blog 270 例 ✅，typecheck/tokens:check ✅；反向校验门禁按计划留待 M-42，视觉断言留待 M-44 |
| 2026-09-19 | M-18 触屏可达：行/卡片/队列操作改 hub-tags 姿势（UI-7） | 2c5c914d | 三处控件由无条件 opacity-0（触屏不可见且透明钮反向拦截点击）改为默认可见、md: 起 hover/focus 降级并配 pointer-events-none/auto；收藏曲目的心形按钮全断点常显。无自动化红：媒体查询行为 jsdom 不可断言，手动验证=窄窗(<768px)三处按钮直接可见可点、宽窗悬停行才显形；375px 视觉断言随 M-44 入门禁。music 17 套件 91 ✅，typecheck ✅ |
| 2026-09-19 | M-19 重复入队曲目按自身位次播放/移除 + byId 记忆化（UI-8+PERF-18） | 526737b3 | 新增 2 例先红后绿（修复前 queue.indexOf 恒返回首个下标：第二次出现的 a 曾 removeFromQueue(0)/playQueueAt(0)→现均为 2，React 同时实测报出 duplicate key `a-0` 告警；修复后告警消失），O(Q²)→O(Q+S)，byId 以 useMemo 随 tracks 缓存，music 18 套件 93 ✅，typecheck/size/comments ✅ |
| 2026-09-19 | M-20 单曲/歌单删除统一 danger 确认 + 加入队列 toast 反馈（UI-18+FEAT-2） | 3d57ecce | 新增 3 例先红后绿（三个入队分支曾全部静默→现均 toast music.added_to_queue，含去重换位分支）；删除确认复用既有 delete_track_confirm/delete_playlist_confirm 未引用键（歌单文案注明曲目保留），与批量/远端删除同 confirm({tone:'danger'}) 形态；确认弹窗为 overlay 门控，键盘路径手测（打开焦点入对话框/ESC 取消），视觉门禁随 M-44；music 19 套件 96 ✅，typecheck ✅ |
| 2026-09-19 | M-21 浮动播放器把手换 IconButton 且文案改移动语义（UI-12） | 248ea804 | 新增 2 例先红后绿（修复前实测捕获 span[role=button] 且 ArrowRight 零调用→现为原生 button（aria-label=music.move_player 新键，en/zh 同步，删无引用的 drag_to_reorder 误导键）且 ArrowRight 提交 {x:116,y:100} 即 16px 键盘步长；键盘方向移动本就在 drag.onKeyDown，无需新行为），music 20 套件 98 ✅，typecheck/i18n ✅ |
| 2026-09-19 | M-22 曲目菜单全局单例化：菜单请求入 store，行内不再各挂一份（PERF-1） | e06460ce | 新增 5 例（4 例先红后绿：修复前每行各挂一份 MusicTrackMenu 且第二行开启时实测同时存在 2 个 role=menu、store 无 trackMenu 可用→现按钮/右键均只投递 {target,anchor} 请求，全 hub 唯一实例，条目执行后自动关闭）；useTrackMenuItems 删去每实例一次的 useVisibleTracks（9 订阅+整库派生），play-all 改为点击时对 getState() 惰性派生；行/卡删局部 isMenuOpen/menuButtonRef 与内联 target={{track}} 字面量；TrackGrid 与表格视图共用的 use-track-menu.ts 随死代码删除；handlers 仍在列表层构一次保持行 memo 生效。music 21 套件 103 ✅，typecheck/size/comments ✅ |
| 2026-09-19 | M-23 播放心跳移出全局 store：独立 progress 订阅源，歌词高亮按 index 变化更新（PERF-2） | 896b0ffe | 新增 6 例全部先红后绿（修复前 progress 模块不存在即红；现 heartbeat/seek 只写 progress store，主 store 状态对象身份实测不变，切歌清零进度，位置保存仍由进度心跳按 >4s 步长触发）；新增 music-store/progress.ts（zustand 小 store + setProgressTime/progressTimeMs），types/index 删 currentTimeMs；player.ts onTime/seek/playTrack/playCollection/playQueueAt/removeFromQueue/clearQueue/loadAndPlay 断点续播改走 progress；playback-sync restore/save 读写 progress，session-sync 双订阅合并快照保住原保存节奏；7 处 UI 订阅（悬浮条/进度环、控制条、状态栏、迷你与沉浸与侧栏歌词）改 useProgress，歌词高亮选择器返回行号数值→仅换行才重渲染；useSeekNudge 改点击时读 progressTimeMs() 不再每拍重渲染。music 22 套件 109 ✅，全量单测 218 套件 1695 ✅，typecheck/size/comments ✅ |
| 2026-09-19 | M-24 库负载去 lyric：/library 只发 hasLyric 标志 + GET /tracks/:id/lyric 懒取，mutation 单条本地 merge 不再整库 reload，loadLibrary in-flight 去重 + 60s 新鲜度（PERF-3） | a293667a | 9 例先红后绿（worker 路由断言库内 lyric 为 null/hasLyric 为真且懒取端点返回全文、PATCH 清空后标志回落；store 断言并发共享一次请求/新鲜期跳过/60s 后重取/失败立即可重试/force 绕过；单条删除与批量收藏实测不再触发 library()）；MusicTrack 新增 hasLyric，rows.ts 出 toLightTrack（SQL CASE 出 has_lyric 列，不再 SELECT lyric 大字段），library.ts 走轻负载；tracks.ts 注册 GET /tracks/:id/lyric；客户端 api.music.trackLyric + 存储层 ensureTrackLyric（hasLyric 且 lyric===null 才取，pending 去重，失败仅告警）；deleteTrack/batchTracks/create/rename/deletePlaylist/addToPlaylist/removeFromPlaylist 全部本地 merge + summarizeLibrary 客户端重算 stats，上传/webdav 导入/移标签失败兜底改 loadLibrary(true)；loadLibrary(set,get,force) 模块级 in-flight 共享 + store 字段 lastLoadedAt 60s 新鲜度（失败不记时间）；工具栏刷新按钮显式 force；三处歌词消费点挂 useTrackLyric，编辑曲目标题弹窗在歌词在途时禁用保存且回填不丢用户已编辑内容；demo 后端同步轻负载 + 懒取端点 + patchTrack 维护 hasLyric。music/demo 28 套件 175 ✅，全量单测 219 套件 1707 ✅，typecheck 与全部静态门禁（size 无新增豁免）✅ |
| 2026-09-19 | M-25 KV Range 改 1MB 对齐窗口流式读取（PERF-4） | 655854fe | 6 例先红后绿（3MB 对象取 100 字节：206 Content-Range 为对齐窗口 bytes 1048576-2097151/3145728、Content-Length=窗口 1MiB、fake KV 实测 arrayBuffer 读取 0 次、stream 拉取 ≤2MiB+64KiB 且提前 cancel；16 字节小文件窗口按 size 截断为 0-15/16；≥1MiB 请求不改动原样透传；跨窗口边界请求扩到下一边界 0-2097151；无 Range 仍走 arrayBuffer 恰 1 次）。range.ts 新增 KV_RANGE_WINDOW_BYTES+alignKvRangeWindow（length≥窗口直通，否则 floor/ceil 对齐并保证包含被请求区间、按 size 截断）；storage.ts KV 有 range 分支改 get(key,'stream')+sliceKvStream 逐块跳读，命中窗口尾 reader.cancel() 且 controller.close()，isolate 不再物化整段 ≤25MiB 值，无 range 保持 arrayBuffer（需真实 byteLength 出 Content-Length）；stream.ts 仅当 backend 为 kv 时把 range 对齐为窗口，Content-Range 如实描述被服务的窗口（nginx slice 先例，浏览器可接受超集 206）。R2 路径与既有精确 Content-Range 断言未动。music-routes 31 ✅，全量单测 219 套件 1712 用例两轮 ✅（首轮 1 例负载抖动超时、复跑两轮全绿），typecheck 与全部静态门禁（escape 双改单经一次修正、size 无新增豁免）✅ |
| 2026-09-19 | M-26 mapWithConcurrency + WebDAV 批量导入并发改本地 append 尾一次 reload（PERF-8+PERF-5） | b053a0c6 | 11 例先红后绿（async 工具 4 例：乱序完成仍按输入序返回、10 项限 3 实测 maxActive===3、首错即拒、空表直通；webdav 导入 5 例：5 文件 importWebdav 5 次且 loadLibrary 恰 1 次且 reload 时 tracks 已含 5 条本地 append、8 文件并发 >1 且 ≤4 且结束清空 importingPaths、单文件失败其余继续且仍尾 reload、在途行 importingPaths 含该路径、单文件导入保持一次 reload；track-io 3 例：6 文件上传并发 >1 且 ≤4 且整批仅 1 次 reload、5 轨下载 fetch 并发 >1 且 ≤4 且各自完成即 saveBlob；refreshTrackMetadata 并发 >1 且 ≤4 且 8 轨全部 updated）。新增 lib/async.mapWithConcurrency（游标工作池，结果按输入序，首拒即拒）；music-utils 新增 TRACK_IO_CONCURRENCY=4；uploadFiles/downloadTracks/refreshTrackMetadata（循环体抽为 refreshOneTrack 持 counters）/importWebdavFolder 四处串行 for-await 全改限流并发；WebDAV 目录导入不再每首整库重载：importOneWebdav 抽公共导入 + appendImportedTrack 本地并入 + 尾一次 loadLibrary(true)；MusicWebdavState.importingPath 改 importingPaths: string[]（并发行各自可见），webdav 模态行内 loading 与 import-all 按钮 disabled/loading 同步。既有串行行为断言（poison 单轨跳过、toast 聚合计数）未动。全量单测 222 套件 1725 ✅，typecheck 与全部静态门禁（size 无新增豁免、webdav-import describe 按并发/非并发拆二避长函数）✅ |
| 2026-09-19 | M-27 搜索输入 debounce + useDeferredValue + 罗马化分片让出事件循环（PERF-12） | e8527780 | 10 例先红后绿（music-search 2 例：ensureRomanized 每批按 ROMANIZATION_BATCH=200 回调累计尺寸 [200,400,450]、批间 setTimeout(0) 让出宏任务队列实测穿插；library-load 2 例：prepareRomanization 并发共享同一次 pass（模块级 in-flight 守卫）且 onBatch 中途已把部分 romanized 写进 store；music-search-box 4 例：假定时器下 200ms 内不写 store、到期写入最终值、连打只落最后一次、Enter 立即 commitQuery 并入历史、外部改 query 时输入框采纳；既有关键词/拼音排序 2 例保持绿）。实现：SearchBox 迁入新文件 music-search-box.tsx（连同 SearchHistory/SearchClearButton/useDebouncedText 拆分以过 50 行长函数门禁，未新增 size 豁免），组件本地 text 状态 + 200ms debounce 写 setQuery，Enter/清除按钮 flush 走 commitQuery，卸载清理定时器，lastSentRef 防外部回写回声；selectors.useVisibleTracks 对 query 走 useDeferredValue，重排名期间先画上一帧结果；music-search.ensureRomanized 新增可选 onBatch 并每批 await setTimeout(0) 让出；prepareRomanization 在途去重且逐批发布部分字典。验证边界如实记录：useDeferredValue 为 React 胶水，无并发渲染专用断言，仅由 typecheck 与既有渲染测试覆盖；i18n 门禁为 library-load.test 的中文曲名夹具按既有格式登记白名单。全量单测见本行提交时结果，typecheck 与全部静态门禁 ✅ |
| 2026-09-19 | M-28a PATCH track 去重复行读 + tag 归属校验并入 batch（PERF-14） | adcc5797 | 1 例先红后绿（新增 recordingDb 代理统计 prepare 的 SQL：track 行 SELECT 3→2 即写前存在性读+响应权威读、外键归属独立探测 1→0；同一 PATCH 拒绝外来 tag 的行为断言 tagIds==[own] 保持）。tracks.ts：registerPatchRoute 删除第一次仅判存在的 loadTrackRow，row 复用给 coverDataUrl 分支；replaceTrackTags 由「全量 owned select + 逐 tag INSERT」改为 DELETE + INSERT…SELECT（user_id 双保险）单 batch。M-28 行按原子提交拆分为 M-28a/b/c 三行（PATCH 去重 / 歌单批量 / transfers 任务模型为三件独立事项）。music-routes 32 例全绿，全量单测见本行提交结果，typecheck 与静态门禁 ✅ |
| 2026-09-19 | M-28b 歌单条目路由全 batch 化并新增批量 items 接口（PERF-15） | c4375f4e | 5 例先红后绿（服务端 countRoundTrips 代理区分批内/批外执行：单条 addItem 批外 5→0 且 ≤2 次 batch；批量 items/batch 一次请求跳过已存在与非本人 track 且 added/skipped 计数正确、顺序为请求序追加；reorder 批外 5→0；demo 后端 batch 路由镜像同契约；客户端 store 一次 addPlaylistItems 请求合并返回条目并入 playlist 且 sortOrder 续排）。playlists.ts：addItem/reorderItems/removeItem/loadPlaylist 全部改 db.batch 往返，playlistSelect/touchStatement/columnOf/chunks 抽公共件，ownership+cap 探测并入单批读、写+touch 并入单批写；orderAfterReorder 纯函数化（顺带把原 O(n²) includes 过滤改 Set）。schemas.ts 新增 batchPlaylistItemsSchema（≤500，IN 按 96 切块防 D1 100 参上限）。客户端 addSelectionToPlaylist 由逐首 await 改一次批量请求，mergePlaylistItems 直接消费服务端返回条目；demo backend routes/music-organizer.ts 同步注册 /items/batch。既有单条 added/existed/400/404/413 行为断言未动。全量单测见本行提交结果，typecheck 与静态门禁 ✅ |
| 2026-09-19 | M-28c 批量元数据/封面匹配并入 transfers 任务模型（PERF-9） | bc422e6c | 5 例先红后绿（元数据扫描：运行中实测存在 kind=metadata 的 running 任务、成功后清空且 transfersOpen 为 true；在途时第二次 refreshTrackMetadata 返回 0 且不叠加任务；封面匹配：3 轨逐次 lookup 时 done 采样实测 [0,1,2]；lookup 拒绝留下 failed 任务并 toast action_failed，dismissLibraryJob 可清；无缺封面时不登记任务、不开对话框）。types 新增 MusicLibraryJob{kind,done,total,status} + libraryJobs 状态 + dismissLibraryJob 动作；transfers.ts 新增 runLibraryJob（每 kind 唯一在途、二次调用直接拒绝、body 内 advance 逐步推进、成功即移除、失败保留为 failed 并带具名 toast），与 uploads/downloads 共用 transfers 模型；refreshTrackMetadata/matchMissingCovers 主体包进 runLibraryJob，toast 汇总与返回计数语义不变；MetadataButtons 删本地 scanning/matching useState 改 store libraryJobs 选择器（防叠加不再随工具栏重挂载丢失），顺带消除 matchCovers 原先无 catch 的未处理拒绝路径；传输对话框新增批量任务区（种类文案 + done/total 进度 + failed 可关闭），i18n 两语各 5 键。服务端 batch-metadata 合并端点本次未做：审计该建议属另一事项，PATCH 往返已由 M-26 并发上限与 M-28a 去重兜底。全量单测 224 套件 1744 ✅，typecheck 与全部静态门禁（size 无新增豁免）✅ |
| 2026-09-19 | M-29 可视器 rAF 随暂停/出视口停帧 + AudioContext 暂停后 suspend（PERF-10） | 075aa2e8 | 6 例先红后绿（visualizer 4 例：暂停时实测绘制恰 1 帧且 rAF 请求 0 次；播放开始请求帧、再暂停即 cancel 且不再排新帧；IO 报出视口后 cancel 且 rAF 计数冻结、回视口恢复排帧；暂停态改 documentElement class 触发恰一次重绘且仍 0 rAF；既有 visualizerLevels 3 例保持绿。audio-engine 2 例：假 AudioContext + 假定时器实测 pause 后 4.999s 不 suspend、play 撤销挂起计时且不误 resume、持续 5s 才 suspend 恰 1 次、再 play 即 resume 且 state 回 running；未建图时 pause 不新造 AudioContext）。music-visualizer.tsx：paintLoop 改门控调度（start 需 visible∧isPlaying∧!reduceMotion，step 不再无条件续排；paused 只画基线一帧），watchRepaints 抽 IntersectionObserver 与 documentElement class/style MutationObserver（后者仅在循环停着时补画一帧，运行中仍按 60 帧刷新 accent），卸载 disconnect 双观察器；隐藏标签页由浏览器原生冻结 rAF 回调，不再叠一层 visibilitychange。audio-engine.ts：pause 监听挂 scheduleAnalyserSuspend（PAUSE_SUSPEND_DELAY_MS=5s 窗口吸收 transport 点按/切歌抖动），play 监听先 resumeAnalyserContext（清计时 + 仅 suspended 才 resume）再上报状态；只 suspend 不 close——createMediaElementSource 每元素仅一次，close 会毁掉分析图；注释记录「播放中挂起会消音」故仅在暂停站稳后挂起。jsdom 无 matchMedia，测试内补桩。全量单测 225 套件 1750 ✅，typecheck 与全部静态门禁（size 无新增豁免）✅ |
| 2026-09-19 | M-30 playback-sync 位置量化改显式锚点 + 队列轨道读取单 batch（PERF-11） | dc77370f | 2 例先红后绿（客户端：挂载 session-sync 后按 250ms 心跳推进位置至 20s，旧实现相邻快照差恒 250ms 从未越过 4000ms 步长、保存 0 次即红，新实现按累计漂移每 ~4s 落一次盘、20s 内保存 ≥1 且 <10 次；服务端：150 轨队列 GET /playback，旧实现逐 50 块串行 await 实测批外往返 1+3 次即红，新实现批外仅播放行 1 次读 + 轨道读并入恰 1 次 batch，且队列顺序/currentIndex/positionMs 断言不变）。playback-sync.ts：hasPlaybackChanged 位置分支改与显式 savedPositionMs 锚点比较（队列/索引仍按相邻快照的身份比较）；music-session-sync.tsx 持有锚点，调度保存与 hidden flush 时各自更新锚点。worker playback.ts：loadQueueTracks 由串行 for-await 改 db.batch 单往返，QUEUE_CHUNK 50→96 对齐 D1 100 参上限（与 playlists.ts chunks 同规则）；tests/music-routes.test.ts makeDb 补 MUSIC_PLAYBACK_MIGRATION_STATEMENTS 建 music_playback 表。审计另列的「队列上限降 200–300 或只存来源指纹」未采纳：GET 已是单往返、PUT 本就是单 upsert，500 上限无实际代价，缩上限反而损功能。全量单测 225 套件 1752 ✅，typecheck 与全部静态门禁（size 无新增豁免，progress.test 两个保存用例拆入新 describe 避长）✅ |
| 2026-09-19 | M-31 WebDAV 目录超限具名 502 + 条目截断显式标记（PERF-13） | 1e852a34 | 4 处新增断言（webdav-xml：parseMultistatus 返回 {entries,truncated}，2001 块只解析 2000 且 truncated 为 true、小列表恒 false、空体返回空结果对象；路由：600KB PROPFIND 响应实测 502 且 error.code=webdav_listing_too_large、消息指向子目录、2005 条目录实测 200 + entries 恰 2000 + truncated true + 既有整列表 truncated false；store：browseWebdav 把 truncated 记入 webdav 状态、下一次完整列表清除标记）。backup/common.ts 把两处「超出安全上限」裸 Error 改为具名 ResponseTooLargeError（消息逐字不变，备份按消息文本映射的 friendlyError/transient 判定不受影响）；routes/music/webdav.ts 新增 readListingBytes 仅在命中该具名错误时转 ApiError(502, webdav_listing_too_large)，网络错误仍走原路径；listMusicDirectory 返回 {entries,truncated}，browse 三个分支均带 truncated；ApiErrorCode 联合新增该码。music-webdav-modal.tsx：新增 WebdavNotices（两个 role=status 行，顺带激活原本无处可见的 browse 失败态与闲置键 music.webdav_failed，服务端消息按既有 reason 直显惯例展示），Browser 因此不超 50 行门禁；webdav-xml.test 拆分 caps describe、progress.test 拆 describe 均过 size 门禁（17 豁免未增）。demo 无 webdav browse 端点，无需镜像。全量单测 226 套件 1757 ✅，typecheck 与全部静态门禁 ✅ |
| 2026-09-19 | M-32a 元数据按声明长度一次取、只解码一次（PERF-6） | 12079076 | 2 例先红后绿（music-metadata.test：对 600KB 声明长度的 ID3 标签，旧实现每 256KB 步进对累积缓冲重跑封面解码与整套帧遍历，实测 cover/tags 各 3 次即红，新实现取满至 tagSize+10 恰 1 次解码，且断言标签窗口请求止于 614409；上传路径旧实现任何格式先物化 min(6MB,整文件)，新实现 fakeFile 记录 slice 窗口、实测最大读止于 tagSize+10=614410 即红→绿）。测试经文件级 music-cover 直通桩计数（importOriginal 委托真实实现），既有 7 例不动全绿。music-metadata.ts：readId3Tags 由「步进循环+每轮解析+提前止」改为 fetchChunked 精确长度单取、末尾一次性解码（旧「cover+lyric 齐即停」的最好情况换为确定代价，标签长度由 header 权威声明）；fetchChunked 预分配单缓冲就地填充，短块即对象尾（省旧实现尾段 1 次注定失败的 3 重试请求），concatBytes 随之删除；readFileMetadata 先读 10 字节头判格式，再按格式取窗：FLAC 64KB（neededBytes 扩张循环保留）、MP4 512KB（与网络扫描窗一致，moov 在尾仍走 tail 回退）、ID3 精确 tagSize+10（上限 6MB 不变）、无标签直接 NO_TAGS。Web Worker 化评估（审计原文即「评估」）：本次不做——修复后解码每轨恰一次，帧遍历为 O(frames) 的 subarray 零拷贝视图，剩余大头 createImageBitmap 本就异步，一次 320px 缩放 toDataURL 为数十 ms 单次占用；搬 Worker 需新增 vite worker 入口+消息协议（transferable 可免拷贝但缓冲须独占所有权）+vitest jsdom 无 Worker 的测试基座，代价 M 且收益仅在该数十 ms，批量扫描实测若有卡顿再以本行为据升级。M-32 行按原子提交拆为 M-32a/M-32b 两行。全量单测 225 套件 1759 ✅，typecheck 与全部静态门禁（size 无新增豁免、注释白名单同步再生成）✅ |
| 2026-09-19 | M-32b 下载改流式 Blob + 进度 200ms 节流（PERF-7） | be68fda0 | 3 例新增、2 例先红后绿（假定时器冻结 Date.now 后 40 块突发下载，旧实现每块一次 updateDownload 实测 40 次中间态写入即红，新实现节流为 1 次；「细水长流」守卫例双向绿，防过度节流：每块推进 250ms 假时钟则 5 次进度全报 [20,40,60,80,99]；空响应体例守住 'The downloaded file is empty' 具名失败）。transfers.ts：collectStream 改 streamToBlob({body,totalBytes,mime,onProgress})，块直接进 Blob（浏览器可落盘 backing），删去「chunks 数组→concatChunks 整倍复制→toArrayBuffer 再三倍复制」路径，峰值内存自 ~3× 体积降至浏览器 Blob 托管；throttledProgress（PROGRESS_THROTTLE_MS=200，Date.now 锚点）包 updateDownload，终态 100% 由 done 分支直写不经节流；fetchTrackBytes/toArrayBuffer/concatChunks 删除。审计所列 File System Access 直写磁盘备选未做：saveBlob 走 <a download> 已是既有统一保存路径，FS Access 需 handle 权限与 Safari 回退双路径，属另一事项。既有 blob.size 断言经 jsdom 真 Blob 保持。全量单测 225 套件 1762 ✅，typecheck 与全部静态门禁（size 无新增豁免）✅ |
| 2026-09-19 | M-33a MediaSession 位置状态与锁屏拖动（PERF-21） | cd247ff9 | 4 例先红后绿（audio-engine.test 新增 FakeMediaSession，其 setPositionState 按规范对 duration≤0/position 越界抛 RangeError 以逼实现方自检：90.5s/120s + 元素 playbackRate=1.5 实测发布 {duration:120,position:90.5,playbackRate:1.5}；duration 0 与 NaN 位置零发布、过期 tick 钳到时长尾；bindMediaSessionActions 绑定 seekto，回调 {seekTime:12.5} 收到 seek(12500)、无 seekTime 不动作；navigator.mediaSession 缺失两函数均静默）。audio-engine.ts：新增 updateMediaSessionPosition（秒制、钳位、播放倍率取 audioElement().playbackRate，try/catch 走具名 console.warn 惯例）；bindMediaSessionActions handlers 增 seek，entries 增 seekto 项（类型改 MediaSessionActionHandler）。player.ts connectAudio：onTime 心跳同步喂 updateMediaSessionPosition(ms, get().durationMs)，seekto 处理器接回 store 的 seek（元素+progress store 双写与手动拖动同路）。锁屏进度条自此可拖可显。验证边界：onTime/seekto 两行接线为薄胶水，由 typecheck 与既有 store 测试守编译与语义，行为断言落在 engine 层。全量单测 225 套件 1766 ✅，typecheck 与全部静态门禁（size 无新增豁免）✅ |
| 2026-09-19 | M-33b 上传时长探测 8s 超时（PERF-22） | d36f3e34 | 新增 music-probe.test.ts 3 例，1 例先红后绿（假定时器下浏览器永不解码的容器：旧实现 promise 永挂，7.999s 无值→8.000s 实测解析 0 且 revokeObjectURL 恰 1 次、src 已摘除即红→绿；另 2 例守卫双向绿：loadedmetadata 先到解析 96_000 且其后超时不再二次结算/不重复 revoke、error 事件解析 0）。music-probe.ts：PROBE_TIMEOUT_MS=8_000 一次性 setTimeout + settled 闩，finish 统一 clearTimeout/revoke/摘 src/resolve，超时与 error/无时长同走 0（上传链按 0 处理，与既有 .catch(() => 0) 调用点语义并集不变）。串行上传链不再被永久挂起的探测卡死。全量单测 226 套件 1769 ✅，typecheck 与全部静态门禁（size 无新增豁免）✅ |
| 2026-09-19 | M-33c 拖拽/DropZone/进度事件节流（PERF-23） | 1d821c43 | music-drag.ts：指针移动不再 setLive 逐帧重渲染，改 showPosition 直写元素 left/top（liveRef 记账），仅 pointerup/pointercancel 提交一次；hook 新增 setNode 回调 ref，floating-player 以 attachCard 与 useMeasuredSize 组合；style 与键盘基准只读已提交 position；useDragListeners 参数收为对象。music-transfer-dialog.tsx：DropZone 内聚 isDragOver 并以 dragenter/dragleave 深度计数消抖子元素进出抖动，drop 归零重置，UploadPicker 不再持有该状态。lib/async.ts：新增共享 throttledProgress（200ms、Date.now 锚点，第三次出现故提取），transfers.ts 删本地副本改导入，library-collections.ts 上传进度写点包裹（XHR 每事件写 store 聚合为 ≤1/200ms，终值 100 仍走直写）。先红观察：music-drag.test.ts 每次 move 重渲染计数 3≠2 红、track-io.test.ts 突发 50 次进度全量落 store 红、async.test.ts throttledProgress is not a function 红、music-transfer-dialog.test.ts 因 DropZone 未导出/无 dragenter 两例红，实现后全绿。诚实说明：修复后首跑全量有 1 例失败但未留存用例名，随后两次全量 1775 例全绿（新测试单跑亦全绿），未能定位该偶发用例。全量单测 228 套件 1775 ✅，typecheck 与全部静态门禁（size 基线维持 17 项无新增豁免）✅ |
| 2026-09-19 | M-33d 偏好落盘 250ms trailing debounce（PERF-24） | d15d4c99 | 新增 persist.test.ts 2 例先红后绿（假定时器下连续 3 次 setVolume，旧实现每次同步写 localStorage 即红；249ms 无写、250ms 恰写 1 条且 volume=0.5 绿；旧实现 pagehide 前已写 4 条即红，新实现 pagehide 立刻 flush 恰 1 条 volume=0.25、其后推进 500ms 不再重复写）。player.ts：persist 单点（11 个动作调用点）改 PREFS_WRITE_DEBOUNCE_MS=250 定时器合并，到期 read-through 最新 store 值；注册 window pagehide 具名 flush（typeof window 守卫，node 端不注册），杜绝关页静默丢失末次变更；state.ts savePreferences 本体与加载语义不动。验证诚实说明：并行全量前两次出现 3~4 例失败，均为 calendar fuzz/auth D1/excalidraw/slides 渲染等非音乐用例，单跑全部通过，判定为本机并行高负载下的超时（失败时长 5~12s 贴着默认 5s 超时），与本 diff 无关；最终以串行全量（--no-file-parallelism --testTimeout=30000）229 套件 1777 全 ✅，typecheck 与全部静态门禁（size 基线 17 项不变）✅ |
| 2026-09-19 | M-34a 歌单 scope 按 items 顺序短路排序（UI-15 之一） | 0e07c6e5 | 新增 library-load.test.ts 歌单两例，1 例先红后绿（scope=playlist、sort=title 时旧实现把 items 顺序 t3/t1/t2 重排为 t1/t2/t3 即红；置顶位 t1 不再被提前）。library-load.ts：visibleTracks 在 playlist scope 直接返回过滤结果，不再走 sortTracks（查询/来源过滤保留）。music-hub-toolbar.tsx：ToolbarActions 订阅 scope，歌单下隐藏排序 Segmented 与其分隔线（该控件此时改了也无效，属死控件）。为 M-34b/c 铺垫：先让手工顺序成为唯一秩序来源。验证：music 32 套件 172 ✅，typecheck ✅，10 项静态门禁全 ✅（注释白名单再生成 3689 条），串行全量（--no-file-parallelism --testTimeout=30000）229 套件 1779 全 ✅ |
| 2026-09-19 | M-34b 歌单上移/下移接 reorderPlaylist（UI-15 之二） | 3036a2e1 | 8 例先红后绿（movePlaylistItem 3 例：换邻位发完整 itemIds 并以响应整体替换 store 条目、越界移动不发请求、服务端拒绝时保序并 toast；openTrackMenu 3 例：playlist scope 自动补 itemId/playlistId、非歌单不补、显式身份不被覆盖；菜单 UI 2 例：行内出现上移/下移/移除且边界禁用、点击上移以 (p1,i1,-1) 调 store 并关菜单——红于函数缺失与身份未注入）。library-collections.ts 新增 movePlaylistItem（按 sortOrder 排序、摘出再插回、调 api.music.reorderPlaylist、renamePlaylist 惯用法替换条目、不重载库）；library-load.ts openTrackMenu 增 get 参数并集中回填行身份（行/卡片仍只上报 track，救活原死掉的 unlink 菜单行）；music-track-menu.tsx 抽出 playlistMenuItems（对象参数，规避 50 行限）；types.ts/library.ts 登记动作；locales en/zh 补 music.move_up/move_down。为守 50 行函数限把 library.ts 歌单绑定拆为 playlistActions 辅助、菜单拆 playlistMenuItems，未新增 size 豁免（基线 17 不变）。验证：music 32 套件 180 ✅，typecheck ✅，10 项静态门禁全 ✅，串行全量（--no-file-parallelism --testTimeout=30000）229 套件 1787 全 ✅ |
| 2026-09-19 | M-34c 歌单行拖拽排序接 movePlaylistItemToIndex（UI-15 之三） | 8fc61a75 | store 3 例先红后绿（movePlaylistItemToIndex：摘出再插回到目标索引、目标越界钳到末位、原地落点不发请求）。library-collections.ts：抽出 orderedPlaylistItems 与 sendReorderedPlaylistItems，菜单移动与拖拽共用发送核心；拖拽落点按指针位置钳位（不同于菜单的相邻一步越界即忽略），因拖拽只知道落点索引。types.ts/library.ts 登记 movePlaylistItemToIndex。music-track-list.tsx 新增 usePlaylistDrag（仅 playlist scope 提供 handlers；dataTransfer 携带 itemId，落点 track 映射回存储顺序的索引，查询过滤下依然对位）；music-track-row.tsx 增 TrackRowDragHandlers 与 dragProps 展开（抽出以守住 50 行函数限，未新增 size 豁免，基线 17 不变），拖拽行 opacity-40。新 music-playlist-drag.test.ts 3 例：拖 t1 行落到 t2 行以 ('p1','i1',2) 调 store（红于动作缺失）、落到自身不调用、非歌单 scope 行不可拖拽——后两例为守卫例（旧行为本就不触发，绿测）。已知缺口：网格视图未接拖拽（键盘/菜单等价已在 M-34b），登记于本行。验证：music 33 套件 186 ✅，typecheck ✅，10 项静态门禁全 ✅（注释白名单 3701），串行全量（--no-file-parallelism --testTimeout=30000）230 套件 1793 全 ✅ |
| 2026-09-19 | M-35 表头点击排序 + aria-sort + 艺人列 + 表格 ARIA 整改（UI-9/UI-18/FEAT-9 之排序部分） | c8585c92 | store 5 例 + UI 5 例先红后绿（sortTracks：专辑列序、时长升/降、desc 反转且置顶仍提前；setSort 换字段即回 asc；visibleTracks 采纳方向；表头：点标题升序再点降序且 aria-sort 跟随、歌手列可排、闲置列 aria-sort=none、行内子级全为 cell 且列数与表头一致）。playlist scope 无排序按钮一例为守卫例（旧行为本就无按钮，红测时即绿，实现后仍绿）。types.ts：MusicSort 增 album/duration，新增 MusicSortDirection 与 sortDirection 状态、setSortDirection 动作；state.ts/index.ts/player.ts：偏好持久化 sortDirection 并校验；library-load.ts：setSort 重置方向为 asc，sortTracks 拆出 compareByField（自然升序）+ flip，钉选双方向恒提前；library.ts 登记动作。music-track-table.tsx：容器 role=grid 降级为 role=table（无单元格级漫游导航的表格本就不是 grid，读屏语义合法），全部列头 columnheader 化并新增 SortableColumn（原生 button + aria-sort + 方向箭头，playlist scope 只出纯文本）；新增歌手列（xl 起显示，标题副行艺人在 xl 隐藏避免重复）；music-track-row.tsx：每行 10 个直接子级全部 role=cell（含封面/收藏/菜单包裹），来源列固定 w-16 与列头对齐（修 UI-9 提到的窄→宽错位）。复用既有 music.table_artist 键（原为未引用幽灵键）。验证：music 35 套件 196 ✅，typecheck ✅，10 项静态门禁全 ✅（注释白名单 3706），串行全量（--no-file-parallelism --testTimeout=30000）1803 全 ✅ exit 0 |
| 2026-09-19 | M-36 播放失败自动跳下一首 + 连续失败熔断（FEAT-1） | d4018836 | 4 例先红后绿（player-failure.test.ts：坏曲跳过并在下一首起播、连败 3 首停住并 toast playback_repeated_failures、用户重新 playCollection 清零连败、repeat-one 失败不跳转——最后一例为守卫例，红测时即绿）。player.ts：reportPlaybackFailure 改返回 boolean（同一尝试内 error 事件与看门狗竞速只计一次），新增 handlePlaybackFailure（计数、熔断、repeat-one 直接停、否则 playNext），loadAndPlay 失败路径与 onBuffering 看门狗、connectAudio onError 三处接入；成功起播即清零；playTrack/playCollection（用户主动起播）入口清零连败计数。locales en/zh 新增 music.playback_repeated_failures。缺口如实登记：审查方案里的「坏曲标记」（持久化标记坏文件）未做，本条只兑现自动跳 + 熔断。验证：music 35 套件 200 ✅，typecheck ✅，10 项静态门禁全 ✅（注释白名单 3709），串行全量 exit 0 全绿（232 套件 1807 例，上一轮 230/1793 加本项两文件与增量推算） |
| 2026-09-19 | M-37 FEAT-6 元数据扫描「强制覆盖」开关 | 7ba48d30 | 4 例先红后绿（library-tracks.test.ts force 组：强制模式用文件标签覆盖已存的封面/歌词/歌手/专辑、强制模式对元数据齐全的曲目仍重新扫描、强制模式不改写标题——3 例红于 force 未实现；「填充模式对齐全曲目零写入」一例为守卫例，红测时即绿）。library-tracks.ts：refreshTrackMetadata/refreshOneTrack/scanPatch 增 force 参数，force 下无条件重扫且封面/歌词/歌手/专辑以文件为准整体覆盖，标题修复与时长只填不删两条规则两 mode 共用（标题恒不被扫描改写）；types.ts 登记 force 可选参。music-hub-toolbar.tsx：MetadataButtons 新增强制覆盖 IconButton（RotateCw），目标是当前列表全部曲目而非 missingIds，点击先走 overlay confirm（tone=danger，明示手动修改会丢失），确认后 refreshTrackMetadata(ids, true)。locales en/zh 新增 music.metadata_force / metadata_force_confirm。诚实说明：工具栏按钮本身无专属 UI 测试（confirm 编排 + 禁用态未测），store 层 force 语义由上述 4 例覆盖。验证：library-tracks 12 ✅，music 35 套件 204 ✅，typecheck ✅，10 项静态门禁全 ✅（注释白名单 3715，i18n 3065 键），串行全量（--no-file-parallelism --testTimeout=30000）232 套件 1811 全 ✅ exit 0 |
| 2026-09-19 | M-38 FEAT-8 播放快捷键 + 命令面板条目 | 1b3664f6 | 7 例先红后绿（新 music-hotkeys.test.ts 5 例：有曲且焦点在非交互处时空格切换播放并 preventDefault、无曲时空格放行、焦点在按钮上空格放行（守键盘激活按钮不被劫持）、mod+方向键切上下一曲、alt+方向键±10s 且钳进曲内；新 command-palette/music-commands.test.ts 2 例：playbackCommands 三条目 id/combo 齐全、run 接通 store 动作。两文件红于模块/函数缺失）。lib/hotkeys.ts：Hotkey 增可选 when 门控（combo 命中后、preventDefault 前咨询，false 则把键留给页面），matches 增 space 别名，prettyCombo 渲染 Space。新增 features/music/music-hotkeys.ts：MUSIC_HOTKEYS 五条（space 播放/暂停、mod+←/→ 上下一曲、alt+←/→ 快退快进 10s），空格仅在已载入曲目且焦点非交互时接管——否则会毁掉按钮键盘激活与空格滚动，方向键全部带修饰避免劫持编辑器/列表导航。music-utils.ts：SEEK_STEP_MS 与钳位式 seekTargetMs 自 music-transport-widgets 迁入（按钮与快捷键共用一份算式）；app-shell.tsx 注册 MUSIC_HOTKEYS（快捷键参考面板自动列出）；use-commands.tsx：playbackCommands 三条命令并入面板结果集（经 music barrel 直达 store，中枢关闭时也能从面板控制播放）；music-immersive-player.tsx 沉浸页底部渲染 music.keyboard_hint（消灭幽灵键，文案改为与真实绑定一致）；locales 新增 music.play_pause。验证：music+command 38 套件 214 ✅，typecheck ✅，10 项静态门禁全 ✅（注释白名单 3720，i18n 3066 键），串行全量（--no-file-parallelism --testTimeout=30000）234 套件 1818 全 ✅ exit 0 |
| 2026-09-19 | M-39 UI-10/UI-11 空搜索结果与 WebDAV 失败态出口 | 96352f24 | 新 music-empty-states.test.ts 4 例中 2 例先红（清空搜索出口、失败态优先于空目录且重试按当前路径调 browseWebdav）、2 例为守卫例（无查询时保留 no_tracks_hint、无错误时仍显示 webdav_empty，红测时即绿）。music-track-list.tsx：抽出 NoTracks——查询非空时显示 music.no_results + 命中词回显 + 「清空搜索」按钮（setQuery('')），不再对空结果库推销上传；music-webdav-modal.tsx：listing 拆出 WebdavListing（守 50 行限），error 且无条目时渲染 webdav_failed Empty（description 带服务端错误原文 + common.retry 重试按钮）而非空目录文案，WebdavNotices 加 hasEntries 条件避免失败文案双份。无新增 i18n 键（复用 no_results/search_clear/webdav_failed/common.retry）。验证：music-empty-states 4 ✅，music 37 套件 213 ✅，typecheck ✅，10 项静态门禁全 ✅（注释白名单 3722，size 基线 17 不变），串行全量（--no-file-parallelism --testTimeout=30000）235 套件 1822 全 ✅ exit 0 |
| 2026-09-19 | M-40 FEAT-3+UI-24 上传文件夹/真取消/预检/聚合反馈 | dcc36b2d | track-io.test.ts 新增 8 例中 6 例先红（扩展名预检聚合跳过 toast、超限文件不建任务不重载、dismiss 即 abort（captured signal.aborted 且无错误 toast）、webdav 通道同样收到 AbortSignal、整批一条成功 toast 带计数、整批一条失败 toast 且失败行保留；「全部合格则无跳过提示」与「全部通过预检正常上传」为守卫例，红测时即绿）+ transfer-dialog 测试 1 例红（UploadPicker 挂载后 input[webkitdirectory] 与「选择文件夹」按钮存在，红于组件未导出）。music-utils.ts：partitionUploadableFiles（扩展名白名单镜像 worker 的 EXTENSION_FORMAT；0 字节文件计入不支持而非静默消失——旧实现 size>0 过滤是静默丢失）。library-collections.ts：uploadFiles 预检分流 + 每条原因聚合 skip toast（toastUploadSkip，标题用既有幽灵键 upload_unsupported/upload_too_large，新键 upload_skipped_count 给计数）；每任务持 AbortController 传入两路上传 API；uploadOne 返回结局（done/aborted/错误码），取消静默不算失败；批尾一条成功 toast（upload_done 带整批计数）与一条失败 toast（首个错误码，逐行细节仍在任务行）；dismissUpload 改为先 controller.abort() 再移行（types.ts 加 controller 字段，library.ts 透传 get）。api/music.ts：uploadMusicToWebdav/sendUpload 增可选 signal（XHR abort 监听与 uploadMusicTrack 同构）。music-transfer-dialog.tsx：UploadPicker 导出并新增 webkitdirectory 第二输入（非标准属性经 ref 回调 setAttribute，React 类型不带）；DropZone 可选 onChooseFolder 渲染第二按钮；上传行 uploading 时 X 的可访问名改为新键 upload_cancel（取消语义，不再是「关闭」）。locales：新增 upload_choose_folder/upload_cancel/upload_skipped_count，删除不可达的 upload_quota（worker 配额错误复用 payload_too_large 码，客户端无从区分——若要区分需服务端契约变更，登记为已知缺口）。上传成功/失败 toast 无专属 UI 级测试（对话框内按钮点击路径未测），store 层 8 例覆盖行为。验证：track-io+dialog 14 ✅，music 37 套件 221 ✅，typecheck ✅，10 项静态门禁全 ✅（注释白名单 3728，i18n 3068 键，size 基线 17 不变），串行全量（--no-file-parallelism --testTimeout=30000）1830 例全 ✅ exit 0 |
| 2026-09-19 | M-41a UI-16 Segmented 替换手写 radiogroup（视图切换 + 来源筛选） | 43c3cf1b | 新 music-view-toggles.test.ts 4 例全部先红（视图组以 aria-label=music.view_mode 存在为 role=radiogroup、组内恰有一个 tabindex=0 的 radio（roving 焦点）、方向键 ArrowRight 切 grid 且回绕到 list 并同步 store、来源筛选组同样单 tab 停靠点且点击即 setSourceFilter；红于旧实现两组均为裸按钮平铺，无组标签、每钮各占一次 Tab、无方向键）。music-track-list.tsx：ViewModeToggle 改渲染共享 Segmented（label=music.view_mode 新键）；music-hub-toolbar.tsx：SourceFilter 改渲染 Segmented（label=music.source_filter 复用既有键），删去随之无引用的 cn 导入。roving 焦点、方向键/Home/End、aria-checked 全部由 components/form 的 Segmented 统一实现，不再各写一份。locales en/zh 新增 music.view_mode。验证：view-toggles 4 ✅，music 38 套件 225 ✅，typecheck ✅，10 项静态门禁全 ✅（注释白名单 3729，i18n 3069 键，size 基线 17 不变），串行全量（--no-file-parallelism --testTimeout=30000）236 套件 1834 例全 ✅ exit 0 |
| 2026-09-19 | M-41b UI-17 睡眠倒计时 timer 语义 + 溢出容器键盘可达 | 本次提交（hash 由下一次提交回填） | 6 例新增中 5 例先红（新 music-sleep-status.test.ts：倒计时须为 role=timer/aria-live=off，红于旧 role=status/polite 每秒重播——另 1 例「未定时不渲染」为守卫例；now-playing/immersive/empty-states/transfer 各 1 例：溢出容器须 tabIndex=0 且有可访问名，红于缺属性）。immersive 一例首跑红于挂载异常（种子歌词触达 jsdom 缺失的 scrollIntoView），补桩后该例的断言级红改用变异检验：以修复前组件跑该文件仍红（× 1），修复后绿。music-transport-widgets.tsx：倒计时改 role=timer/aria-live=off——启停播报由睡眠按钮既有 aria-pressed 承担，每秒滴答不再进播报队列。music-now-playing.tsx：滚动区补 tabIndex=0 + aria-label 随当前页签（歌词/详情）；music-immersive-player.tsx：歌词滚动区与底部队列滚动区各自 tabIndex=0 + aria-label（music.lyrics / music.queue）；music-webdav-modal.tsx WebdavListing：ul tabIndex=0 + aria-label=music.webdav_title；music-transfer-dialog.tsx TaskSection：ul tabIndex=0 + aria-label=区块标题（三个区块共用同一 label prop）。无新增 i18n 键（复用 details/lyrics/queue/webdav_title/transfers_* 既有键）。验证：五测试文件 18 ✅，music 39 套件 231 ✅，typecheck ✅，10 项静态门禁全 ✅（注释白名单 3735，i18n 3069 键不变，size 基线 17 不变），串行全量（--no-file-parallelism --testTimeout=30000）237 套件 1840 例全 ✅ exit 0 |
