# ADR-0006: 看板卡片依赖的方向与落盘口径

状态：已接受（2026-09-24）

## 背景

看板批次 3 的 KU-23 要求卡片依赖：甘特/时间轴上把「阻塞」画出来，详情面板里能编辑，且手写栅栏塞进环时不能炸。做这个功能前必须先定两件事：**一条依赖往哪个方向算**，以及**这个数据住在哪、怎么落盘**。

此前看板没有「哪张卡等哪张卡」的概念：甘特与时间轴只画各自的条，条与条之间没有关系；`KanbanItem` 上也没有承载它的字段。

## 决策

**方向：`item.dependsOn` 列出的是 `item` 的阻塞者（blockers）。一条边从阻塞者指向被阻塞者——「from 做完，to 才开始」。**

理由：这是阅读方向与数据方向一致的唯一选择。读者在卡片 A 上说「A 等 B」，数据就长在 A 上、指向 B；画在时间轴上时箭头从 B 的条出发、落在 A 的条上，与「B 先发生」的读法相同。反向建模（`blocks`：我阻塞谁）要求读者做一次脑内反转，且「删除阻塞者」与「删除被阻塞者」的语义会分家：前者是修正认知，后者牵连第三张卡。反向建模均无此性质，故不取。

**落盘：住在 `KanbanItem.dependsOn: string[]`，随 JSON 栅栏体往返；outline 体不承载。**

理由与既有口径一致：outline 体本来就无法表达子任务、文件、视图、板标题，而「首次 UI 写入把栅栏升级为 JSON」是这些字段已经依赖的既有通道（`write.ts`）。依赖字段照此搭车，不另造迁移，也不给 outline 的行语法扩「[depends: …]」标记——那会把一张卡的依赖写死在一行括号里，删除一张被引用的卡时无法清理引用它的行。

**环检测：编辑器在写入前拒绝会闭环的整张清单（`withKanbanDependenciesGuarded`），而不是写入后回滚或画警示。**

理由：环一旦落盘，代价会扩散到每个读者（连线成死结、后续任何「关键路径」「自动排期」类功能都先要解环）。编辑期拒绝把代价留在造成它的那一次编辑里，且拒绝以 no-op 形式表现——提交时同一数组原样返回，不产生撤销步，读者不会「撤掉一个本没发生的改动」。检查在「移除该卡自身旧边」后的图上跑：保留一条已有的依赖不能算作新边。

**画法：箭头只存在于甘特/时间轴两个有「天」这一坐标的视图；从阻塞者条的右缘（做完之日）到被阻塞者条的左缘（等待开始之日）。**

理由：依赖是「时间上的先后」，在以天为横轴的视图里它才有位置；看板列、表格行、日历格子都没有一个可以放箭头的「先后」。条的右缘/左缘是两个条上仅有的两个语义端点，中间用向右凸的肘形折线连接，两个条重叠时也不会穿过条的内部。被引用的卡不在当前窗口（无日期、被裁剪）时**不画**，不猜位置。

**缺引用（指向已删除或手写栅栏里不存在的 id）：读取时保留并单独报告（详情面板列出），不静默清洗。**

理由：静默清洗意味着「读一个文档 = 改一个文档」，读者打开面板就会把没动过的栅栏写脏（K-25 的同一条教训：解析器不发明文档里没有的东西）。保留让作者下次编辑时自己看见并处置。

## 后果

- 数据模型只加一个可选数组，旧栅栏（无 `dependsOn`）解析不变；新增字段对旧版本读者是不可见的多余键，不构成破坏性变更。
- 编辑器只提供「不会闭环」的选项，因此正常 UI 路径写不出环；手写栅栏仍可能带环——环在这种情形下被**读**（照常画连线、照常显示），只被**写**拒绝。把「环」升级为解析错误（整板打不开）会是过度反应：读者需要看到板才能修它。
- 「阻塞者完成」目前没有自动状态推断（比如自动把被阻塞卡标灰）：这是有意的最小实现。状态语义属于 select 列，哪些状态算「完成」是每个板自己的口径，替读者决定会让依赖字段变成第二个状态机。后续若做，应作为独立条目回到计划，而不是顺手加进这次。
- 连线是 SVG 叠加在条所在滚动区里的，随缩放（`dayWidth`）与窗口裁剪重算；它不进入打印/导出的静态快照（KU-24 的导出走同一套条几何，依赖连线暂不进导出面），需要时在导出层复用 `kanbanDependencyLinks` 即可。
- 依赖不参与排序/过滤的任何口径：`filter-sort` 不知道这个字段。把它纳入排序（比如拓扑序）同样留给独立条目。

## 与其他决策的边界

- ADR-0002（渲染物跟随主题）：连线颜色取自 CSS 令牌（`var(--border-strong)` 等），SVG 元素不内联颜色，天然跟随主题，不在例外清单上。
- KU-21a/KU-21b 的拖拽改日：依赖不改写日期。拖动被阻塞卡的条不会被阻止，也不会连带移动阻塞者的条——「改期是否撞上依赖」是读者读图后自己做的决定，不是编辑器替他做的限制。
