# Cloudflare 生产部署配置说明
# PUBLIC_API_URL 必须以 PUBLIC_ 为前缀，Astro 才会将其注入至前端客户端代码中
# 本地调试时使用 Inkstone 本地 Worker 地址：
PUBLIC_API_URL=http://localhost:8787

# 部署到 Cloudflare 生产环境后，请修改为您真实的 Inkstone Worker 生产域名，例如：
# PUBLIC_API_URL=https://inkstone.your-domain.workers.dev
