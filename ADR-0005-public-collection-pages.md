# ADR 0005: 公开集合落地页（文件夹 / 标签 → 一页目录）

Status: Accepted（已实现：`/c/:slug` + `POST /api/public/collection/:slug` + 所有者侧的发布/暂停/撤销）

Date: 2026-09-21

## Context

分享模型是**一条链接 = 一篇笔记**，这个前提在所有地方成立：`shares` 表以 `slug` 为键、`/s/:slug` 是整页（`registerSharePageRoutes` → `renderShareShell`，`noindex` + `no-store`）、密码与有效期是**每篇**的属性、访客行以 `slug` 归属、资产会话（`share_asset_sessions`）也按 slug 签发。

但分发侧已经出现了"按集合公开"的动作：`POST /api/share/batch-folder`、`batch-tag` 能把一个文件夹/标签下的所有笔记**一次性开启分享**（SH-54 一轮做过）。结果是所有者拿到了 N 条链接，而访客拿到的是 N 个散页——没有目录、没有"共几篇"、没有下一步去哪。SH-68 要的就是那一页。

现状里可直接复用的：

- 公共阅读页外壳与 SEO 口径（`renderShareShell`：`noindex, nofollow` + `X-Robots-Tag: noindex` + `no-store`），任何新公开页必须与它**同一口径**，不能出现一个可被索引的集合页把散页暴露出来。
- 口令与限流：`scrypt` 校验 + `assertNotLocked` / `consumeAttemptBudget` / `recordLoginFailure`（按 slug 的按页锁定），以及 `share_asset_sessions`（页面通过后签发的短时资产凭证）。
- 成员关系的唯一真相源：`shares.folder_id` 与 `shares.tags`（JSON 数组）。**没有**"集合"这个概念，也没有成员表。

## Decision

### 一、模型：一条"发布记录" + 现算成员

集合页需要两样东西：**稳定的地址与访问策略**（必须落库），以及**它包含哪些分享**（可以从 `shares` 现算）。方案对比：

| 方案 | 评述 |
| --- | --- |
| **A. 新建 `share_collections`（一条发布记录）+ 成员实时派生（推荐）** | 记录 = `{ id, slug, user_id, folder_id 或 tag, password_hash, expires_at, is_enabled, created_at }`；成员 = 每次请求按 `folder_id`/tag 查 `shares`（已启用、未过期、笔记未删除）。发布是一次写入，成员永远与列表一致（"刚公开的那篇立刻出现在目录里"成为可陈述的行为而不是同步 bug）。 |
| B. 成员表（发布时把 note id 快照进去） | 需要双写与一致性维护（新增分享、撤销、取消分享、改文件夹、改标签、软删笔记都要更新成员表），且会静默过期。否决。 |
| C. 不落库，用签名 URL（把 folder/tag 与口令编码进链接） | 无法吊销、无法改口令、无法关闭，且 URL 里带着筛选条件的签名难以轮换。否决。 |
| D. 复用某一条分享的 slug 作为集合地址 | 语义混装（那条分享本身也是一篇），且它的暂停/过期会连带影响整页。否决。 |

**一个文件夹或一个标签最多一条启用的集合记录**（`UNIQUE(user_id, target_type, target_value) WHERE is_enabled = 1`），避免同一集合出现两个口令/两个地址。

### 二、公共契约

- 页面：`GET /c/:slug`（与 `/s/:slug` 并列，共用 `ASSETS` 外壳与 meta 注入），`noindex, nofollow`、`X-Robots-Tag: noindex`、`Cache-Control: no-store`——**与单篇完全一致**。
- 数据：`POST /api/public/collection/:slug`，body `{ password?, ref? }`（与单篇同形，`ref` 若 SH-67 落地则同样采）。响应 = `{ collection: { title, count }, notes: [{ slug, title, excerpt, updatedAt, hasPassword }] }`。
- **未通过口令之前不得泄露任何成员信息**：目录、篇数、标题一律在通过之后返回；口令错误与"不存在/已关闭"必须给出**同一种**响应（沿用单篇的既有做法，避免集合地址成为枚举探针）。
- 访问策略是**集合级**的：一个口令保护整页；成员自身的口令仍然有效（单独打开某篇时照旧校验），且目录里对 `hasPassword` 的成员做出标记。**不**提供"集合口令可以绕过成员口令"的能力。
- 资源：通过页面校验后按**集合**签发资产会话，而不是每篇一个（否则打开一页要签 N 次）；`share_asset_sessions` 的键需要能表达"集合"这一作用域（新增一种前缀，避免与 slug 冲突）。
- 限流与预算：复用按 slug 的 `assertNotLocked` / `consumeAttemptBudget`（键换成集合 slug）与公开读的现有节流；集合体积有上限（例如一次最多返回 100 篇 + 分页游标），防止一页把账号的整库拉出来。
- 访客归属：访客行仍写单篇的 `slug`（这是分析的最小单位）；集合页本身不产生"集合级"访客行。若需要"从集合页进入"的归属，应通过 SH-67 的 `ref`（页面给出的链接自动带 `?ref=collection`）而不是新增字段。

### 三、所有者的界面

- 分享中心新增"集合"视图/区块：列出已发布的集合（目标、篇数、口令、有效期、是否启用），提供暂停/恢复/改口令/改有效期/撤销；发布入口就在现有的批量条/侧栏筛选的"按此文件夹/标签公开"处（已经存在那个动作）。
- **撤销集合不等于撤销分享**：撤销集合只删除发布记录（散页链接仍在，仍可单独访问）——这一点必须在确认文案里写明，反之"暂停集合"也不同于暂停 N 条分享。这是本设计里最容易造成误解的一处，必须靠文案与 UI 区分，而不是靠文档。
- 计数与看板：集合页的访问计入各篇（与现在一致），看板不需要新的 KPI；不要为集合发明一套独立口径的"集合浏览量"，那会与 UV 的去重口径打架。

## Consequences

- 成员实时派生意味着**改动文件夹/标签会立刻改变集合内容**（包括"把一篇从文件夹挪走"会让它从目录消失，而它自己的散页链接仍在）。这是有意的、可陈述的行为；文档与 UI 必须写明"集合是视图，不是快照"。若将来需要冻结快照，那是另一个功能，不能悄悄改变这条语义。
- 新增一张小表与一次迁移（只增）；保留期与清理：撤销集合记录后不留孤儿；成员派生不需要清理。
- 公共表面积增加一个可被探测的路由，因此口令与错误响应的一致性、限流、`noindex` 是**上线前置条件**，不是后续优化。
- AGENTS「全屏归属」「工具栏展开」等既有约定不受影响；但 `scripts/check-surface-coverage.mjs` 的名单是否需要新增（公开页不是 `fixed inset-0` 表面，按现有规则不需要）要在实现时按该脚本的判据核对，而不是想当然。

## 验证

1. **访问策略**：错误口令与不存在的 slug 返回**完全相同**的状态码与 body（逐字节比较，与单篇同一断言方式）；锁定阈值与单篇一致。
2. **不泄露**：未通过口令时响应里不出现成员标题、篇数或 slug（对完整 body 断言不含），且页面外壳的 meta 不含标题（复用 `renderShareShell` 的空标题路径）。
3. **成员派生**：把一篇从文件夹挪走/取消分享/软删笔记后，目录立刻变化（三条独立断言）；暂停的成员不出现；过期的成员不出现。
4. **上限与游标**：101 篇时返回 100 + 游标，第二页不重不漏。
5. **SEO 与缓存**：响应头与单篇一致（`noindex`、`no-store`、`X-Robots-Tag`）。
6. **隔离**：别的账号的集合 slug 不可访问；集合里的成员一定属于同一账号（断言查询带 `user_id`，复用 SH-80 的教训）。
7. **可访问性**：目录是真正的列表/表格语义、键盘可达、每篇有可访问名称（链接文本即标题），空态/失败态可读。

## 交付分期（实际交付情况）

1. **一期：已完成**。只读集合页、成员派生 + 上限 + `noindex`，所有者侧的发布/暂停/撤销/改策略。
2. **二期：口令与限流已完成**（集合级口令、锁定/限流、错误响应逐字节一致）。资产会话作用域**未做**，理由见上面"局限①"：当前集合页只渲染目录与摘要，不请求附件，先建一套用不上的凭证不如留到真有需要时；要做的话是把 `share_asset_sessions.slug` 换成可寻址的作用域而非另建一张表。
3. **三期：已完成**。目录项统一带 `?ref=collection`（与其它营销位共用 ADR-0004 的通道，未新增字段）；排序与置顶沿用分享列表规则（见"已定的取值"第 3 条）。

## 已定的取值（实现时裁定，取代原先的"待评审的问题"）

1. **一个目标一条启用记录：够用，且是发布路径的规则而不只是约束**。重复发布同一目标不是失败也不是第二条地址，而是**改写同一条记录的策略**（口令、截止时间）并返回同一地址——这正是"改口令"的实现方式（见下面第 5 条）。因此不需要"同一集合两个口令"；真有那种需求，该拆成两次不同目标的发布。暂停中的记录不占用唯一约束（部分唯一索引 `WHERE is_enabled = 1`），所以"暂停后再发布一次"会得到一条新记录，旧的恢复时会拿到 400 并说明冲突——而不是撞在约束上。
2. **成员口令提示角标：显示**。它在**通过集合口令之后**才出现，诚实反映访客点开后必然遇到的事（点进去还要输口令），而"提前告知"正是目录页存在的理由。未通过口令前整页什么都不给，所以这一角标不泄露任何未授权信息。
3. **默认排序 = 分享列表的排序**（`is_pinned DESC, updated_at DESC, slug DESC`）。集合页是同一份列表的访客视角，不引入第二种排列；游标就是这三列（`(is_pinned, updated_at, slug)` 复合键），不靠 offset，因为新增一篇会把后续页整体错位。
4. **不展示未公开的成员**（已按默认执行）。未开启/已过期/笔记已删的成员不在目录里出现，也不计入 `count`——集合是公开目录而"未公开"在访客世界里不存在；把它们灰显会变成一种公布（"这里曾有一篇"）。
5. **撤销用 confirm + 文案说明"带走什么"**，强度与批量撤销同级（不可撤销的操作都走同一个确认组件）。描述里写明"它列出的链接照旧可用，各自的口令与访问记录不受影响"——这是本设计里最容易被反向理解的一处，因此它也有一条**断言在测这句文案**，而不是只靠文档。

## 实现记录

- 迁移 42：`share_collections`（id、slug、user_id、target_type、target_value、password_hash、expires_at、is_enabled、created_at、updated_at）+ 三个索引（slug 唯一、`(user_id, target_type, target_value) WHERE is_enabled = 1` 部分唯一、`(user_id, created_at DESC)`）。
- **成员关系只写一次**：`src/worker/lib/share-collections.ts` 的 `collectionMemberPredicate()` 同时供目录查询与计数使用；两条语句的 WHERE 由同一个谓词拼出，因此"面板上的数字"与"访客看到的条数"不可能不一致。文件夹按 `folder_id`，标签按 `s.tags LIKE '%"id"%'`（与分享列表筛选同一规则）。
- 公共路由：`POST /api/public/collection/:slug`（body `{password?}`，query `limit`/`cursor`）；`GET /c/:slug` 复用 `renderShareShell`，因此 `noindex, nofollow`、`X-Robots-Tag: noindex`、`Cache-Control: no-store` 与单篇**同一口径**（同一函数，不是同一份拷贝）。
- 所有者路由：`GET/POST /api/share/collections`、`PATCH /api/share/collections/:id`（**只能暂停与恢复**）、`DELETE /api/share/collections/:id`。口令与截止时间属于"已发布"的含义，只能经发布路径重新陈述——一个写入路径，不给自己留两个可能口径不同的口子。注册顺序在 `/:noteId` **之前**：否则单段的 `/collections` 会被笔记路由吃掉（实现时真撞到过，答的是"笔记不存在"）。
- 口令门与单篇同形：同样的 `scrypt` 校验、同样的 `consumeAttemptBudget` / `assertNotLocked` / `recordLoginFailure`（键换成 `collection-*`），"没带口令"与"口令错误"返回**逐字节相同**的 401。
- 上限：一次最多 100 篇（默认 50）+ 游标分页；一个账号最多 20 条启用集合（否则所有者列表的逐条 live count 会随滥用增长）。
- 客户端：分享中心侧栏新增"集合"类别（自成视图，不经过列表工具栏与批量条），面板是真表格 + `scope` 表头，四个行内动作（复制地址、改口令/截止时间、暂停/重新发布、撤销）；发布/改策略共用一个对话框，它的截止时间选项**复用分享编辑器的 `shareExpiryOptions()`**（同一套"一天/一周/一个月/永不"，不另立一套）。访客侧是独立懒加载块 `collection-page/`，目录项指向 `/s/<slug>?ref=collection`（第三期，走 ADR-0004 已有通道）。演示模式有同一套路由与同一份种子集合。

## 验证读数（2026-09-21）

- worker 14 例（`tests/share-collections.test.ts`）：成员实时派生（发布后再添一篇立刻出现）、被暂停/过期/软删/移出的成员立即消失、标签按 id 而非前缀匹配且**不跨账号**、未知/暂停/过期三种状态的响应逐字节相同、错口令与无口令逐字节相同、连续猜错后连正确口令也 429、外壳头与标题保护、101 篇翻页不重不漏、（未铸造的游标 400）、置顶优先、重复发布改写同一地址、非本人的目标 404、live count 与暂停/恢复/撤销（并断言**撤销后散页仍启用**）、暂停后再发布导致恢复冲突 400、账号上限。
- **8 发变异全部命中**（各自只杀目标）：去掉启用/过期守卫、去掉软删守卫、去掉账号守卫、标签改前缀匹配、去掉集合状态守卫、错口令换个响应、计数忽略成员谓词、坏游标当首页处理。其中一处**第一轮没杀掉**：标签前缀变异最初没被识别，因为示例里的"近似 id"（`…01` vs `…10`）互不为前缀——把近似值改成真正包含关系（`…001` 包含于 `…0010`）后，那一发才真的杀掉。这条记进台账：假绿往往不是断言错，而是**夹具选得不够近**。
- 客户端 10 例（`share-collections-panel.test.ts`）+ 6 发变异全中（表格拆成 div、跳过确认、撤销文案不再说明散页不受影响、空口令当空串提交、暂停后不重拉列表、目标类型固定写成文件夹）。其中两处测试写法上的教训：弹窗落在 `document.body` 的 portal 上，因此**每个用例前清空 body**（否则后一个用例会点到前一个弹窗的按钮——"发布按钮应是禁用的"曾因此读到上一个弹窗的启用按钮）；受控 `select` 在 portal 里无法用派发事件改写，改测**编辑既有集合**的路径（`initialTarget`）——这正是面板"改口令"按钮走的路径，覆盖等价而不再靠 DOM 把戏。
- 全量单测（本批相关面）：`features/share` + `lib/api` + `demo` + `tests/share-routes` + `src/shared` **59 文件 / 472 用例全绿**；13 项静态门禁全绿。
- 成本：目录查询与计数都是窄查询（`shares` 按 `user_id` + 目标谓词，`notes` 走主键 join）；未在真实 D1 上量，也未加新索引——集合的规模由分页上限与账号上限约束，成员谓词用得到的 `idx_shares_folder` 已存在。
- 局限：① 资产会话仍按**单篇**签发，集合口令不会代替成员自己的口令（ADR 里的"集合级资产会话"因此**未做**，理由是当前目录页不渲染附件；若将来目录页要预览图片，那是二期剩下的那一小块）；② 演示模式的集合口令是内存里的明文比对（演示不涉及真实凭据，但也不假装有 scrypt）；③ 未做集合级访问日志（集合不产生访客行，归属靠成员的 `?ref=collection`）。
