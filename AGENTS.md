# AGENTS.md

## 核心思想

- 第一性原理：先弄清需求背后的真实目标，再选方案，不套模板。
- 解决当前本质问题，不为假想的未来需求预设计。
- 在可维护的前提下，选最简单、可靠、清晰的实现。
- 代码服务业务目标，不为展示技术复杂度。

## 铁律（不可违反）

1. **单文件 ≤ 500 行**，单函数 ≤ 50 行，嵌套 ≤ 3 层。超限即拆分。
2. **禁止硬编码**：颜色/间距/字号/圆角/层级等视觉相关值一律用设计令牌（CSS 变量 / 主题变量 / 样式常量表），魔法数字提为具名常量，URL/密钥/开关走配置文件或环境变量。
3. **禁止复制粘贴逻辑**：出现第二次即抽取；但不为消除 1–2 处小重复造抽象。
4. **禁止跨层直接依赖**：UI 层不碰数据库/网络请求，业务逻辑不依赖具体 UI 实现，模块间只通过公开接口（如 `index`）通信，禁止引用其他模块内部深路径文件。
5. **禁止静默失败**：不允许空 catch、忽略返回的错误、屏蔽类型/编译警告；错误要么处理、要么带上下文向上抛出。唯一例外是确属可选的尽力而为（best-effort）后台操作（如缓存清理、跨标签广播、会话刷新、探活重试）：允许吞错，但 catch 体必须紧邻归因注释说明为何可忽略；无注释空 catch 由 `scripts/check-empty-catches.mjs` 门禁拦截，注释文本再受 `check-comments.mjs` 白名单约束。
6. **禁止留死代码**：不用的代码、注释掉的代码、废弃 fallback、兼容层直接删，历史版本在 git 里，不用留在代码里；不为保持向后兼容而长期保留废弃方案。
7. **禁止重复造轮子，也禁止乱加依赖**：优先用成熟、稳定、维护良好的第三方库解决问题，不自己重写一遍；但引入新依赖前必须先查已有依赖能否解决，并说明理由。
8. **禁止未验证就提交**：改动必须过静态检查（lint/类型检查，如适用）和相关测试；新增逻辑必须有可运行的验证方式（手动或自动）。
9. **禁止无脑全局状态**：状态就近放置在使用它的最小范围，能局部不提升，能派生计算不重复存储。
10. **禁止修改不理解的代码**：改前先读调用方与测试用例，弄清副作用范围。
11. **禁止一个模块承担多种职责**：模块/文件/函数只做好一件事，职责边界不清时先拆分再写。
12. **禁止使用原生 HTML 元素作为交互控件**：不得直接使用 `<button>`、`<label>`、`<textarea>` 等标签而不应用自定义样式或封装组件。所有交互元素必须使用项目内的 UI 组件库或自定义组件，并统一应用设计系统样式。
13. **禁止使用原生弹窗/提示框**：严禁调用 `window.alert()`、`window.confirm()`、`window.prompt()` 等原生浏览器 API。所有用户提示、确认、输入弹窗必须使用项目封装的 Toast、Modal、Dialog 等组件替代。
14. **统一样式来源**：所有样式必须通过 CSS 模块、CSS-in-JS 或全局样式类进行管理，避免依赖浏览器默认样式。


## 写代码前必须过的清单

按顺序自问，任一项不满足先重构再动手：

- **单一职责**：这个模块/函数是否只做一件事？能否用一句话说清它的职责？
- **复用性**：现有代码/函数/工具/组件能否直接用或小改后用？是否已有成熟库能解决，无需自己写？
- **简洁性**：能否用更少的概念、更少的文件、更少的分支实现？当前简单方案是否已满足需求，不必升级为复杂方案？
- **可测性**：核心逻辑是否是纯函数（相同输入永远相同输出）？副作用（IO/网络/DOM/存储）是否被隔离到边界？
- **可维护性**：半年后接手的人能否 5 分钟看懂？命名是否自解释、是否需要注释才能理解？
- **可配置性**：会变的值（阈值、文案、路径、开关）是否外置成配置？但不为不会变的东西加配置。
- **解耦**：模块间是否只依赖对方的接口而非实现细节？删掉这个模块，其他模块是否只需改一处引用？
- **模块化**：目录结构是否按业务域组织，而非把所有同类型文件堆在一个大目录里？
- **工程化**：这条规则/约束能否被工具（lint、类型检查、测试、CI）自动强制，而不是靠人记住？

## 代码规范（框架无关）

- **格式化**：全树统一 2 空格缩进、无行尾分号、字符串用单引号；由 `scripts/check-code-style.mjs` 门禁强制（AST 扫描，`for (;;)` 头与需要双引号的字符串豁免）。
- **命名**：文件名统一风格（推荐 kebab-case），可复用单元（组件/类/模块）用 PascalCase 或该语言惯例，函数/变量用 camelCase 或 snake_case（跟随语言惯例，项目内统一），常量用 UPPER_SNAKE_CASE，布尔值用 `is/has/can/should` 前缀，事件处理函数用 `handleXxx` 或 `onXxxHandler`，对外暴露的回调参数用 `onXxx`。
- **函数**：单一职责，参数超过 3 个时改用对象/结构体传参，优先纯函数，用早返回减少嵌套层级。
- **类型/契约**：有静态类型系统的语言把公共接口的类型集中定义并导出；无静态类型的语言在模块入口用运行时校验（schema 校验、断言）保证外部输入合法。
- **UI 单元（组件/视图）**：展示逻辑与业务逻辑分离（状态管理/数据获取放在独立层，UI 只负责渲染），跨层级共享状态优先使用所在框架的依赖注入机制（如 context / provide-inject / store / 全局状态容器），避免逐层手动透传超过 2 层。
- **样式**：所有视觉数值统一走设计令牌或样式变量表，禁止在标记语言中散落硬编码像素值/颜色值；响应式断点、层级（z-index）等统一定义在一处；避免使用样式覆盖的"最高优先级"技巧（如 `!important` 或等价手段）。
- **注释**：只解释"为什么这样做"，不解释"这行代码在做什么"（代码本身该自解释）；TODO 必须带 issue 编号或负责人，不留无主 TODO。
- **导入/依赖顺序**：外部依赖 → 内部跨模块引用（别名路径） → 同模块相对引用 → 样式文件；禁止模块间循环依赖。
- **异步与错误处理**：统一使用语言/框架推荐的异步写法（如 `async/await` 或等价机制），在调用边界统一处理加载中/失败/空结果三种状态，不允许裸露的未处理 Promise/回调错误。

## 设计原则

- **KISS**：简单直接优先，不为"看起来优雅"增加实际复杂度。
- **DRY**：消除重复逻辑，但抽象要等到同一模式第三次出现且形态稳定时再提取。
- **SOLID**：职责单一、依赖抽象而非具体实现、对扩展开放对修改关闭；不为套用原则而套用原则。
- **YAGNI**：不添加当前用不到的抽象层、配置项、扩展点；如果简单方案已经满足需求，不要主动升级为复杂方案。
- **渐进式演进**：从最小可工作版本开始，每次改动都建立在已验证可运行的系统之上，不做未经验证的提前架构设计。
- **不做长期兼容包袱**：不为向后兼容无限期保留旧方案，确认迁移完成后直接删除旧代码，而非叠加 fallback。
- **不牺牲当前可用性**：永远不用"未来可能需要的复杂性"，换取当前产品可用性的下降。

## 提交前自检

- [ ] 文件 ≤ 500 行，无硬编码，无静默失败，无死代码
- [ ] 静态检查 / 类型检查（如适用）/ 测试全部通过
- [ ] 改动最小化，不夹带无关的格式化或重排
- [ ] commit message 符合下方规范

---

# Git 提交规范

基于 [Conventional Commits](https://www.conventionalcommits.org/)，正文逐文件说明改动。

## 格式

```
<type>(<scope>): <subject>

- <path/to/file>: <改动内容，到方法/组件级>
- <path/to/file>: <改动内容，到方法/组件级>

[可选补充说明]
[可选 footer：BREAKING CHANGE: ... / Closes #123]
```

- `subject`：祈使句，≤ 50 字符，无句号，可中文。
- 正文至少一条 `- 路径: 改动` 行，写"做了什么"而非"文件是什么"。
  - ✅ `tasks-provider.tsx: 新增 importTasks()，经 nextTaskId 防 id 冲突`
  - ❌ `修改了 tasks 模块文件`

## type

| type       | 用途                 |
| ---------- | -------------------- |
| `feat`     | 新功能               |
| `fix`      | 修 bug               |
| `refactor` | 不改行为的重构       |
| `perf`     | 性能优化             |
| `test`     | 测试                 |
| `docs`     | 仅文档               |
| `style`    | 纯格式（不影响逻辑） |
| `build`    | 构建配置、依赖变更   |
| `ci`       | CI 配置              |
| `chore`    | 杂项                 |
| `revert`   | 回退                 |

## scope

业务模块 id：`notes` `folders` `tags` `editor` `preview` `graph` `command` `sidebar` `auth` `files` `backup` `share` `mcp` `settings` `sync` `templates` `update` `blog`

基础设施 scope：

| scope       | 范围                                   |
| ----------- | -------------------------------------- |
| `theme`     | 主题引擎 / 设计令牌                    |
| `shell`     | 布局壳、导航壳层                       |
| `i18n`      | 国际化、locales、check-i18n            |
| `workspace` | 工作区状态（pane、分栏、活动笔记）     |
| `ui`        | `components/*` 通用组件                |
| `db`        | 数据库、本地持久化、迁移               |
| `infra`     | 构建 / CI / Wrangler / 脚本            |
| `docs`      | 跨多文档或文档规范                     |

## 示例

```
feat(tasks): 支持粘贴 JSON 批量导入任务

- modules/tasks/components/tasks-import-dialog.tsx: 新增导入对话框（textarea 粘贴 + Zod 逐条校验，错误行号反馈）
- modules/tasks/components/tasks-provider.tsx: 新增 importTasks()，经 nextTaskId(existing) 生成不冲突的 TASK-<n> id
- modules/tasks/locales.ts: 补导入对话框三语文案
```

```
fix(theme): 修复暗色切换时派生 CSS 变量不重放

- context/shadcn-theme-provider.tsx: MutationObserver 回调改读 configRef.current，保证 class 翻转时应用最新配置
```